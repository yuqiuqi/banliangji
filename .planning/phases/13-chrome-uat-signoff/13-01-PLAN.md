---
phase: 13-chrome-uat-signoff
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/screens/ChartScreen.tsx
  - src/screens/BudgetScreen.tsx
  - src/screens/AssetScreen.tsx
  - src/screens/MineScreen.tsx
  - src/theme/palette.ts
  - UI-SPEC.md
  - .planning/phases/13-chrome-uat-signoff/13-VERIFICATION.md
autonomous: true
requirements:
  - LG-02
  - THEME-01
  - A11Y-01
  - LG-01
must_haves:
  truths:
    - "四屏（Chart/Budget/Asset/Mine）无 **长期裸 hex** 作为唯一强调色（预算超支等须走 **palette/iosSemantic** 或 Accepted deviation 表）"
    - "`npm run verify` 退出码 0"
    - "存在可编辑的 `13-VERIFICATION.md`，含 Tier-2 与 **降低透明度** 勾选清单"
  artifacts:
    - .planning/phases/13-chrome-uat-signoff/13-VERIFICATION.md
---

<objective>
落实 **`13-CONTEXT.md` D13-01…D13-06**：按 **Chart → Budget → Asset → Mine** 将 **LG-02** 副路径屏对齐 **iOS 26 Chrome / Liquid Glass** 与 Tier-1 原语；结转 **THEME/A11Y/LG-01** 的矩阵验收写入 **`13-VERIFICATION.md`**。
</objective>

<threat_model>
## Trust boundaries
- 本地 UI；无新网络。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P13-01 | Tampering | N/A — 表现层 |
</threat_model>

<context>
@.planning/phases/13-chrome-uat-signoff/13-CONTEXT.md
@.planning/research/IOS26-LIQUID-GLASS-REFERENCE.md
@UI-SPEC.md
@.planning/phases/11-chrome-depth/11-MATERIAL-MOTION-SPEC.md
</context>

<tasks>

<task type="execute">
  <name>ChartScreen — Tier-2 Chrome（LG-02）</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>src/screens/ChartScreen.tsx,src/screens/HomeScreen.tsx,src/components/ios/GroupedInset.tsx,.planning/phases/13-chrome-uat-signoff/13-CONTEXT.md</read_first>
  <action>
    按 **D13-03 / D13-04**：周/月/年 **chip 行** 与 KPI/趋势 **卡片区** 使用与 Tier-1 一致的 **语义色、圆角、分隔、按压反馈**；优先复用 `src/components/ios` 原语；**动效** 新增/重写优先 **Reanimated** 与 `11-MATERIAL-MOTION-SPEC`。去除与 **Hierarchy** 冲突的过重阴影堆叠（改为规范分层或 deviation）。
  </action>
  <acceptance_criteria>
    - `rg "GroupedInset|ListRow|ios/" src/screens/ChartScreen.tsx` 至少 1 匹配 **或** `13-VERIFICATION.md` 中本屏记 **Accepted deviation** 及理由
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BudgetScreen — Tier-2 Chrome + 语义危险色</name>
  <files>src/screens/BudgetScreen.tsx,src/theme/palette.ts</files>
  <read_first>src/screens/BudgetScreen.tsx,src/theme/palette.ts,src/screens/HomeScreen.tsx,.planning/phases/13-chrome-uat-signoff/13-CONTEXT.md</read_first>
  <action>
    按 **D13-02 / D13-05**：顶栏与卡片区对齐 **Liquid Glass 分层**（非孤立实心 `colors.main` 块）；**OVER_BUDGET** 等改为 **palette 令牌**（可新增 `danger`/`onDanger` 对）；Modal **scrim** 与卡片圆角/阴影对齐 `UI-SPEC`。
  </action>
  <acceptance_criteria>
    - `rg "OVER_BUDGET_COLOR|#dc3545" src/screens/BudgetScreen.tsx` **无**匹配（或仅在注释说明已迁移）
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>AssetScreen — Tier-2 Chrome + Sheet</name>
  <files>src/screens/AssetScreen.tsx,src/theme/palette.ts</files>
  <read_first>src/screens/AssetScreen.tsx,src/screens/BudgetScreen.tsx,src/theme/palette.ts</read_first>
  <action>
    与 Budget 同期原则：**顶栏分层**、列表/卡片 **Grouped 范式**、**Modal scrim** 令牌化；按钮与 `fluentButton` 阴影与 Tier-1 一致。
  </action>
  <acceptance_criteria>
    - `rg "backgroundColor: colors\\.main" src/screens/AssetScreen.tsx` **无**匹配于 **header** 样式块 **或** `13-VERIFICATION` 记 deviation（须含迁移计划）
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>MineScreen — 顶栏与分组收尾</name>
  <files>src/screens/MineScreen.tsx</files>
  <read_first>src/screens/MineScreen.tsx,src/screens/HomeScreen.tsx</read_first>
  <action>
    **D13-02**：在已用 `GroupedInset` 基础上，将 **大标题/顶区** 与 Tier-1 **栈顶 Chrome** 对齐（避免孤立纯色顶块）；按压与排版遵循 `iosType` / `UI-SPEC`。
  </action>
  <acceptance_criteria>
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>13-VERIFICATION — Tier-2 + 降低透明度矩阵</name>
  <files>.planning/phases/13-chrome-uat-signoff/13-VERIFICATION.md</files>
  <read_first>.planning/REQUIREMENTS.md,.planning/phases/13-chrome-uat-signoff/13-CONTEXT.md</read_first>
  <action>
    新建 `13-VERIFICATION.md`：**LG-02** 四屏在 **浅色 / 深色 / 降低透明度** 下的 spot-check 表；**THEME-01 / A11Y-01 / LG-01** 结转项与 **11-VERIFICATION** 交叉引用；**Accepted deviation** 表（若有）。
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/13-chrome-uat-signoff/13-VERIFICATION.md`
    - `rg "LG-02|降低透明度|Chart|Budget|Asset|Mine" .planning/phases/13-chrome-uat-signoff/13-VERIFICATION.md` 至少 4 匹配
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| REQUIREMENTS LG-02 | 四副路径屏 | 01:任务 1–4 |
| ROADMAP Phase 13 | 验收与 deviation | 01:任务 5 |
