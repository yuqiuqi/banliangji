---
phase: 14-ios26-motion-polish
plan: 02
type: execute
wave: 2
depends_on:
  - "14-01"
gap_closure: false
files_modified:
  - src/screens/ChartScreen.tsx
  - src/components/ios/SegmentedTwo.tsx
autonomous: true
requirements:
  - ANIM-02
  - ANIM-03
must_haves:
  truths:
    - "Chart 周/月/年与 `SegmentedTwo` 使用 **Spring Thumb**（`SPRING.THUMB`），指示器 `withSpring` 跟手"
    - "柱图数据切换时：**取消/覆盖** 未完成 stagger；**SharedValue 数组** 符合 hooks 规则（ref + effect）"
    - "柱 stagger 仅前 12 根；超出无递增 delay"
    - "图表区 fade 优先 Reanimated `withTiming`，`chartFadeMs` 引用 `FADE_MS` 或 motion 常量"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/screens/ChartScreen.tsx
---

<objective>
Wave 2：**图表分段 + 柱 stagger** — ANIM-02 Spring Thumb；ANIM-03 Reanimated 柱生长、周期切换动画取消、合规 SharedValue 管理；对齐 **14-REVIEWS.md**（race、caps、hooks）。
</objective>

<threat_model>
## Trust boundaries
- 仅表现层；无持久化 schema 变更。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P14-03 | Tampering | N/A |
</threat_model>

<context>
@.planning/research/IOS26-MOTION-INTERACTION-SPEC.md
@.planning/phases/14-ios26-motion-polish/14-UI-SPEC.md
@.planning/phases/14-ios26-motion-polish/14-REVIEWS.md
@src/theme/motion.ts
</context>

<tasks>

<task type="execute">
  <name>SegmentedTwo / Chart 分段 — Spring Thumb（ANIM-02）</name>
  <files>src/components/ios/SegmentedTwo.tsx,src/screens/ChartScreen.tsx</files>
  <read_first>src/components/ios/SegmentedTwo.tsx,src/screens/ChartScreen.tsx,src/theme/motion.ts,src/theme/layout.ts</read_first>
  <action>
    1. 在 `SegmentedTwo`（及 Chart 内联分段若未复用）实现 **浮动拇指层**：`thumbX`、`thumbWidth` 为 `useSharedValue`，在 `onLayout` 记录各段 `x/width`；选中变化时 `thumbX.value = withSpring(targetX, SPRING.THUMB)`、`thumbWidth.value = withSpring(targetW, SPRING.THUMB)`。  
    2. 拇指样式：`backgroundColor` 使用语义 surface（如 `colors.surface` 或 Tier-2 白块），`shadows.keyCap` 自 `src/theme/layout.ts` 若存在。  
    3. 快速连点：不对 thumb 做「重置为 0 再动画」，仅更新目标。  
    4. `useReduceMotion`：拇指改为短 `withTiming` 或禁用位移动画。  
    5. 分段切换时调用 `haptics.select`（自 `src/utils/haptics.ts`）。
  </action>
  <acceptance_criteria>
    - `rg "SPRING\\.THUMB|withSpring" src/components/ios/SegmentedTwo.tsx` 至少 2 匹配 **或** `rg "SPRING\\.THUMB" src/screens/ChartScreen.tsx` 至少 2 匹配
    - `rg "thumbX|thumbWidth" src/components/ios/SegmentedTwo.tsx` 至少 1 匹配 **或** Chart 内联实现中有同等命名
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>ChartScreen 柱 stagger + fade（ANIM-03）</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>src/screens/ChartScreen.tsx,src/theme/motion.ts,src/hooks/useReduceMotion.ts</read_first>
  <action>
    1. 将柱高度动画迁移为 **Reanimated**：每柱 `height` 或 `scaleY` 使用 `useSharedValue`；**数组**使用 `useRef<SharedValue<number>[]>([])`，在 `useEffect` 中按 `bars.length` 增删 `useSharedValue(0)`，**禁止** `points.map(() => useSharedValue(0))` 在 render/useMemo。  
    2. 数据或周期（周/月/年）变化时：递增 `generation` ref 或 `cancelAnimation` / 直接赋值覆盖；清除任何 `setTimeout` stagger id（若用 JS 调度 delay）。  
    3. Stagger：第 `i` 根柱延迟 `min(i, 11) * 30` ms（最多前 12 根）；Reduce Motion：无 stagger，直接目标高度。  
    4. 图表容器 fade：`withTiming`，时长 `FADE_MS.normal`（200）或 spec 中 `chartFadeMs` 映射到 `FADE_MS`。  
    5. 移除或降级对 `Animated.timing`（RN Animated）的依赖作为主路径。
  </action>
  <acceptance_criteria>
    - `rg "useMemo\\(.*useSharedValue" src/screens/ChartScreen.tsx` **无**匹配
    - `rg "withSpring\\(SPRING\\.UI" src/screens/ChartScreen.tsx` 至少 1 匹配
    - `rg "min\\(i, 11\\)|STAGGER|30" src/screens/ChartScreen.tsx` 至少 1 匹配（stagger 上限实现证据）
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit
- REQUIREMENTS：ANIM-02, ANIM-03 ✓
- 14-REVIEWS：stagger cancel、max count、hooks ✓
