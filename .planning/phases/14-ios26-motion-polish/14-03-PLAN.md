---
phase: 14-ios26-motion-polish
plan: 03
type: execute
wave: 3
depends_on:
  - "14-01"
  - "14-02"
gap_closure: false
files_modified:
  - src/screens/HomeScreen.tsx
  - src/screens/ChartScreen.tsx
  - src/screens/BudgetScreen.tsx
  - src/screens/AssetScreen.tsx
  - src/screens/MineScreen.tsx
  - src/screens/BillDetailScreen.tsx
  - src/screens/CalendarScreen.tsx
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements:
  - ANIM-04
  - ANIM-05
  - ANIM-06
  - INT-01
  - HAP-01
  - MOT-01
must_haves:
  truths:
    - "Home / Chart 列表 / Asset 列表：入场 stagger 前 12 行，`FadeInDown`/`entering` + springify；Reduce Motion 无 stagger"
    - "Budget / Asset Modal：sheet `translateY` + `SPRING.SHEET`，scrim `withTiming` + `colors.modalScrim`"
    - "Budget / Asset / Mine：Scroll Header Collapse 72→44；**contentInset 或等效补偿** 避免列表跳变"
    - "残余 `Pressable`（主路径可点击）替换为 `SpringPressable` 或登记 deviation"
    - "保存/删除/分段/modal 等路径已调用 `haptics` 映射"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/screens/BudgetScreen.tsx
    - src/screens/AssetScreen.tsx
---

<objective>
Wave 3：**列表入场、Modal Spring、顶栏折叠、全站 SpringPressable 收口** — ANIM-04~06；INT-01 全屏迁移；HAP-01 主路径补全；MOT-01 与各动效联动。
</objective>

<threat_model>
## Trust boundaries
- 本地 UI；Modal 与导航无新权限。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P14-04 | Tampering | N/A |
</threat_model>

<context>
@.planning/phases/14-ios26-motion-polish/14-UI-SPEC.md
@.planning/research/IOS26-MOTION-INTERACTION-SPEC.md
@.planning/phases/14-ios26-motion-polish/14-REVIEWS.md
</context>

<tasks>

<task type="execute">
  <name>列表入场 stagger（ANIM-04）</name>
  <files>src/screens/HomeScreen.tsx,src/screens/ChartScreen.tsx,src/screens/AssetScreen.tsx</files>
  <read_first>src/screens/HomeScreen.tsx,src/screens/ChartScreen.tsx,src/screens/AssetScreen.tsx,src/hooks/useReduceMotion.ts,src/theme/motion.ts</read_first>
  <action>
    1. 使用 Reanimated 3/4 `FadeInDown.delay(i * 40).springify()`（或等价 `entering`），对 **Home 账单列表**、**Chart 分类列表**、**Asset 账户列表** 的行容器包裹。  
    2. **`STAGGER_MAX = 12`**：`i >= 12` 时 `delay(0)` 或固定 `delay(480)`，不对无限列表线性放大 delay。  
    3. 数据源快速变化（筛选/切换周期）：`useEffect` cleanup 清除 `setTimeout` ids；列表 `key` 与动画 generation 协调，避免重复入场。  
    4. `useReduceMotion`：禁用 stagger，直接显示 opacity 1。
  </action>
  <acceptance_criteria>
    - `rg "FadeInDown|entering" src/screens/HomeScreen.tsx` 至少 1 匹配
    - `rg "FadeInDown|entering" src/screens/ChartScreen.tsx` 至少 1 匹配
    - `rg "FadeInDown|entering" src/screens/AssetScreen.tsx` 至少 1 匹配
    - `rg "12|STAGGER_MAX" src/screens/HomeScreen.tsx src/screens/ChartScreen.tsx src/screens/AssetScreen.tsx` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Modal Spring（ANIM-05）+ haptics</name>
  <files>src/screens/BudgetScreen.tsx,src/screens/AssetScreen.tsx</files>
  <read_first>src/screens/BudgetScreen.tsx,src/screens/AssetScreen.tsx,src/theme/motion.ts,src/theme/palette.ts,src/utils/haptics.ts</read_first>
  <action>
    1. 将 Budget / Asset 的全屏 `Modal` 内容区改为 Reanimated：`translateY` 从 **60** 到 **0**，`withSpring(SPRING.SHEET)`；关闭反向。  
    2. 背景 scrim：`opacity` 0→1 `withTiming(250)`（ms 与 `FADE_MS` 对齐）；`backgroundColor: colors.modalScrim`。  
    3. Modal `onShow` 或打开路径调用 `haptics.light`；保存成功 `haptics.success`；删除确认 `haptics.error`（与 HAP-01 表一致）。  
    4. Reduce Motion：`translateY` 改为短 timing 或减小位移。
  </action>
  <acceptance_criteria>
    - `rg "SPRING\\.SHEET|translateY" src/screens/BudgetScreen.tsx` 至少 1 匹配
    - `rg "SPRING\\.SHEET|translateY" src/screens/AssetScreen.tsx` 至少 1 匹配
    - `rg "modalScrim" src/screens/BudgetScreen.tsx src/screens/AssetScreen.tsx` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Scroll Header Collapse（ANIM-06）</name>
  <files>src/screens/BudgetScreen.tsx,src/screens/AssetScreen.tsx,src/screens/MineScreen.tsx</files>
  <read_first>src/screens/BudgetScreen.tsx,src/screens/AssetScreen.tsx,src/screens/MineScreen.tsx,.planning/phases/11-chrome-depth/11-MATERIAL-MOTION-SPEC.md</read_first>
  <action>
    1. 三屏实现：**大标题区高度** 随 `scrollY` 从 **72** 插值到 **44**（pt，按 `PixelRatio`/`useWindowDimensions` 与现有布局一致）；顶栏 hairline / 阴影与滚动联动。  
    2. **列表跳变防护**：对主 `ScrollView`/`FlatList` 设置与动画头高度相关的 **`contentInset`（iOS）或 paddingTop** 同步策略，或 `Animated.event` 同步 `contentOffset`；验收目标：快速滚动时内容不「闪跳」。  
    3. ChartScreen **不**做折叠（ROADMAP 已注明）。  
    4. 使用 `SPRING.UI` 或 `withTiming` 插值头高度（按 reduce motion 降级）。
  </action>
  <acceptance_criteria>
    - `rg "scrollY|useAnimatedScrollHandler|72|44" src/screens/BudgetScreen.tsx` 至少 2 匹配
    - `rg "scrollY|useAnimatedScrollHandler|72|44" src/screens/AssetScreen.tsx` 至少 2 匹配
    - `rg "scrollY|useAnimatedScrollHandler|72|44" src/screens/MineScreen.tsx` 至少 2 匹配
    - `rg "contentInset|paddingTop|scrollEventThrottle" src/screens/BudgetScreen.tsx src/screens/AssetScreen.tsx src/screens/MineScreen.tsx` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>全站 SpringPressable 收口 + 副屏 haptics（INT-01 / HAP-01 / MOT-01）</name>
  <files>src/screens/BudgetScreen.tsx,src/screens/AssetScreen.tsx,src/screens/MineScreen.tsx,src/screens/BillDetailScreen.tsx,src/screens/CalendarScreen.tsx,src/navigation/RootNavigator.tsx</files>
  <read_first>src/components/SpringPressable.tsx,src/utils/haptics.ts</read_first>
  <action>
    1. 将 **Budget、Asset、Mine、BillDetail、Calendar** 及 **RootNavigator**（若含 Pressable）中用户可点击主控件从 `Pressable` 迁到 `SpringPressable`（14-01 已覆盖 Home、记一笔、`BillCalculator`）。  
    2. 无法迁移的（第三方、TextInput）在 `14-VERIFICATION.md` **Accepted deviation** 列一行。  
    3. 补全 HAP-01：分段/chip（若未接）、记一笔保存、Alert 删除、Modal 开、预算保存 — 与 `14-UI-SPEC` 表一致。  
    4. 确认所有新动效分支读取 `useReduceMotion`。
  </action>
  <acceptance_criteria>
    - `rg "<Pressable" src/screens --glob "*.tsx" | wc -l` 输出 **较执行前减少**（记录于 commit message 或 14-VERIFICATION）；且 **或** `rg "SpringPressable" src/screens` 匹配数 ≥ 15
    - `rg "haptics\\." src/screens/BudgetScreen.tsx src/screens/AssetScreen.tsx` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>14-VERIFICATION 补全与 REQ 追溯</name>
  <files>.planning/phases/14-ios26-motion-polish/14-VERIFICATION.md</files>
  <read_first>.planning/REQUIREMENTS.md,.planning/phases/14-ios26-motion-polish/14-UI-SPEC.md</read_first>
  <action>
    更新 `14-VERIFICATION.md`：为 ANIM-04~06、INT-01 残余、HAP-01、MOT-01 勾选矩阵；记录 **header collapse 无跳变** 手测步骤；**Accepted deviation** 填实际例外。
  </action>
  <acceptance_criteria>
    - `rg "ANIM-04|ANIM-05|ANIM-06|INT-01|HAP-01|MOT-01" .planning/phases/14-ios26-motion-polish/14-VERIFICATION.md` 至少 4 匹配
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit
- REQUIREMENTS：ANIM-04, ANIM-05, ANIM-06, INT-01, HAP-01, MOT-01 ✓
- 14-REVIEWS：contentInset、stagger cap、SpringPressable 全站 ✓
