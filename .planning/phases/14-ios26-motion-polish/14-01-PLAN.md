---
phase: 14-ios26-motion-polish
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: false
files_modified:
  - package.json
  - src/theme/motion.ts
  - src/hooks/useReduceMotion.ts
  - src/utils/haptics.ts
  - src/components/SpringPressable.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/CreateBillScreen.tsx
  - src/components/BillCalculator.tsx
  - .planning/phases/14-ios26-motion-polish/14-VERIFICATION.md
autonomous: true
requirements:
  - ANIM-01
  - INT-01
  - HAP-01
  - MOT-01
must_haves:
  truths:
    - "`src/theme/motion.ts` 导出 `SPRING`、`REDUCE_SPRING`、`FADE_MS`，与 `IOS26-MOTION-INTERACTION-SPEC.md` 数值一致"
    - "`SpringPressable` 使用 `React.forwardRef`，`PressableProps` 透传；按压 scale 0.97 / opacity 0.92，`withSpring(SPRING.UI)`"
    - "`useReduceMotion` 订阅 `AccessibilityInfo.isReduceMotionEnabled` 并暴露布尔"
    - "`haptics.ts` 每个对外函数 try/catch；`expo-haptics` 已加入依赖"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/theme/motion.ts
    - src/components/SpringPressable.tsx
    - src/utils/haptics.ts
    - src/hooks/useReduceMotion.ts
---

<objective>
Wave 1：**动效基础设施** — `motion.ts`、`useReduceMotion`、`haptics`（含容错）、`SpringPressable`；按 **14-REVIEWS.md** 先做 **Home + 记一笔（`CreateBillScreen` / `BillCalculator`）** 按压迁移试点；新建 **`14-VERIFICATION.md`** 骨架。
</objective>

<threat_model>
## Trust boundaries
- 本地依赖新增 `expo-haptics`；无网络数据面。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P14-01 | Tampering | N/A — 客户端动效 |
| P14-02 | Denial of Service | 低 — haptics try/catch 避免崩溃 |
</threat_model>

<context>
@.planning/research/IOS26-MOTION-INTERACTION-SPEC.md
@.planning/phases/14-ios26-motion-polish/14-UI-SPEC.md
@.planning/phases/14-ios26-motion-polish/14-REVIEWS.md
@UI-SPEC.md
</context>

<tasks>

<task type="execute">
  <name>依赖与 motion 令牌（ANIM-01）</name>
  <files>package.json,src/theme/motion.ts</files>
  <read_first>package.json,.planning/research/IOS26-MOTION-INTERACTION-SPEC.md,src/theme/palette.ts</read_first>
  <action>
    1. 运行 `npx expo install expo-haptics`（或等价）写入 `package.json`。  
    2. 新建 `src/theme/motion.ts`，导出：  
       `SPRING.UI = { damping: 15, stiffness: 220, mass: 1 }`  
       `SPRING.THUMB = { damping: 13, stiffness: 260, mass: 0.9 }`  
       `SPRING.SHEET = { damping: 20, stiffness: 200, mass: 1.1 }`  
       `SPRING.GESTURE = { damping: 12, stiffness: 300, mass: 1 }`  
       `REDUCE_SPRING = { damping: 50, stiffness: 400, mass: 1 }`  
       `FADE_MS = { fast: 120, normal: 200, slow: 350 } as const`  
    3. 自 `src/theme/index.ts`（若存在）或常用 barrel 再导出 `motion`，便于 `import { SPRING } from '@/theme/motion'` 或相对路径一致。
  </action>
  <acceptance_criteria>
    - `rg "damping: 15" src/theme/motion.ts` 有匹配
    - `rg "expo-haptics" package.json` 有匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>useReduceMotion（MOT-01 基础）</name>
  <files>src/hooks/useReduceMotion.ts</files>
  <read_first>src/hooks/useReduceMotion.ts</read_first>
  <action>
    新建 `src/hooks/useReduceMotion.ts`：  
    - `import { AccessibilityInfo } from 'react-native'`  
    - `useState` + `useEffect` 调用 `AccessibilityInfo.isReduceMotionEnabled()` 初始化并订阅 `change` 事件  
    - 返回 `boolean` `reduceMotion`  
    文档注释：供 `SpringPressable`、stagger、sheet 降级使用。
  </action>
  <acceptance_criteria>
    - `rg "isReduceMotionEnabled|AccessibilityInfo" src/hooks/useReduceMotion.ts` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>haptics 封装（HAP-01）</name>
  <files>src/utils/haptics.ts</files>
  <read_first>src/utils/haptics.ts</read_first>
  <action>
    新建 `src/utils/haptics.ts`，使用 `expo-haptics`：  
    - 导出对象或命名函数：`select`（`ImpactFeedbackStyle.Light` 或 `selectionAsync`）、`success`（`notificationAsync(Success)`）、`error`（`notificationAsync(Error)`）、`light`、`medium`  
    - **每个**对外函数体 `try { ... } catch { /* noop */ }`  
    - 不抛到业务层。
  </action>
  <acceptance_criteria>
    - `rg "try \\{" src/utils/haptics.ts` 至少 1 匹配
    - `rg "expo-haptics" src/utils/haptics.ts` 有匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>SpringPressable + 试点屏（INT-01）</name>
  <files>src/components/SpringPressable.tsx,src/screens/HomeScreen.tsx,src/screens/CreateBillScreen.tsx,src/components/BillCalculator.tsx</files>
  <read_first>src/screens/HomeScreen.tsx,src/screens/CreateBillScreen.tsx,src/components/BillCalculator.tsx,src/theme/motion.ts,.planning/phases/14-ios26-motion-polish/14-UI-SPEC.md</read_first>
  <action>
    1. 新建 `src/components/SpringPressable.tsx`：  
       - `React.forwardRef<React.ElementRef<typeof Pressable>, PressableProps>(...)`  
       - 内部 `Animated.createAnimatedComponent(Pressable)` 或 Reanimated `AnimatedPressable`（与项目现有 Reanimated 用法一致）  
       - `onPressIn` / `onPressOut`：`withSpring(0.97|1, SPRING.UI)` 与 opacity `0.92|1`；合并用户传入的 `onPressIn`/`onPressOut`（先调用用户再调用弹簧）  
       - `useReduceMotion()` 为 true 时：**不** scale 或 `withTiming` 极短 opacity  
    2. **HomeScreen、CreateBillScreen、BillCalculator**：将主要可点击行/键位从 `Pressable` 换为 `SpringPressable`（保留 `hitSlop`、`accessibilityLabel` 等）。  
    3. 不在本计划全量替换其他屏（留给 14-03）。
  </action>
  <acceptance_criteria>
    - `rg "SpringPressable" src/screens/HomeScreen.tsx` 至少 1 匹配
    - `rg "SpringPressable" src/screens/CreateBillScreen.tsx src/components/BillCalculator.tsx` 至少 1 匹配
    - `rg "forwardRef" src/components/SpringPressable.tsx` 有匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>14-VERIFICATION 骨架</name>
  <files>.planning/phases/14-ios26-motion-polish/14-VERIFICATION.md</files>
  <read_first>.planning/phases/14-ios26-motion-polish/14-UI-SPEC.md</read_first>
  <action>
    新建 `14-VERIFICATION.md`：含 **Reduce Motion**、**分段 Spring Thumb**、**图表 stagger**、**列表 stagger**、**Modal spring**、**Header collapse**、**haptics** 的 spot-check 表；**Accepted deviation** 空表；引用 `14-REVIEWS.md` Planner Directives。
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/14-ios26-motion-polish/14-VERIFICATION.md`
    - `rg "Reduce Motion|SpringPressable|stagger|modalScrim" .planning/phases/14-ios26-motion-polish/14-VERIFICATION.md` 至少 3 匹配
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit
- REQUIREMENTS：ANIM-01, INT-01, HAP-01, MOT-01（基础设施）✓
- 14-REVIEWS：SpringPressable、try/catch haptics、试点 rollout ✓
