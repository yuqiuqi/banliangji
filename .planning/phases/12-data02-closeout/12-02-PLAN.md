---
phase: 12-data02-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01-PLAN.md
gap_closure: true
files_modified:
  - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
  - .planning/phases/10-persist-uat/10-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/01-data-layer-baseline/01-VERIFICATION.md
autonomous: false
requirements:
  - DATA-02
must_haves:
  truths:
    - "**（完成语义）** `12-02` **计划跑完** = 设备已按协议执行 **或** `Result: BLOCKED` 已满足 `12-CONTEXT` 短期规则（dated 取证 + 文档镜像）；**不等于** ROADMAP 上 Phase 12 **证据已闭合**"
    - "**（里程碑）** Phase 12 在 DATA-02 上 **证据闭合** = `DATA-02-SMOKE.md` **唯一** `Result:` 行为 **PASS** 或 **FAIL**（非无限期 BLOCKED），且 `REQUIREMENTS` / `STATE` / `10-VERIFICATION` Sign-off 已与该 Result **一致**"
    - "若 PASS：`REQUIREMENTS.md` 中 **DATA-02** 为 `- [x]`；Traceability 中 DATA-02 **Status** 为 **Done**；**Phase** 列统一为 **Phase 12**（验证收口）"
    - "若 FAIL：**不**勾选 DATA-02；`Investigation notes` 含评审要求字段（见任务 1）；`STATE.md` Deferred 或 Current 指向取证"
    - "若 BLOCKED：仅允许短期；取证计划须含**目标日**；超过目标日须在 SMOKE 或 `STATE.md` 写明**升级路径**（责任人、下一动作）"
    - "`npm run verify` 退出码 0"
  artifacts:
    - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
---

<objective>
在 **12-01** 已创建 `10-VERIFICATION.md` 的前提下，由**维护者在设备上**执行 `DATA-02-SMOKE.md` Steps，更新 **Result 与 Environment**，并据结果**诚实**同步 `REQUIREMENTS.md`、`STATE.md`。

**完成语义：** 见 frontmatter `must_haves` 与 `12-CONTEXT.md` — **勿**将「本 PLAN 任务勾选完毕」与「DATA-02 REQ Done」混为一谈。
</objective>

<threat_model>
## Trust boundaries
- 手测在本地设备；文档更新在仓库。

## STRIDE
| threat_id | category | disposition |
|-----------|----------|-------------|
| P12-02 | Tampering | 禁止伪造 PASS；Result 须与设备事实一致 |
</threat_model>

<context>
@.planning/phases/10-persist-uat/DATA-02-SMOKE.md
@.planning/phases/10-persist-uat/10-VERIFICATION.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-data02-closeout/12-CONTEXT.md
</context>

<tasks>

<task type="human-verify">
  <name>设备执行 DATA-02 并更新 SMOKE 文件</name>
  <files>.planning/phases/10-persist-uat/DATA-02-SMOKE.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md</read_first>
  <action>
    1) 在 **iOS Simulator 或真机** 按 `DATA-02-SMOKE.md` **Steps** 与 **Kill protocol** 完整执行一遍。

    2) 更新 **Environment** 表：补全 **iOS 版本**、**机型/Simulator runtime**、**Date**、**Git**（`git rev-parse --short HEAD`）、所用 **Build**（Expo Go / Dev client）。**PASS 仅覆盖本表所列平台**；若未测 Android，须在 Environment 或 Note 中声明 **范围：iOS only**（或等价表述）。

    3) 将 **Result:** 行改为 `Result: PASS` 或 `Result: FAIL`；其下 **1–3 行**写证据摘要（例如：识别串仍可见 / 或复现步骤与现象）。

    4) 若 **FAIL**：在 **Investigation notes** 记录（**以现象为主**，避免长篇根因分析）：**可观察现象**、**可复现性**（稳定 / 偶发 / 仅某环境）、**疑似层**（SQLite / 查询 / UI）、**是否**需要另开 bugfix 阶段（Y/N）。**不**在本任务擅自改 `src/` 业务逻辑。

    5) 若 **BLOCKED**（短期）：`Result:` 可暂留 `BLOCKED`，但 **下一步取证计划** 须含 **目标日** 与执行人占位；**超过目标日仍无法验证**时，须更新 SMOKE 或 `STATE.md`：**升级路径**（谁、下一步、是否改记为 FAIL / 开新 Phase）。

    6) 同步更新 `10-VERIFICATION.md` 的 **Sign-off**：摘要 **逐字镜像** `DATA-02-SMOKE.md` 的 `Result:` 与日期；`BLOCKED` 时注明「临时态 / 见 SMOKE 取证计划」。
  </action>
  <acceptance_criteria>
    - `rg "^Result: (PASS|FAIL|BLOCKED)" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 恰好 1 行
    - 若为 **BLOCKED**：同文件 **下一步取证计划** 须含 **目标日**（YYYY-MM-DD 或明确里程碑名）
    - 若为 **PASS** 或 **FAIL**：Environment 表 **iOS target** 不得为未执行占位；且证据摘要非空
    - `rg -n "iOS|Simulator|范围|Android" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 至少 1 匹配（平台范围可辨）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>据 Result 更新 REQUIREMENTS 与 STATE</name>
  <files>.planning/REQUIREMENTS.md,.planning/STATE.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,.planning/REQUIREMENTS.md,.planning/STATE.md</read_first>
  <action>
    1) **若 `Result: PASS`**：将 `REQUIREMENTS.md` 中 **DATA-02** 从 `- [ ]` 改为 `- [x]`；Traceability 表中 DATA-02 **Status** 改为 `Done`；**Phase** 列填 **Phase 12**（与表中其它行风格一致，禁止含糊的「10 + 12」除非全表统一）。

    2) **若 `Result: FAIL` 或 `BLOCKED`**：**不要**勾选 DATA-02；Traceability 中 Status 为 `Fail` 或 `Blocked`；更新 `STATE.md` **Deferred** / **Current**，指向 `DATA-02-SMOKE.md`；**BLOCKED 且已过取证目标日**时须含 **升级路径** 一句。

    3) 从 `STATE.md` **Deferred** 中移除或更新已解决的 DATA-02 条目（若 PASS）。
  </action>
  <acceptance_criteria>
    - 若 `rg "^Result: PASS" .planning/phases/10-persist-uat/DATA-02-SMOKE.md`：`rg "^- \\[x\\] \\*\\*DATA-02" .planning/REQUIREMENTS.md` 有 1 匹配
    - 若 `rg "^Result: FAIL" .planning/phases/10-persist-uat/DATA-02-SMOKE.md`：`rg "^- \\[x\\] \\*\\*DATA-02" .planning/REQUIREMENTS.md` **无**匹配
    - `rg "DATA-02" .planning/STATE.md` 至少 1 匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>（可选 · 收尾卫生）01-VERIFICATION 手测行</name>
  <files>.planning/phases/01-data-layer-baseline/01-VERIFICATION.md</files>
  <read_first>.planning/phases/01-data-layer-baseline/01-VERIFICATION.md,.planning/phases/10-persist-uat/DATA-02-SMOKE.md</read_first>
  <action>
    **非阻塞项**：仅在 DATA-02 已 **PASS** 后执行；将 `01-VERIFICATION.md` 中与「按 PROJECT 四步冒烟在设备上执行」对应的 `- [ ]` 改为 `- [x]`，并引用 `DATA-02-SMOKE.md`。**PASS 前**不得因本任务延迟 `12-02` 其它任务；未 PASS 时仅可追加备注指向阻塞，**不**改勾选。
  </action>
  <acceptance_criteria>
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| REQUIREMENTS | DATA-02 勾选诚实性 | 02:任务 2 |
| MILESTONE-AUDIT | E2E 杀进程流 | 02:任务 1 |
