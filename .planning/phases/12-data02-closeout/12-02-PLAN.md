---
phase: 12-data02-closeout
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01-PLAN.md
gap_closure: true
files_modified:
  - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
  - .planning/phases/10-persist-uat/10-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/01-data-layer-baseline/01-VERIFICATION.md
autonomous: false
requirements:
  - DATA-02
must_haves:
  truths:
    - "**Phase 12 / DATA-02 里程碑闭合** 当且仅当 `DATA-02-SMOKE.md` 的 `Result:` 为 **PASS** 或 **FAIL** 且 `REQUIREMENTS`/`STATE` 已诚实同步；**BLOCKED** 只表示 **证据仍待定**（允许带 dated 取证计划），**不得**解释为 DATA-02 已验收"
    - "**Plan 02 执行完成** 允许在 `Result:` 仍为 **BLOCKED**（须 dated 取证计划）；与 **里程碑闭合** 是两件事（见 `12-CONTEXT.md` Completion Semantics）"
    - "`DATA-02-SMOKE.md` 在里程碑意义上**目标终态**为 **PASS** 或 **FAIL**；若暂时 **BLOCKED** 须附 **dated** 下一步；**超期**须更新计划或改判 FAIL（资源/环境不可用）见任务 1"
    - "若 PASS：`REQUIREMENTS.md` 中 **DATA-02** 为 `- [x]`；Traceability 中 DATA-02 **Status** 为 **Done**；**Phase** 列为 **`Phase 12`**"
    - "若 FAIL：**不**勾选 DATA-02；`Investigation notes` 非空（含现象、可复现性、层级、是否需 bugfix）；`STATE.md` 指向取证"
    - "`npm run verify` 退出码 0"
  artifacts:
    - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
reviews_incorporated: "12-REVIEWS.md (2026-04-23)"
---

<objective>
在 **12-01** 已创建 `10-VERIFICATION.md` 的前提下，由**维护者在设备上**执行 `DATA-02-SMOKE.md` Steps，更新 **Result 与 Environment**，并据结果**诚实**同步 `REQUIREMENTS.md`、`STATE.md`。**里程碑闭合**仅当 `Result:` 为 **PASS** 或 **FAIL**；**BLOCKED** 仅对应 Plan 02 部分执行 / 证据待定。`01-VERIFICATION.md` 勾选为 **收口后卫生项**，非本 Phase 主目标。
</objective>

<threat_model>
## Trust boundaries
- 手测在本地设备；文档更新在仓库。

## STRIDE
| threat_id | category | disposition |
|-----------|----------|-------------|
| P12-02 | Tampering | 禁止伪造 PASS；Result 须与设备事实一致 |
</threat_model>

<context>
@.planning/phases/10-persist-uat/DATA-02-SMOKE.md
@.planning/phases/10-persist-uat/10-VERIFICATION.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-data02-closeout/12-CONTEXT.md
</context>

<tasks>

<task type="human-verify">
  <name>设备执行 DATA-02 并更新 SMOKE 文件</name>
  <files>.planning/phases/10-persist-uat/DATA-02-SMOKE.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md</read_first>
  <action>
    1) 在 **iOS Simulator 或真机** 按 `DATA-02-SMOKE.md` **Steps** 与 **Kill protocol** 完整执行一遍。

    2) 更新 **Environment** 表：补全 **iOS 版本**、**机型/Simulator runtime**、**Date**、**Git**（`git rev-parse --short HEAD`）、所用 **Build**（Expo Go / Dev client）；**须含一行「验证范围」**（例如 `iOS Simulator only` / `真机 iPhone XX`），以免 **PASS 被误读为全平台**。

    3) 将 **Result:** 行改为 `Result: PASS` 或 `Result: FAIL`；其下 **1–3 行**写证据摘要（例如：识别串仍可见 / 或复现步骤与现象）。

    4) 若 **FAIL**：在 **Investigation notes** 用**短表或编号列表**记录（避免长篇根因）：**① 观察到的现象**；**② 可复现性**（必现 / 偶现 / 仅某环境）；**③ 疑似层级**（SQLite | 查询 | UI | 环境）；**④ 是否需另开 bugfix / 复现阶段**。**不**在本任务擅自改 `src/` 业务逻辑。

    5) 若 **BLOCKED**（仍无法跑设备）：更新 **下一步取证计划**，含 **维护者/责任人**（GitHub id 或角色名）与 **目标日期**；若已超过上一目标日，**必须**修订日期或将 `Result:` 改为 **FAIL** 并说明「环境/资源不可用」等诚实原因（**不得**静默超期）。

    6) 同步更新 `10-VERIFICATION.md` 的 **Sign-off**（或等价小节）中 DATA-02 Result 摘要，与 SMOKE 一致。
  </action>
  <acceptance_criteria>
    - `rg "^Result: (PASS|FAIL|BLOCKED)" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 恰好 1 行；若为 BLOCKED，同文件 **下一步取证计划** 须含日期或目标里程碑引用
    - 若为 BLOCKED：`rg -n "取证计划|下一步" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 至少 1 匹配 **且** 文中须出现 **2026-** 或 **目标** 之一（dated 跟进）
    - Environment 表中 **iOS target** 行不得仍为「未在本环境执行」占位（除非 BLOCKED 且已说明原因）
    - `rg -n "验证范围|Simulator|真机|iOS" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 在 **Environment** 表所在段（文件前 30 行内）至少 1 匹配，或 **Result 为 PASS/FAIL** 且 Environment 表已填机型/runtime（人工核对）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>据 Result 更新 REQUIREMENTS 与 STATE</name>
  <files>.planning/REQUIREMENTS.md,.planning/STATE.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,.planning/REQUIREMENTS.md,.planning/STATE.md</read_first>
  <action>
    1) **若 `Result: PASS`**：将 `REQUIREMENTS.md` 中 **DATA-02** 从 `- [ ]` 改为 `- [x]`；Traceability 表中 DATA-02 **Status** 改为 `Done`；**Phase** 列填 **`Phase 12`**（与 `12-CONTEXT.md` 锁定一致）。

    2) **若 `Result: FAIL` 或 `BLOCKED`**：**不要**勾选 DATA-02；Traceability 中 Status 为 `Blocked` 或 `Fail`；更新 `STATE.md` **Deferred Items** 或 **Current Status**，指向 `DATA-02-SMOKE.md` 与 Investigation / 取证计划。

    3) 从 `STATE.md` **Deferred** 中移除或更新已解决的 DATA-02 条目（若 PASS）。
  </action>
  <acceptance_criteria>
    - 若 `rg "^Result: PASS" .planning/phases/10-persist-uat/DATA-02-SMOKE.md`：`rg "^- \\[x\\] \\*\\*DATA-02" .planning/REQUIREMENTS.md` 有 1 匹配
    - 若 `rg "^Result: FAIL" .planning/phases/10-persist-uat/DATA-02-SMOKE.md`：`rg "^- \\[x\\] \\*\\*DATA-02" .planning/REQUIREMENTS.md` **无**匹配
    - `rg "DATA-02" .planning/STATE.md` 至少 1 匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>（可选 · 收口后卫生）01-VERIFICATION 手测行</name>
  <files>.planning/phases/01-data-layer-baseline/01-VERIFICATION.md</files>
  <read_first>.planning/phases/01-data-layer-baseline/01-VERIFICATION.md,.planning/phases/12-data02-closeout/12-CONTEXT.md</read_first>
  <action>
    **非** DATA-02 里程碑闭合条件。仅当 **DATA-02 已 PASS** 且主任务已完成时：将 `01-VERIFICATION.md` 中与「按 PROJECT 四步冒烟在设备上执行」对应的 `- [ ]` 改为 `- [x]`，并加一句引用 `DATA-02-SMOKE.md`。**若未 PASS**：不改勾选，可备注阻塞原因。
  </action>
  <acceptance_criteria>
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| REQUIREMENTS | DATA-02 勾选诚实性 | 02:任务 2 |
| MILESTONE-AUDIT | E2E 杀进程流 | 02:任务 1 |
