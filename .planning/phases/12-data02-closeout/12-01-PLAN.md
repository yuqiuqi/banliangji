---
phase: 12-data02-closeout
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/phases/10-persist-uat/10-VERIFICATION.md
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "本 PLAN **不**闭合 **DATA-02**；仅补齐缺失的 **`10-VERIFICATION.md`**（结构/引用）；**DATA-02 验收**仍以 `DATA-02-SMOKE.md` 的 `Result:` 为唯一终态依据"
    - "新建 `10-VERIFICATION.md`，可被 `rg '10-VERIFICATION|DATA-02|QA-04'` 检索"
    - "明确 **DATA-02** 证据源为 `DATA-02-SMOKE.md`；**QA-04** 引用 `10-QA-04-ALIGNMENT.md`"
    - "`10-VERIFICATION.md` 含可检索句：**在 `Result:` 为 PASS 或 FAIL 之前 Phase 10 验证视为未完成（证据待定）**"
    - "`10-VERIFICATION.md` 明示：**`npm run verify` 不能替代 DATA-02 杀进程手测**（原文或等价表述）"
    - "Sign-off 中 **BLOCKED** 仅可描述为自 `DATA-02-SMOKE.md` **临时镜像**的待定态，**非**里程碑闭合"
    - "`npm run verify` 退出码 0（本 PLAN 仅改文档时亦须保持）"
  artifacts:
    - .planning/phases/10-persist-uat/10-VERIFICATION.md
reviews_incorporated: "12-REVIEWS.md (2026-04-23)"
---

<objective>
补齐 **Phase 10** 级 **`10-VERIFICATION.md`**，使审计中「无 10-VERIFICATION」缺口在文档结构上闭合；内容以 **引用 + 验收清单** 为主，不重复 `DATA-02-SMOKE` 全文。**不**闭合 **DATA-02**（不替代 `DATA-02-SMOKE.md` 的终端 `PASS`/`FAIL`）。
</objective>

<threat_model>
## Trust boundaries
- 仅规划文档；无新运行时、无密钥。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P12-01 | Info disclosure | 验证文档案不写真实用户 PII；沿用测试识别串 |
</threat_model>

<context>
@.planning/phases/12-data02-closeout/12-CONTEXT.md
@.planning/v2.3-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="execute">
  <name>撰写 10-persist-uat/10-VERIFICATION.md</name>
  <files>.planning/phases/10-persist-uat/10-VERIFICATION.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,.planning/phases/10-persist-uat/10-QA-04-ALIGNMENT.md,.planning/phases/12-data02-closeout/12-CONTEXT.md</read_first>
  <action>
    新建 `.planning/phases/10-persist-uat/10-VERIFICATION.md`，建议结构：

    1. **Phase 10 范围** — 一句：持久化 UAT 闭环（DATA-02 + QA-04）。

    2. **DATA-02** — 指向 `DATA-02-SMOKE.md` 为**唯一**杀进程手测协议；列出与 `REQUIREMENTS.md` **DATA-02** 条目的对齐关系；**Result 以该文件 `Result:` 行为准**。须含**单独一行（或小节标题下首段）**原文或等价含义：**「在 `DATA-02-SMOKE.md` 的 `Result:` 为 `PASS` 或 `FAIL` 之前，Phase 10 验证视为未完成（证据待定）。」**

    3. **溯源** — 一句注明：本文件于 **Phase 12** 缺口收口期间补建，便于审计时间线（可置于 Phase 10 范围段落后）。

    4. **QA-04** — 一句「已闭环」+ 链接 `10-QA-04-ALIGNMENT.md`；注明 08/09 UAT `pending: 0` 事实源文件路径。

    5. **自动化门禁** — 明示 **`npm run verify` 不能替代 DATA-02**（可引用 `DATA-02-SMOKE.md` Note on verify）。

    6. **Sign-off** — 表或列表：执行人 / 日期 / DATA-02 Result 摘要栏；**仅允许** `PASS` | `FAIL` | `BLOCKED`，且若出现 `BLOCKED` 须注明「与 `DATA-02-SMOKE.md` 临时一致，**非** DATA-02 已验收」或等价表述。

    不得删除或改写 `DATA-02-SMOKE.md` 内 Kill protocol（除非修复明显笔误且不改语义）。
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/10-persist-uat/10-VERIFICATION.md`
    - `rg -n "DATA-02-SMOKE|10-QA-04-ALIGNMENT" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 2 匹配
    - `rg "DATA-02" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 2 匹配
    - `rg -n "证据待定|未完成" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 1 匹配
    - `rg -n "npm run verify|verify" .planning/phases/10-persist-uat/10-VERIFICATION.md` 与 `rg -n "不能替代|不替代|不覆盖|不证明" .planning/phases/10-persist-uat/10-VERIFICATION.md` **均**至少 1 匹配（可不在同一行）
    - `rg -i "phase 12|Phase 12" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| ROADMAP Phase 12 | 10-VERIFICATION 存在 | 01:全任务 |
| MILESTONE-AUDIT | DATA-02 验证文档缺口 | 01:全任务 |
