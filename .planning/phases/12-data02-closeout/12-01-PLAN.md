---
phase: 12-data02-closeout
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/phases/10-persist-uat/10-VERIFICATION.md
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "新建 `10-VERIFICATION.md`，可被 `rg '10-VERIFICATION|DATA-02|QA-04'` 检索"
    - "明确 **DATA-02** 证据源为 `DATA-02-SMOKE.md`；**QA-04** 引用 `10-QA-04-ALIGNMENT.md`"
    - "`10-VERIFICATION.md` 含显式说明：在 `Result:` 为 **PASS** 或 **FAIL** 之前 DATA-02 **未完成**（`BLOCKED` 仅临时态）；且声明 **`npm run verify` 不替代**该手测"
    - "注明本文件为 Phase **12** 回补（时间线可追溯）"
    - "`npm run verify` 退出码 0（本 PLAN 仅改文档时亦须保持）"
  artifacts:
    - .planning/phases/10-persist-uat/10-VERIFICATION.md
---

<objective>
补齐 **Phase 10** 级 **`10-VERIFICATION.md`**，使审计中「无 10-VERIFICATION」缺口在文档结构上闭合；内容以 **引用 + 验收清单** 为主，不重复 `DATA-02-SMOKE` 全文。

**范围边界（Cross-AI review）：本 PLAN 不闭合 DATA-02**，仅恢复可检索的验证索引与 Sign-off 结构；**设备证据**仍由 `12-02-PLAN` + `DATA-02-SMOKE.md` 承担。
</objective>

<threat_model>
## Trust boundaries
- 仅规划文档；无新运行时、无密钥。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P12-01 | Info disclosure | 验证文档案不写真实用户 PII；沿用测试识别串 |
</threat_model>

<context>
@.planning/phases/12-data02-closeout/12-CONTEXT.md
@.planning/v2.3-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="execute">
  <name>撰写 10-persist-uat/10-VERIFICATION.md</name>
  <files>.planning/phases/10-persist-uat/10-VERIFICATION.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,.planning/phases/10-persist-uat/10-QA-04-ALIGNMENT.md,.planning/phases/12-data02-closeout/12-CONTEXT.md</read_first>
  <action>
    新建 `.planning/phases/10-persist-uat/10-VERIFICATION.md`，建议结构：

    1. **Phase 10 范围** — 一句：持久化 UAT 闭环（DATA-02 + QA-04）。

    2. **DATA-02 验证完备性** — 单独小节（标题自拟，须含「未完成」或「PASS 或 FAIL」语义）：写明在 `DATA-02-SMOKE.md` 的 `Result:` 为 **`PASS` 或 `FAIL`** 之前，**REQ 意义上 DATA-02 未完成**；`**BLOCKED**` 仅允许为**短期**过渡，且须与 SMOKE 内 dated 取证计划一致，**不是**可接受的里程碑终态。

    3. **DATA-02** — 指向 `DATA-02-SMOKE.md` 为**唯一**杀进程手测协议与 **canonical `Result:`**；列出与 `REQUIREMENTS.md` **DATA-02** 条目的对齐关系。

    4. **QA-04** — 一句「已闭环」+ 链接 `10-QA-04-ALIGNMENT.md`；注明 08/09 UAT `pending: 0` 事实源文件路径。

    5. **自动化门禁** — `npm run verify` 不覆盖 DATA-02；**明确一句**「verify **不能替代**杀进程设备手测」（可引用 `DATA-02-SMOKE.md` Note on verify）。

    6. **回补说明** — 脚注或短段：本文件由 Phase **12** `12-01-PLAN` 补齐，满足审计对 Phase 10 验证产物**可引用结构**的要求。

    7. **Sign-off** — 表或列表：执行人 / 日期 / DATA-02 Result 摘要栏；**须注明** Sign-off 中 `BLOCKED` 若出现，仅为**镜像** SMOKE 的临时态，**非** Phase 12 在 ROADMAP 意义上的完成。

    不得删除或改写 `DATA-02-SMOKE.md` 内 Kill protocol（除非修复明显笔误且不改语义）。
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/10-persist-uat/10-VERIFICATION.md`
    - `rg -n "DATA-02-SMOKE|10-QA-04-ALIGNMENT" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 2 匹配
    - `rg "DATA-02" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 2 匹配
    - `rg -n "未完成|PASS.*FAIL|BLOCKED" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 1 匹配（验证完备性小节）
    - `rg -n "不替代|不覆盖|verify" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 1 匹配（自动化 ≠ DATA-02 证据）
    - `rg -n "12-01-PLAN|回补" .planning/phases/10-persist-uat/10-VERIFICATION.md` 至少 1 匹配（Phase 12 时间线）
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| ROADMAP Phase 12 | 10-VERIFICATION 存在 | 01:全任务 |
| MILESTONE-AUDIT | DATA-02 验证文档缺口 | 01:全任务 |
