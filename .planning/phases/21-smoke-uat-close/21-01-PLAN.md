---
phase: 21-smoke-uat-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
  - .planning/phases/11-chrome-depth/11-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/21-smoke-uat-close/21-VERIFICATION.md
autonomous: false
requirements:
  - UAT-01
  - UAT-02
  - UAT-03
  - UAT-04
must_haves:
  truths:
    - "`DATA-02-SMOKE.md` 中 `Result:` 为 **PASS** 或 **FAIL** 或 **BLOCKED**；若为 BLOCKED，须含日期、设备/环境与阻塞说明（与 `21-CONTEXT.md` D-21-01 一致）"
    - "`11-VERIFICATION.md` 中 THEME/A11Y/LG-01 对应项已勾选 **或** 在 **Accepted deviations** 登记延期与原因；**Sign-off** 含执行人与日期"
    - "`REQUIREMENTS.md` 追溯表中 UAT-01～UAT-04 对应 Phase 21 行更新为 **Done** 或 **Blocked**+理由，与上文文件一致"
    - "凡修改仓库内除仅手测说明外的文件后：`npm run verify` 退出码 0"
  artifacts:
    - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
    - .planning/phases/11-chrome-depth/11-VERIFICATION.md
    - .planning/phases/21-smoke-uat-close/21-VERIFICATION.md
---

<objective>
闭合 **Phase 21**：在设备/Simulator 上完成 **DATA-02** Kill protocol 冒烟，固化 **`11-VERIFICATION.md`** 中 **THEME-01 / A11Y-01 / LG-01** 证据与 Sign-off，同步 **`REQUIREMENTS.md`** 追溯表与 **`STATE.md`** 结转句；新增 **`21-VERIFICATION.md`** 作为本相收口索引。无阻断级数据缺陷时**不改**应用源码；若 DATA-02 为 **FAIL**，按 `21-CONTEXT.md` 允许的最小修复另开提交后再重跑冒烟。
</objective>

<threat_model>
## Trust boundaries
- 变更限于规划 Markdown 与（仅当冒烟 FAIL 时）本地数据层/持久化相关最小代码；无新网络面。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P21-01 | Tampering | N/A — 文档为主；代码修复仅限已授权的最小 bugfix |
| P21-02 | Information disclosure | 低 — 勿在 Smoke 文件中粘贴密钥、PII；识别串仅测试数据 |
</threat_model>

<context>
@.planning/phases/21-smoke-uat-close/21-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="execute">
  <name>UAT-01 — DATA-02 设备杀进程冒烟与文书回填</name>
  <files>.planning/phases/10-persist-uat/DATA-02-SMOKE.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,.planning/phases/21-smoke-uat-close/21-CONTEXT.md</read_first>
  <action>
    1. 在 **iOS Simulator 或真机**上按 `DATA-02-SMOKE.md` 的 **Kill protocol** 执行：记一笔 → 列表可见 → **App Switcher 划掉 App**（或文内允许的等效强制停止）→ 等待 ≥5s → 冷启动 → 用 **Test data** 区当前识别串（如 `DATA02-20260422`）检索；若重跑新数据，在 **Test data** 更新新串并在 Steps 中同步替换。
    2. 更新 **Environment** 表：**Date**、**Git**（`git rev-parse --short HEAD`）、**iOS target** 填具体机型/Simulator runtime 或真机型号+OS；**禁止**将「仅停 Metro」写为已完成 Kill。
    3. 将 **Result** 段设为以下之一（单行标题保留 `Result:` 前缀便于检索）：
       - `Result: PASS` — 检索到同一条；
       - `Result: FAIL` — 数据丢失或路径断裂；并填写 **Investigation notes**（含是否查 `main.db` / `bill_list` 等结论）；
       - `Result: BLOCKED` — 无设备/无签名等；**下一步取证计划** 第 4 条填 **执行人 / 完成日期**。
    4. **Note on verify**：不修改本文件以外的代码时，可不重复跑 `npm run verify`；若同时改了其他 tracked 文件，须跑 verify。
  </action>
  <acceptance_criteria>
    - `rg "^Result: (PASS|FAIL|BLOCKED)" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 恰好 1 行匹配
    - 若 `Result: BLOCKED`：`rg "阻塞|BLOCKED|设备|Simulator" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 至少 1 匹配
    - 若 `Result: FAIL`：**Investigation notes** 小节非空且不含仅 `N/A — Result was not FAIL` 作为唯一内容（须为实际取证文字）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>UAT-02～04 — 11-VERIFICATION 勾选、偏差表与 Sign-off</name>
  <files>.planning/phases/11-chrome-depth/11-VERIFICATION.md</files>
  <read_first>.planning/phases/11-chrome-depth/11-VERIFICATION.md,.planning/phases/21-smoke-uat-close/21-CONTEXT.md,.planning/IOS26-DESIGN-GUIDE.md</read_first>
  <action>
    1. 在 **系统深色**下走查 **THEME-01** 条目（Tab / 明细 / 记一笔 / 账单流 / 我的），将对应 `[ ]` 改为 `[x]`；不可达项写入 **Accepted deviations**（含原因与指向 `UI-SPEC.md` 或 `DESIGN.md` 的条款）。
    2. 在 **设置 → 辅助功能 → 显示与文字大小 → 降低透明度** 开启状态下验证 **A11Y-01**：主路径完成记一笔与查看明细；勾选或记 deviation。
    3. 按 **LG-01** spot-check（列表快速滚动 ~3s、Tab 切换 ≥3 次、记一笔 Dock 按压）完成手感与层次检查；勾选或记 deviation。
    4. 将 **门禁** `npm run verify` 行勾选，并填 **最后一次执行日期**（执行日本机跑 `npm run verify` 的日期）。
    5. **Sign-off**：填写 **执行人 / 日期**；若某子项延期，在同一文件写明延期原因与目标相位（与 `21-CONTEXT.md` D-21-02c 一致）。
  </action>
  <acceptance_criteria>
    - `rg "## THEME-01|## A11Y-01|## LG-01|## Sign-off" .planning/phases/11-chrome-depth/11-VERIFICATION.md` 至少 4 段标题存在
    - `rg "\\[x\\]" .planning/phases/11-chrome-depth/11-VERIFICATION.md` 至少 3 行（表示有关键项已勾选）**或** `Accepted deviations` 表含至少 1 行非 None 说明
    - `rg "执行人|日期" .planning/phases/11-chrome-depth/11-VERIFICATION.md` 在 Sign-off 区有非占位填写（非仅 `________`）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Phase 21 — 21-VERIFICATION 收口索引</name>
  <files>.planning/phases/21-smoke-uat-close/21-VERIFICATION.md</files>
  <read_first>.planning/phases/21-smoke-uat-close/21-CONTEXT.md,.planning/phases/10-persist-uat/DATA-02-SMOKE.md,.planning/phases/11-chrome-depth/11-VERIFICATION.md</read_first>
  <action>
    新建或更新 `21-VERIFICATION.md`，包含：
    - 标题 **Phase 21 — Verification**
    - 小节 **UAT-01**：指向 `DATA-02-SMOKE.md`，抄录一行当前 `Result:` 状态与日期
    - 小节 **UAT-02～04**：指向 `11-VERIFICATION.md`，说明 THEME/A11Y/LG 已签或 deviation 摘要
    - 小节 **自动化**：除下述字面行外可附说明；**须**包含以下两种格式之一（二选一，整行字面）：`npm run verify: exit 0, YYYY-MM-DD, git <short-sha>` **或** `npm run verify: skipped (no tracked edits), YYYY-MM-DD`。其中日期与 SHA 为当次实跑值；凡对本仓库 **任意 tracked 文件** 有保存（含本文件），**禁止**使用 skipped 行而不跑 verify。在落盘本文件前若已修改任意 tracked 文件：于仓库根执行 `npm run verify`，且仅当命令 **exit 0** 后，将 **exit 0** 行写入上文字面格式。
    - **Out of scope**：一句声明 **GLASS-01/02** 归 Phase 22
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/21-smoke-uat-close/21-VERIFICATION.md`
    - `rg "DATA-02-SMOKE|11-VERIFICATION|Phase 22|GLASS" .planning/phases/21-smoke-uat-close/21-VERIFICATION.md` 至少 4 匹配
    - `rg "npm run verify: exit 0, 20[0-9]{2}-[0-1][0-9]-[0-3][0-9], git [0-9a-f]{7,40}" .planning/phases/21-smoke-uat-close/21-VERIFICATION.md` 至少 1 匹配 **或** `rg "npm run verify: skipped \\(no tracked edits\\), 20[0-9]{2}-[0-1][0-9]-[0-3][0-9]" .planning/phases/21-smoke-uat-close/21-VERIFICATION.md` 至少 1 匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>REQUIREMENTS 追溯表与 STATE 结转同步</name>
  <files>.planning/REQUIREMENTS.md,.planning/STATE.md</files>
  <read_first>.planning/REQUIREMENTS.md,.planning/STATE.md,.planning/phases/21-smoke-uat-close/21-VERIFICATION.md</read_first>
  <action>
    1. 在 `REQUIREMENTS.md` 顶部 **UAT-01～UAT-04** checklist：若对应验收已达，将 `- [ ]` 改为 `- [x]`；若整体阻塞，保持未勾选但在追溯表标 **Blocked**。
    2. 在 **Traceability** 表中将 Phase 21 对应 UAT-01～UAT-04 行的 **Status** 改为 **Done**（全部满足）或 **Blocked**（任一关键项未闭合且已书面说明）；不得留 **Pending** 而无理由。
    3. 在 `STATE.md` 中更新或关闭与 DATA-02 / 11-手测 相关的 **Blockers / Deferred** 一句交叉引用至 `21-VERIFICATION.md` 或上述源文件（仅句子级编辑，不扩大范围）。**若**原无此类句，则**新增**固定一行：`Phase 21 收口索引：.planning/phases/21-smoke-uat-close/21-VERIFICATION.md`
  </action>
  <acceptance_criteria>
    - `rg "\\| UAT-0[1-4] \\| Phase 21 \\| (Done|Blocked)" .planning/REQUIREMENTS.md` 至少 4 行匹配（四行 UAT 均为 Done 或均为 Blocked 或与混合一致）
    - `rg "## Traceability" .planning/REQUIREMENTS.md` 至少 1 匹配
    - `rg "Phase 21 收口索引：\\.planning/phases/21-smoke-uat-close/21-VERIFICATION\\.md" .planning/STATE.md` 至少 1 匹配
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| 21-CONTEXT D-21-01 | DATA-02 判据与 Kill | 01:任务 1 |
| 21-CONTEXT D-21-02 | 11-VERIFICATION 签字 | 01:任务 2 |
| 21-CONTEXT D-21-03 | verify 门与追溯表 | 01:任务 3–4 |
| 21-CONTEXT D-21-04 | Phase 22 边界 | 01:任务 3（Out of scope） |
| ROADMAP Phase 21 | Success criteria 四行 | 01:任务 1–4 |

## PLANNING COMPLETE
