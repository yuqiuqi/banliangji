---
phase: 18
plan: 18-01
wave: 1
depends_on: []
autonomous: true
requirements:
  - DESIGN-GLASS-01
files_modified:
  - package.json
  - package-lock.json
  - src/components/ios/GlassEffectContainer.tsx
  - src/components/ios/GlassEffectUnion.tsx
  - src/components/ios/VibrantText.tsx
objective: "Wave 1 — 玻璃体系 Tier 1：Container + Union + VibrantText + expo-linear-gradient"
---

# Plan 18-01: Tier 1 玻璃基础原语

## must_haves
- `expo-linear-gradient` 装好（Expo SDK 54 兼容）
- `GlassEffectContainer` 可包裹多玻璃元素，单外层 `BlurView` 共享采样
- `GlassEffectUnion` 可在 Container 内把子元素合并为单一玻璃形体
- `VibrantText` 提供玻璃内文字 vibrancy 近似
- `npm run verify` 绿

---

## Task 18-01-1 — 安装 expo-linear-gradient

<read_first>
- `package.json` 验证 Expo / RN 版本
</read_first>

<action>
```bash
npx expo install expo-linear-gradient
```
由 Expo CLI 自动解析 SDK 54 兼容版本（预期 `~15.0.x`）。严禁手改版本号。
</action>

<acceptance_criteria>
- `grep "\"expo-linear-gradient\"" package.json` 返回非空
- `npm ls expo-linear-gradient` 显示单一版本
- `grep "\"version\"" node_modules/expo-linear-gradient/package.json` 版本号 `15.x.x`
</acceptance_criteria>

---

## Task 18-01-2 — `src/components/ios/GlassEffectContainer.tsx`

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §3.10 G1 + §3.15 约束 1/4
- `src/components/ios/IOSChromeGlassBackground.tsx` 参考风格
- `src/theme/layout.ts` `radii`, `hairlineBorder`
- `src/theme/useAppTheme.ts`
</read_first>

<action>
创建：
```tsx
/**
 * iOS 26 Liquid Glass — 共享采样容器（v1.2 §3.10 G1）。
 *
 * 策略（与 SwiftUI `GlassEffectContainer` 的 RN 近似）：
 * - 容器层仅有一层 `BlurView`，提供共享采样；子元素**不得**再嵌 BlurView
 * - 子元素只做 tint / borderRadius / content
 * - 顶部 1px hairline 高光（§3.6 Specular 静态版）
 * - 必须绝对定位于动态内容之上（§3.15 约束 1）
 */
import React from "react";
import { StyleSheet, View, type ViewStyle } from "react-native";
import { BlurView } from "expo-blur";

import { useAppTheme } from "../../theme/useAppTheme";
import { radii } from "../../theme/layout";

type Tint = "default" | "dark" | "light";

export type GlassEffectContainerProps = {
  intensity?: number;
  tint?: Tint;
  borderRadius?: number;
  style?: ViewStyle;
  children?: React.ReactNode;
};

export function GlassEffectContainer({
  intensity = 60,
  tint,
  borderRadius = radii.card,
  style,
  children,
}: GlassEffectContainerProps) {
  const { scheme } = useAppTheme();
  const resolvedTint: Tint = tint ?? (scheme === "dark" ? "dark" : "default");

  return (
    <View style={[{ borderRadius, overflow: "hidden" }, style]}>
      <BlurView
        intensity={intensity}
        tint={resolvedTint}
        style={StyleSheet.absoluteFill}
      />
      {/* 顶部高光 hairline —— 静态 Specular */}
      <View style={styles.topHighlight} pointerEvents="none" />
      <View style={styles.content}>{children}</View>
    </View>
  );
}

const styles = StyleSheet.create({
  topHighlight: {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    height: StyleSheet.hairlineWidth,
    backgroundColor: "rgba(255,255,255,0.35)",
    zIndex: 1,
  },
  content: {
    zIndex: 2,
  },
});
```
</action>

<acceptance_criteria>
- `test -f src/components/ios/GlassEffectContainer.tsx`
- `grep -n "export function GlassEffectContainer" src/components/ios/GlassEffectContainer.tsx` 返回非空
- `grep -n "BlurView" src/components/ios/GlassEffectContainer.tsx` ≥ 1
- `grep -n "hairlineWidth" src/components/ios/GlassEffectContainer.tsx` ≥ 1
- `tsc --noEmit` 通过
</acceptance_criteria>

---

## Task 18-01-3 — `src/components/ios/GlassEffectUnion.tsx`

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §3.10bis G4
- `src/components/ios/GlassEffectContainer.tsx`（Task 18-01-2 产物）
- `src/theme/layout.ts` `radii`
</read_first>

<action>
创建：
```tsx
/**
 * iOS 26 Liquid Glass — 静态合并（v1.2 §3.10bis G4）。
 *
 * 用法：必须位于 `GlassEffectContainer` 之内。子元素之间 gap: 0，
 *       共享 borderRadius，形成视觉单一胶体。
 *
 * 禁区：
 * - 不可跨 Container 使用同一 union（Apple 明确要求同容器内）
 * - 不可合并动态变化的元素（用 Morphing，不用 Union）
 */
import React from "react";
import { StyleSheet, View, type ViewStyle } from "react-native";

import { radii } from "../../theme/layout";

export type GlassEffectUnionProps = {
  borderRadius?: number;
  style?: ViewStyle;
  children?: React.ReactNode;
};

export function GlassEffectUnion({
  borderRadius = radii.card,
  style,
  children,
}: GlassEffectUnionProps) {
  return (
    <View style={[styles.union, { borderRadius }, style]}>
      {children}
    </View>
  );
}

const styles = StyleSheet.create({
  union: {
    overflow: "hidden",
    // 子元素间无 gap，形成视觉单一胶体
    flexDirection: "column",
  },
});
```
</action>

<acceptance_criteria>
- `test -f src/components/ios/GlassEffectUnion.tsx`
- `grep -n "export function GlassEffectUnion" src/components/ios/GlassEffectUnion.tsx` 返回非空
- `grep -n "overflow: \"hidden\"" src/components/ios/GlassEffectUnion.tsx` 返回非空
- `tsc --noEmit` 通过
</acceptance_criteria>

---

## Task 18-01-4 — `src/components/ios/VibrantText.tsx`

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §3.8 E7 Vibrancy
- `src/theme/useAppTheme.ts` / `src/theme/typography.ts`
</read_first>

<action>
创建：
```tsx
/**
 * 玻璃内文字的 vibrancy 近似（v1.2 §3.8 E7 Tier 1+2）。
 *
 * 策略：
 * - 颜色使用主题 title（保证深浅模式下对比度）
 * - iOS 下追加轻微 textShadow（补偿玻璃模糊的可读性损失）
 * - 推荐 fontWeight ≥ '600'（默认 '600'）
 */
import React from "react";
import { Platform, StyleSheet, Text, type TextProps, type TextStyle } from "react-native";

import { useAppTheme } from "../../theme/useAppTheme";

export type VibrantTextProps = TextProps & {
  vibrantWeight?: TextStyle["fontWeight"];
};

export function VibrantText({
  vibrantWeight = "600",
  style,
  children,
  ...rest
}: VibrantTextProps) {
  const { colors } = useAppTheme();

  return (
    <Text
      {...rest}
      style={[
        { color: colors.title, fontWeight: vibrantWeight },
        Platform.OS === "ios" ? styles.iosShadow : null,
        style,
      ]}
    >
      {children}
    </Text>
  );
}

const styles = StyleSheet.create({
  iosShadow: {
    textShadowColor: "rgba(0,0,0,0.12)",
    textShadowOffset: { width: 0, height: 1 },
    textShadowRadius: 2,
  },
});
```
</action>

<acceptance_criteria>
- `test -f src/components/ios/VibrantText.tsx`
- `grep -n "export function VibrantText" src/components/ios/VibrantText.tsx` 返回非空
- `grep -n "textShadowColor" src/components/ios/VibrantText.tsx` 返回非空
- `grep -n "useAppTheme" src/components/ios/VibrantText.tsx` 返回非空
- `tsc --noEmit` 通过
</acceptance_criteria>

---

## Wave 1 整体验收
- [ ] 4 tasks 全部通过 acceptance_criteria
- [ ] `npm run verify` 绿（本 Wave 不加测试，只保持原 17 tests 全绿）
- [ ] 未修改任何业务组件
