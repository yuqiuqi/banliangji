---
phase: 20
plan: 20-01
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - src/components/ios/HeaderIconButton.tsx
  - src/components/ios/SegmentedTwo.tsx
  - src/components/ios/GlassEffectContainer.tsx
  - src/components/ios/GlassEffectUnion.tsx
  - src/components/ios/MonthYearPickerSheet.tsx
  - src/screens/BillQueryScreen.tsx
  - src/screens/CreateBillScreen.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/ChartScreen.tsx
objective: "回溯波次 — Phase 20 UI 精修已合入；本 plan 将 `20-CONTEXT`/`20-VERIFICATION` 与仓库现状对齐为可 grep 的回归基线"
---

# Plan 20-01: Phase 20 UI 精修 — 回溯与回归基线

## must_haves

- `npm run verify` 退出码 0（tsc + eslint + vitest）
- `HeaderIconButton` 固定外层 `headerFabSize` + 内层 `SpringPressable` 压感（当前实现：`scaleTo={1}`、`opacityTo={0.8}`）
- `BillQueryScreen` / `CreateBillScreen` 日期 overlay：`GlassEffectContainer` + `VibrantText` + `DateTimePicker` `display="inline"`
- `SegmentedTwo` 使用 `SPRING.THUMB` 驱动 thumb
- `ChartScreen` 柱体/分类条含渐变与阴影语义（以代码为准）
- 推迟项仅出现在 `20-VERIFICATION.md`「推迟到后续相位」表，不在本 plan 新实现

## threat_model

| ID | 资产/面 | 威胁 | 严重度 | 缓解（本相） | 余留 |
|----|----------|------|--------|--------------|------|
| T-20-01 | 本地用户数据在屏上展示 | 无新增收集；仅 UI 展示路径 | 低 | 无新网络/无新存储 API | 继承应用既有威胁模型 |
| T-20-02 | 依赖的第三方组件 | 供应链投毒 | 低 | 仅 `package.json` 已锁定版本，本 plan 不新增 dep | 常规 `npm audit` |
| T-20-03 | 无障碍/误触 | 错误 Modal 关合 | 低 | 沿用既有 Modal 与 a11y 标签 | 手测表 |

**Block 阈值:** 无 `high` 及以上与本相新引入行为相关；本相为样式与动效，无新密钥/无 eval。

---

## Task 20-01-0 — 工具链回归门（最高优先级）

<read_first>
- `package.json` 中 `scripts.verify`
- `.planning/phases/20-ui-polish/20-VERIFICATION.md` § verify
</read_first>

<action>
在仓库根执行：

```bash
npm run verify
```

预期：`tsc --noEmit`、`eslint`（max-warnings 0 若配置如此）、`vitest run` 全部通过。
</action>

<acceptance_criteria>
- 命令 `npm run verify` 退出码为 `0`
- 标准输出/错误中无 `FAIL` / `Error:` 与测试失败堆栈
</acceptance_criteria>

---

## Task 20-01-1 — `HeaderIconButton` 外框隔离

<read_first>
- `src/components/ios/HeaderIconButton.tsx`
- `.planning/phases/20-ui-polish/20-VERIFICATION.md` §1
</read_first>

<action>
不修改业务逻辑，仅作回归确认：顶栏图标本体使用固定 `headerFabSize` 外层 `View`，`SpringPressable` 使用 `scaleTo={1}` 与 `opacityTo={0.8}` 作为压感（与 2026-04 合入一致）。
</action>

<acceptance_criteria>
- `HeaderIconButton.tsx` 中 `styles.outer` 同时包含 `width: headerFabSize` 与 `height: headerFabSize`
- 同一文件 `scaleTo={1}` 与 `opacityTo={0.8}` 各至少出现 1 次
</acceptance_criteria>

---

## Task 20-01-2 — 日期/月份 picker 玻璃 + inline

<read_first>
- `src/screens/BillQueryScreen.tsx`
- `src/screens/CreateBillScreen.tsx`
- `src/components/ios/MonthYearPickerSheet.tsx`
</read_first>

<action>
确认 Bill/Create 的日期 Modal 内使用 `GlassEffectContainer` + 文案「完成」经 `VibrantText`；`DateTimePicker` 为 `display="inline"`。首页经 `HomeScreen` → `MonthYearPickerSheet` 走玻璃化月份选择（`GlassEffectContainer` 在子组件中）。
</action>

<acceptance_criteria>
- `BillQueryScreen.tsx` 中 `display="inline"` 至少 1 处；且同文件 `GlassEffectContainer` 与 `VibrantText` 被 import/使用
- `CreateBillScreen.tsx` 同样满足 `display="inline"` + `GlassEffectContainer` + `VibrantText`
- `MonthYearPickerSheet.tsx` 含 `GlassEffectContainer` 与 `VibrantText` 的 import/JSX
</acceptance_criteria>

---

## Task 20-01-3 — `SegmentedTwo` Spring Thumb

<read_first>
- `src/components/ios/SegmentedTwo.tsx`
- `src/theme/motion.ts`（`SPRING.THUMB` 定义处）
</read_first>

<action>
确认分段控件 thumb 位移动画使用 `SPRING.THUMB`（与 Reduce Motion 分支并存）。
</action>

<acceptance_criteria>
- `SegmentedTwo.tsx` 含子串 `SPRING.THUMB`
- `withSpring` 或等价调用与 thumb 的 `translateX`/`thumbX` 逻辑同文件可检索
</acceptance_criteria>

---

## Task 20-01-4 — 图表柱体与分类条立体/渐变

<read_first>
- `src/screens/ChartScreen.tsx`
- `src/theme/palette` 或 `useAppTheme` 中 `expense` 色引用
</read_first>

<action>
确认 `ChartScreen` 中柱体或分类条使用 `LinearGradient` 或 shadow + 品牌支出色，满足「立体」视觉（以当前合入为准，不作像素重写）。
</action>

<acceptance_criteria>
- `ChartScreen.tsx` 中 `LinearGradient` 出现至少 1 次
- 同文件 `shadow` 或 `Shadow` 相关 style 与 `expense` / `colors.expense` 至少一处共现
</acceptance_criteria>

---

## Task 20-01-5 — 推迟项登记

<read_first>
- `.planning/phases/20-ui-polish/20-VERIFICATION.md`「推迟到后续相位」
</read_first>

<action>
不实现新功能；仅确认「陀螺仪高光」「3D 柱图」等仍只在表格中，不在本 plan 的代码任务中。
</action>

<acceptance_criteria>
- `20-VERIFICATION.md` 含二级标题 `## 推迟到后续相位` 且表格中 **Phase 21** 或 **22** 至少各出现 1 次
</acceptance_criteria>

---

## PLANNING COMPLETE

- 1 个 plan，1 个 wave，6 个任务 + 0 个 verify 任务已封装在 Task 20-01-0
