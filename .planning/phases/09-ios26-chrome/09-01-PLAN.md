---
phase: 09-ios26-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/colors.ts
  - src/theme/layout.ts
  - src/theme/typography.ts
  - src/components/ios/GroupedInset.tsx
  - src/components/ios/ListRow.tsx
  - src/components/ios/SegmentedTwo.tsx
  - src/components/ios/index.ts
autonomous: true
requirements:
  - IOS26-COMP
must_haves:
  truths:
    - "`src/components/ios/` 下至少存在 GroupedInset、ListRow、SegmentedTwo 三个导出组件"
    - "`colors.ts` 导出与 `09-UI-SPEC` §1 一致的语义 token（或 re-export），且无 Tesla 绿 `#4A8B6A` 作为主强调"
  artifacts:
    - src/components/ios/GroupedInset.tsx
    - src/theme/typography.ts
---

<objective>
建立 **iOS 26 Chrome 共享原语** 与 **类型刻度**：供后续各屏组合使用，落实 IOS26-COMP。
</objective>

<threat_model>
## Trust boundaries
- 仅本地 UI；无网络、无新权限。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P9-01 | Tampering | N/A — 无服务端 |
| P9-02 | XSS | N/A — RN 非 Web |
</threat_model>

<context>
@.planning/phases/09-ios26-chrome/09-CONTEXT.md
@.planning/phases/09-ios26-chrome/09-UI-SPEC.md
@src/theme/colors.ts
@src/theme/layout.ts
</context>

<tasks>

<task type="execute">
  <name>语义色与 typography 文件</name>
  <files>src/theme/colors.ts, src/theme/layout.ts, src/theme/typography.ts</files>
  <read_first>src/theme/colors.ts, src/theme/layout.ts, .planning/phases/09-ios26-chrome/09-UI-SPEC.md</read_first>
  <action>
    1) 在 `colors.ts` 增加 **与 Apple 语义对齐的导出**（可选用 `export const iosSemantic = { groupedBackground: colors.canvas, ... }` 或等价），至少包含键名：`groupedBackground`、`secondaryGrouped`、`label`、`secondaryLabel`、`separator`、`systemBlue`、`systemGreen`、`systemRed`（值与 `09-UI-SPEC` §1 表一致）。
    2) 新建 `src/theme/typography.ts`，导出对象 `iosType`，至少含：`navTitle: { fontSize: 17, fontWeight: "600" }`、`body: { fontSize: 17 }`、`footnote: { fontSize: 13 }`、`caption1: { fontSize: 12 }`（数值可微调 ±1，但须在文件顶注释「对齐 UI-SPEC」）。
    3) `rg "#4A8B6A" src/theme/colors.ts` 结果为空（主强调不得再为 Tesla 绿）。
  </action>
  <acceptance_criteria>
    - `test -f src/theme/typography.ts`
    - `rg "iosSemantic|groupedBackground" src/theme/colors.ts` 至少 1 匹配
    - `rg "iosType" src/theme/typography.ts` 至少 1 匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>GroupedInset ListRow SegmentedTwo</name>
  <files>src/components/ios/GroupedInset.tsx, src/components/ios/ListRow.tsx, src/components/ios/SegmentedTwo.tsx, src/components/ios/index.ts</files>
  <read_first>src/theme/colors.ts, src/theme/layout.ts, .planning/phases/09-ios26-chrome/09-UI-SPEC.md</read_first>
  <action>
    1) 新建目录 `src/components/ios/`。
    2) **GroupedInset**：接收 `children`；容器样式：`backgroundColor: colors.surface`（或 secondaryGrouped）、`borderRadius: 10`、`marginHorizontal: 16`、可选 `overflow: "hidden"`；使用 `colors.body` hairline 边框宽度 1。
    3) **ListRow**：接收 `children`、`onPress?`、`isLast?`；`minHeight: 44`、`paddingHorizontal: 16`、`paddingVertical: 12`；底部分隔线除非 `isLast` 为 true；`Pressable` 按压 `opacity` 使用 `pressedOpacity` from `layout.ts`。
    4) **SegmentedTwo**：props：`leftLabel: string`、`rightLabel: string`、`value: 0 | 1`、`onChange: (v: 0 | 1) => void`；选中侧背景 `rgba(0,122,255,0.12)` 或 `colors.systemBlue` 浅底 + 字色 `007AFF`；未选中 `secondaryLabel` 色。
    5) **index.ts** 导出上述三组件。
  </action>
  <acceptance_criteria>
    - `test -f src/components/ios/GroupedInset.tsx`
    - `test -f src/components/ios/ListRow.tsx`
    - `test -f src/components/ios/SegmentedTwo.tsx`
    - `rg "export.*GroupedInset" src/components/ios/index.ts`
  </acceptance_criteria>
</task>

</tasks>
