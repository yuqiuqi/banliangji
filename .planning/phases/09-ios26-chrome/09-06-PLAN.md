---
phase: 09-ios26-chrome
plan: 06
type: execute
wave: 3
depends_on:
  - "09-01"
files_modified:
  - src/screens/BudgetScreen.tsx
  - src/screens/AssetScreen.tsx
  - src/screens/MineScreen.tsx
autonomous: true
requirements:
  - IOS26-CHROME
  - IOS26-UX
must_haves:
  truths:
    - "预算 / 资产 / 我的 列表与卡片使用 grouped 语义色与圆角 10"
    - "我的页 chevron 或 disclosure 使用 systemBlue 或 secondaryLabel 符合设置 app 向"
  artifacts:
    - src/screens/BudgetScreen.tsx
    - src/screens/AssetScreen.tsx
    - src/screens/MineScreen.tsx
---

<objective>
收敛 **Budget、Asset、Mine**（§3 行 8–10）。
</objective>

<threat_model>
| threat_id | disposition |
|-----------|-------------|
| P9-01 | 本地 |
</threat_model>

<context>
@src/screens/BudgetScreen.tsx
@src/screens/AssetScreen.tsx
@src/screens/MineScreen.tsx
@src/components/ios/GroupedInset.tsx
</context>

<tasks>

<task type="execute">
  <name>Budget Asset 卡片</name>
  <files>src/screens/BudgetScreen.tsx, src/screens/AssetScreen.tsx</files>
  <read_first>src/screens/BudgetScreen.tsx, src/screens/AssetScreen.tsx, src/theme/colors.ts, src/theme/layout.ts</read_first>
  <action>
    1) 两屏 `backgroundColor` 大底为 `colors.canvas`；内容卡片 `colors.surface`、`borderRadius: radii.card`（10）。
    2) 进度条前景色 `colors.accent`（#007AFF）。
  </action>
  <acceptance_criteria>
    - `rg "colors.surface" src/screens/BudgetScreen.tsx` 至少 1 行
    - `rg "colors.surface" src/screens/AssetScreen.tsx` 至少 1 行
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Mine inset grouped</name>
  <files>src/screens/MineScreen.tsx</files>
  <read_first>src/screens/MineScreen.tsx, src/components/ios/GroupedInset.tsx, src/components/ios/ListRow.tsx</read_first>
  <action>
    1) 每个设置块用 **GroupedInset** 包裹或样式等价；块间距 8–12pt。
    2) `chevron` / 展开提示色为 `colors.accent`（#007AFF）。
    3) 标题「我的」使用 `iosType.navTitle` 或 `fontSize: 20` + `fontWeight: "600"`。
  </action>
  <acceptance_criteria>
    - `rg "GroupedInset" src/screens/MineScreen.tsx` 至少 1 匹配
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
</task>

</tasks>
