---
phase: 09-ios26-chrome
plan: 05
type: execute
wave: 3
depends_on:
  - "09-01"
files_modified:
  - src/screens/CalendarScreen.tsx
  - src/screens/ChartScreen.tsx
autonomous: true
requirements:
  - IOS26-CHROME
  - IOS26-UX
must_haves:
  truths:
    - "日历网格卡片与列表卡片使用与 Home 一致的 grouped 样式"
    - "图表周/月/年切换与 Tab 芯片样式使用 ACCENT 基于 #007AFF 的透明填充（与现 ChartScreen 一致）"
  artifacts:
    - src/screens/CalendarScreen.tsx
    - src/screens/ChartScreen.tsx
---

<objective>
收敛 **Calendar** 与 **Chart**（§3 行 6–7）；图表数据逻辑不变。
</objective>

<threat_model>
| threat_id | disposition |
|-----------|-------------|
| P9-01 | 只读聚合 |
</threat_model>

<context>
@src/screens/CalendarScreen.tsx
@src/screens/ChartScreen.tsx
@.planning/phases/09-ios26-chrome/09-UI-SPEC.md
</context>

<tasks>

<task type="execute">
  <name>Calendar grouped 与 FAB</name>
  <files>src/screens/CalendarScreen.tsx</files>
  <read_first>src/screens/CalendarScreen.tsx, src/components/ios/GroupedInset.tsx</read_first>
  <action>
    1) `gridCard` 与 `listCard` 的 `borderRadius` 统一为 **10**（`radii.card`）。
    2) FAB 背景 `colors.accent`，图标色 `colors.onAccent`（已满足则保留）。
  </action>
  <acceptance_criteria>
    - `rg "radii.card" src/screens/CalendarScreen.tsx` 至少 1 行
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Chart 分段与卡片</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>src/screens/ChartScreen.tsx, src/components/ios/SegmentedTwo.tsx</read_first>
  <action>
    1) 若周/月/年切换为自定义 chip，评估改为 **SegmentedTwo 的扩展**（三档）或保留 chip 但 **边框/选中色** 必须为 `007AFF` 系；若保留三档 chip，须在代码注释 `// IOS26: 三档 segmented，SegmentedTwo 仅两档`。
    2) 卡片 `backgroundColor` 为 `colors.surface`；主进度条 `colors.accent`。
  </action>
  <acceptance_criteria>
    - `rg "007AFF|accent" src/screens/ChartScreen.tsx` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>
