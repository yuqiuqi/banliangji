---
phase: 09-ios26-chrome
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - src/screens/HomeScreen.tsx
  - src/screens/BillQueryScreen.tsx
  - src/screens/BillDetailScreen.tsx
autonomous: true
requirements:
  - IOS26-CHROME
  - IOS26-UX
must_haves:
  truths:
    - "明细 / 账单 / 账单详情 使用 GroupedInset 或 ListRow 等原语重构外层列表容器与行（允许渐进：至少 **列表区** 与 **卡片** 使用原语）"
    - "支出金额色为 `colors.expense`（#FF3B30），收入为 `colors.income`（#34C759）"
  artifacts:
    - src/screens/HomeScreen.tsx
    - src/screens/BillQueryScreen.tsx
    - src/screens/BillDetailScreen.tsx
---

<objective>
收敛 **HomeMain、BillQuery、BillDetail** 至 `09-BREAKTHROUGH-SPEC` §3 行 1–3 的 UI/UX。
</objective>

<threat_model>
| threat_id | disposition |
|-----------|-------------|
| P9-01 | 本地 SQLite 展示不变 |
</threat_model>

<context>
@.planning/phases/09-ios26-chrome/09-BREAKTHROUGH-SPEC.md
@src/components/ios/GroupedInset.tsx
@src/components/ios/ListRow.tsx
</context>

<tasks>

<task type="execute">
  <name>HomeScreen grouped 列表壳</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>src/screens/HomeScreen.tsx, src/components/ios/GroupedInset.tsx, src/theme/typography.ts</read_first>
  <action>
    1) 将 `listCard` 区域改为使用 **GroupedInset** 包裹 `SectionList`，或把 `listCard` 样式改为与 GroupedInset 相同 token（`borderRadius: 10`、`marginHorizontal: 16`、`backgroundColor: colors.surface`）。
    2) `sectionHead` 使用 `iosType.footnote` 或 `caption1` 的字号 + `colors.lightTitle`。
    3) 月汇总条 `headerBanner` 保持功能；分隔线使用 `colors.body`（已实现则保留）。
  </action>
  <acceptance_criteria>
    - `rg "GroupedInset|listCard" src/screens/HomeScreen.tsx` 至少 1 匹配
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BillQuery 分段与原语</name>
  <files>src/screens/BillQueryScreen.tsx</files>
  <read_first>src/screens/BillQueryScreen.tsx, src/components/ios/SegmentedTwo.tsx, src/components/ios/GroupedInset.tsx</read_first>
  <action>
    1) 单日/区间切换 UI 改为使用 **SegmentedTwo**（若当前为双 Pressable，替换为组件调用，标签文案仍为「单日」「区间」或等价中文）。
    2) 列表外层容器与 Home 一致使用 GroupedInset 或等价样式 token。
  </action>
  <acceptance_criteria>
    - `rg "SegmentedTwo" src/screens/BillQueryScreen.tsx` 至少 1 行
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BillDetail 分组卡片</name>
  <files>src/screens/BillDetailScreen.tsx</files>
  <read_first>src/screens/BillDetailScreen.tsx, src/components/ios/GroupedInset.tsx</read_first>
  <action>
    1) `detailCard` 用 GroupedInset 包裹或样式与之一致（圆角 10、白底、hairline）。
    2) `hero` 区背景 `colors.main`；`footerDestructive` 文字色为 `#FF3B30`（或 `colors.expense`）。
  </action>
  <acceptance_criteria>
    - `rg "GroupedInset|detailCard" src/screens/BillDetailScreen.tsx` 至少 1 匹配
    - `rg "footerDestructive|expense|FF3B30" src/screens/BillDetailScreen.tsx` 至少 1 匹配
  </acceptance_criteria>
</task>

</tasks>
