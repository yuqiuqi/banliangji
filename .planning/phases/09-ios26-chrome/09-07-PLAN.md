---
phase: 09-ios26-chrome
plan: 07
type: execute
wave: 4
depends_on:
  - "09-03"
  - "09-04"
  - "09-05"
  - "09-06"
files_modified:
  - App.tsx
  - DESIGN.md
  - .planning/PROJECT.md
  - .planning/phases/09-ios26-chrome/09-VERIFICATION.md
autonomous: true
requirements:
  - IOS26-IPAD
  - IOS26-VERIFY
must_haves:
  truths:
    - "根布局对宽屏限制 `maxWidth`（建议 600–720）并水平居中（或等价 `useWindowDimensions` 分支）"
    - "`DESIGN.md` 声明主视觉为 iOS 26 Chrome；`PROJECT.md` Current Milestone 指向 v2.2"
    - "`rg '#4A8B6A' src/` 无匹配（除 `.planning` 与注释历史外）"
  artifacts:
    - App.tsx
    - DESIGN.md
---

<objective>
**iPad 可读宽度**、**文档单一来源更新**、**全仓 Tesla 绿硬编码扫除**、**09-VERIFICATION 填表**，落实 IOS26-IPAD / IOS26-VERIFY。
</objective>

<threat_model>
| threat_id | disposition |
|-----------|-------------|
| P9-01 | 文档与布局 |
</threat_model>

<context>
@App.tsx
@DESIGN.md
@.planning/PROJECT.md
@.planning/phases/09-ios26-chrome/09-VERIFICATION.md
</context>

<tasks>

<task type="execute">
  <name>根级 maxWidth 居中</name>
  <files>App.tsx</files>
  <read_first>App.tsx, src/navigation/RootNavigator.tsx</read_first>
  <action>
    1) 在 `App.tsx` 中，用 `View` 包裹 `RootNavigator`，`style={{ flex: 1, alignItems: "center", backgroundColor: /* groupedBackground */ }}`。
    2) 内层 `View` `style={{ flex: 1, width: "100%", maxWidth: 720 }}`（常量可提取为 `MAX_APP_WIDTH = 720`）。
    3) 小屏手机宽度 100% 不受影响。
  </action>
  <acceptance_criteria>
    - `rg "maxWidth" App.tsx` 至少 1 行
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>DESIGN PROJECT 更新</name>
  <files>DESIGN.md, .planning/PROJECT.md</files>
  <read_first>DESIGN.md, .planning/PROJECT.md, .planning/phases/09-ios26-chrome/09-BREAKTHROUGH-SPEC.md</read_first>
  <action>
    1) `DESIGN.md` 首段改为：**SwiftCostRN 主视觉为 iOS / iPadOS 26 Chrome**（Liquid Glass、语义色见 `09-UI-SPEC` / `colors.ts`）；Tesla 方案标记为 **历史 / v2.1 前**。
    2) `PROJECT.md` 的 **Current Milestone** 增加或改为 **v2.2 Phase 9** 进行中/已规划说明，并链接 `09-BREAKTHROUGH-SPEC.md`。
  </action>
  <acceptance_criteria>
    - `rg "iOS|iPadOS|26|Chrome" DESIGN.md` 至少 2 匹配
    - `rg "v2.2|Phase 9" .planning/PROJECT.md` 至少 1 匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>扫除 Tesla 绿与 VERIFICATION</name>
  <files>（仓库 grep）, .planning/phases/09-ios26-chrome/09-VERIFICATION.md</files>
  <read_first>.planning/phases/09-ios26-chrome/09-VERIFICATION.md</read_first>
  <action>
    1) 运行 `rg "#4A8B6A" src/`；若命中，替换为 `colors.accent` 或 `007AFF` 语义。
    2) 更新 `09-VERIFICATION.md`：在实现完成后将 § 表格 UI/UX 列勾为 Pass（或由执行人在合并前填写）。
    3) 运行 `npm run verify` 并记录全绿。
  </action>
  <acceptance_criteria>
    - `rg "#4A8B6A" src/` 退出码 1（无匹配）或仅命中已解释的测试夹具
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>
