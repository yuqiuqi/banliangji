---
phase: 09-ios26-chrome
plan: 02
type: execute
wave: 1
depends_on:
  - "09-01"
files_modified:
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements:
  - IOS26-CHROME
  - IOS26-UX
must_haves:
  truths:
    - "Tab 栏 iOS 使用 BlurView；Android 使用不透明底栏色（见 09-UI-SPEC §2）"
    - "HomeStack `screenOptions` 中 header 样式与 `colors.main` / `onMain` 一致且全栈统一（模态 group 可保留略深）"
  artifacts:
    - src/navigation/RootNavigator.tsx
---

<objective>
统一 **全局导航 Chrome**：Tab 材质、分隔线、栈顶栏配色与阴影策略，落实 IOS26-CHROME / IOS26-UX 全局行（§3 第 11–12 行）。
</objective>

<threat_model>
## Trust boundaries
- 导航配置仅影响本地 UI。

## STRIDE
| threat_id | disposition |
|-----------|-------------|
| P9-01 | N/A |
</threat_model>

<context>
@.planning/phases/09-ios26-chrome/09-UI-SPEC.md
@src/navigation/RootNavigator.tsx
</context>

<tasks>

<task type="execute">
  <name>Tab 与 NavigationContainer 主题对齐</name>
  <files>src/navigation/RootNavigator.tsx</files>
  <read_first>src/navigation/RootNavigator.tsx, src/theme/colors.ts, .planning/phases/09-ios26-chrome/09-UI-SPEC.md</read_first>
  <action>
    1) 保持 `navTheme.colors.background` 为 `colors.canvas`（`#F2F2F7` 取向）。
    2) **Tab**：`tabBarBackground` 仅在 `Platform.OS === "ios"` 时使用 `BlurView`；`tabBarStyle.borderTopColor` 使用 `rgba(60, 60, 67, 0.29)`；Android 时 `tabBarBackground` 为 `undefined`，`backgroundColor` 为 `#F9F9F9`。
    3) **HomeStack.Navigator** `screenOptions`：`headerStyle.backgroundColor` 为 `colors.main`（`#F6F6F6`）；`headerTintColor` / `headerTitleStyle.color` 为 `colors.onMain`；非模态屏 `headerShadowVisible: true`（与现 Phase 9 目标一致）。
    4) 模态 `Group` 保持 `presentation: "modal"`，header 同色系统一。
  </action>
  <acceptance_criteria>
    - `rg "BlurView" src/navigation/RootNavigator.tsx` 至少 1 行
    - `rg "tabBarBackground" src/navigation/RootNavigator.tsx` 至少 1 行
    - `rg "F9F9F9" src/navigation/RootNavigator.tsx` 至少 1 行（Android 分支）
  </acceptance_criteria>
</task>

</tasks>
