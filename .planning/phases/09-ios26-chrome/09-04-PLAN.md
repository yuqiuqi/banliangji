---
phase: 09-ios26-chrome
plan: 04
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/screens/CreateBillScreen.tsx
  - src/components/BillCalculator.tsx
autonomous: true
requirements:
  - IOS26-CHROME
  - IOS26-UX
must_haves:
  truths:
    - "记一笔主按钮与类型切换符合 systemBlue / 分段范式"
    - "计算器键帽圆角、分隔线、按压态与 `09-UI-SPEC` §4 一致"
  artifacts:
    - src/screens/CreateBillScreen.tsx
    - src/components/BillCalculator.tsx
---

<objective>
收敛 **记一笔** 与 **BillCalculator**（§3 行 4–5）。
</objective>

<threat_model>
| threat_id | disposition |
|-----------|-------------|
| P9-01 | 本地表单 |
</threat_model>

<context>
@.planning/phases/09-ios26-chrome/09-UI-SPEC.md
@src/components/ios/SegmentedTwo.tsx
</context>

<tasks>

<task type="execute">
  <name>CreateBill 分段与分组</name>
  <files>src/screens/CreateBillScreen.tsx</files>
  <read_first>src/screens/CreateBillScreen.tsx, src/components/ios/SegmentedTwo.tsx, src/theme/colors.ts</read_first>
  <action>
    1) 支出/收入切换若当前为双 Tab Pressable，改为 **SegmentedTwo**（`leftLabel="支出"` `rightLabel="收入"` 或项目现有文案）。
    2) 表单区块背景与 `colors.canvas` / `colors.surface` 分层与 `09-UI-SPEC` 一致；主保存按钮背景 `colors.accent`（#007AFF）、文字 `colors.onAccent`。
  </action>
  <acceptance_criteria>
    - `rg "SegmentedTwo" src/screens/CreateBillScreen.tsx` 至少 1 行 **或** 注释说明保留双 Pressable 的 Accepted deviation（不得无解释跳过）
    - 若已用 SegmentedTwo：`npm run typecheck` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BillCalculator 键区</name>
  <files>src/components/BillCalculator.tsx</files>
  <read_first>src/components/BillCalculator.tsx, src/theme/layout.ts, src/theme/colors.ts</read_first>
  <action>
    1) 键帽 `borderRadius` 使用 `radii.chip`（10）或 8–12 范围单值。
    2) `calculatorKeyBorder` 使用 `colors.calculatorKeyBorder`；背景 `colors.calculatorKeyBg`。
    3) 主「确认/等号」类按钮保持 `colors.accent` 填充。
  </action>
  <acceptance_criteria>
    - `rg "borderRadius" src/components/BillCalculator.tsx` 至少 1 行
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>
