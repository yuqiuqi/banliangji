---
phase: 02-core-user-flow-uat
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - .planning/phases/02-core-user-flow-uat/02-UAT-RECORD.md
  - .planning/phases/02-core-user-flow-uat/02-VALIDATION.md
user_setup:
  - 与 02-01 相同：模拟器或真机可运行本构建
autonomous: false
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-05
must_haves:
  truths:
    - "02-UAT-RECORD.md 含复测表且每 FEAT 至少一行 Pass/Fail"
    - "主路径 3 分钟结论有明确计时或阻塞说明"
    - "npm run typecheck 与 npm run lint 退出码 0"
  artifacts:
    - .planning/phases/02-core-user-flow-uat/02-UAT-RECORD.md
  key_links:
    - "与 ROADMAP Success Criteria 1–3 可对照"
---

<objective>
在 02-02 合并后 **复测** 全主路径，填写 `02-UAT-RECORD.md`；更新 `02-VALIDATION.md` frontmatter 或 Sign-Off 区；确认 **typecheck/lint** 仍绿。满足路线图「02-03: 复测与简要 UAT 记录」。

Purpose: 留下可审计的 UAT 痕迹，支撑 Phase 2 完结与 REQUIREMENTS FEAT 行后续勾选。

Output: `02-UAT-RECORD.md`；可选更新 `02-VALIDATION.md`。
</objective>

<threat_model>
## Trust boundaries
- UAT 文档不含真实 PII；金额可用测试数据。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P2-T05 | Information disclosure | UAT 记录 | Low | 截图脱敏；路径用通用描述 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-core-user-flow-uat/02-01-ISSUES.md
@.planning/phases/02-core-user-flow-uat/02-CONTEXT.md
@.planning/phases/02-core-user-flow-uat/02-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>撰写 02-UAT-RECORD.md（复测表 + 3 分钟主路径）</name>
  <files>.planning/phases/02-core-user-flow-uat/02-UAT-RECORD.md</files>
  <read_first>
    .planning/phases/02-core-user-flow-uat/02-01-ISSUES.md,
    .planning/phases/02-core-user-flow-uat/02-CONTEXT.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    新建 `.planning/phases/02-core-user-flow-uat/02-UAT-RECORD.md`，必须包含：
    1) 标题 `# Phase 2 — UAT Record (02-03)` 与 `**Date:** YYYY-MM-DD`。
    2) 小节 `## Retest matrix`：表格列名须含（逐字）：`FEAT` | `Result` | `Evidence` | `Tester`。
       - 至少 5 行，`FEAT` 列分别为 `FEAT-01`、`FEAT-02`、`FEAT-03`、`FEAT-04`、`FEAT-05`。
       - `Result` 列仅允许 `Pass` 或 `Fail`（大小写一致）。
    3) 小节 `## 3-minute path`：至少一段，包含逐字短语 **`Timer`** 与 **`Blocker`** 之一或同时（用于计时/阻塞说明）；若无障碍写 `No blocker` 作为独立词组。
    4) 小节 `## Clay sign-off`：bullet 列表至少 2 条，引用 `02-UI-SPEC.md` 中的具体条目（圆角、边框、按压之一）。
  </action>
  <verify>test -f .planning/phases/02-core-user-flow-uat/02-UAT-RECORD.md</verify>
  <acceptance_criteria>
    - 文件 `.planning/phases/02-core-user-flow-uat/02-UAT-RECORD.md` 存在
    - 文件包含子串 `## Retest matrix` 与子串 `| FEAT |`
    - 文件包含 `FEAT-01`、`FEAT-02`、`FEAT-03`、`FEAT-04`、`FEAT-05` 各至少一次
    - 文件包含子串 `## 3-minute path` 且包含 `Timer` 或 `Blocker` 或同时包含；或包含 `No blocker`
    - 文件包含子串 `## Clay sign-off`
  </acceptance_criteria>
  <done>UAT 记录可用于路线图验收。</done>
</task>

<task type="auto">
  <name>更新 02-VALIDATION.md Sign-Off 勾选说明</name>
  <files>.planning/phases/02-core-user-flow-uat/02-VALIDATION.md</files>
  <read_first>
    .planning/phases/02-core-user-flow-uat/02-VALIDATION.md,
    .planning/phases/02-core-user-flow-uat/02-UAT-RECORD.md
  </read_first>
  <action>
    在 `02-VALIDATION.md` 的 `## Validation Sign-Off` 下，将首条 `- [ ] All tasks have` 改为已勾选形式 `- [x] All tasks have`（仅改该复选框一行）；若该条已为 `[x]` 则不改。
    在 `**Approval:**` 行追加日期，格式 `pending → 2026-04-21 reviewed`（日期用实际执行日）。
  </action>
  <verify>grep -q "\\[x\\] All tasks have" .planning/phases/02-core-user-flow-uat/02-VALIDATION.md</verify>
  <acceptance_criteria>
    - `02-VALIDATION.md` 含 `- [x] All tasks have`（x 为小写）
    - `02-VALIDATION.md` 的 `**Approval:**` 行含子串 `reviewed`
  </acceptance_criteria>
  <done>验证策略与 UAT 对齐。</done>
</task>

<task type="auto">
  <name>最终 typecheck 与 lint</name>
  <files>package.json</files>
  <read_first>
    package.json
  </read_first>
  <action>
    在仓库根执行 `npm run typecheck` 与 `npm run lint`，退出码均为 0。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run typecheck && npm run lint</verify>
  <acceptance_criteria>
    - `npm run typecheck` 在仓库根退出码 0
    - `npm run lint` 在仓库根退出码 0
  </acceptance_criteria>
  <done>Phase 2 技术门槛关闭。</done>
</task>

</tasks>

<verification>
- [ ] UAT 表齐全；失败项有对应 issue 或 defer
- [ ] VALIDATION Sign-Off 已更新
- [ ] typecheck + lint 全绿
</verification>

<success_criteria>
- ROADMAP Phase 2 成功标准可对照文档逐条满足或显式记录例外
</success_criteria>

<output>
完成后创建 `02-03-SUMMARY.md`（由 execute-phase 约定）。
</output>
