---
phase: 02-core-user-flow-uat
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/theme/layout.ts
  - src/screens/HomeScreen.tsx
  - src/screens/CreateBillScreen.tsx
  - src/screens/BillDetailScreen.tsx
  - src/screens/CalendarScreen.tsx
  - src/screens/ChartScreen.tsx
  - src/screens/MineScreen.tsx
  - src/navigation/RootNavigator.tsx
  - .planning/phases/02-core-user-flow-uat/02-01-ISSUES.md
user_setup: []
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-05
must_haves:
  truths:
    - "src/theme/layout.ts 导出 radii.card === 12 与 shadows.card 对象（见任务 1）"
    - "02-01-ISSUES.md 中每个 P0/P1 且 Area 为 Functional/Navigation 的行在 Notes 或新增列中有 Fix: 或明确 defer 说明"
    - "npm run typecheck 与 npm run lint 退出码 0"
  artifacts:
    - src/theme/layout.ts
  key_links:
    - "Clay 视觉以 02-UI-SPEC.md 与 colors.ts 为准"
---

<objective>
依据 `02-01-ISSUES.md` **优先**修复 P0/P1 **功能与导航**问题；同步将主路径屏幕向 **Clay** 收敛（圆角 12、燕麦边框 `colors.body`、卡片白底、Pressable 按压反馈），不改变 Phase 3 的图表聚合语义。

Purpose: 路线图「02-02: 修复 P0/P1 与 Clay UI」。

Output: 更新后的 `src/theme/layout.ts`、相关 `Screen`/`RootNavigator`；`02-01-ISSUES.md` 中已修项标注 `Fix:`。
</objective>

<threat_model>
## Trust boundaries
- 数据仍仅存本机 SQLite；本计划不新增网络权限或第三方 SDK。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P2-T03 | Tampering | 本地账单展示 | Low / accepted | 依赖 OS 沙箱；无服务端 |
| P2-T04 | Denial of Service | UI 渲染循环 | Low | 避免在 Pressable 中引入重计算；列表仍用 keyExtractor |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-core-user-flow-uat/02-01-ISSUES.md
@.planning/phases/02-core-user-flow-uat/02-CONTEXT.md
@.planning/phases/02-core-user-flow-uat/02-UI-SPEC.md
@.planning/phases/02-core-user-flow-uat/02-PATTERNS.md
@src/theme/colors.ts
@src/screens/HomeScreen.tsx
@src/screens/CreateBillScreen.tsx
@src/screens/BillDetailScreen.tsx
@src/screens/CalendarScreen.tsx
@src/screens/ChartScreen.tsx
@src/screens/MineScreen.tsx
@src/navigation/RootNavigator.tsx
</context>

<tasks>

<task type="auto">
  <name>新增主题 layout 常量（radii + card shadow）</name>
  <files>src/theme/layout.ts</files>
  <read_first>
    src/theme/colors.ts,
    .planning/phases/02-core-user-flow-uat/02-UI-SPEC.md,
    DESIGN.md
  </read_first>
  <action>
    新建 `src/theme/layout.ts`，导出：
    - `radii` 对象（`as const`），**必须**包含数值属性 `card`，其字面量为 **12**。
    - `shadows` 对象，**必须**包含键 `card`，值为适用于 RN `View` `style` 的扁平对象，且 **至少**包含 iOS 键 `shadowColor`、`shadowOffset`、`shadowOpacity`、`shadowRadius` 四者；Android 键 **`elevation`** 为 **数字 2–4 之间**（含端点）。
    另导出 `hairlineBorder`：`{ borderWidth: 1, borderColor: string }`，其中 `borderColor` 使用 `colors.body`（从 `colors.ts` import）。
  </action>
  <verify>test -f src/theme/layout.ts</verify>
  <acceptance_criteria>
    - 文件 `src/theme/layout.ts` 存在
    - `grep -E "card.*12|card: 12" src/theme/layout.ts` 有匹配（允许 `as const` 换行）
    - 文件包含子串 `shadowOpacity` 与子串 `elevation`
    - 文件包含子串 `colors.body` 或从 `colors` 解构的 `body` 用于边框色
  </acceptance_criteria>
  <done>主题常量可供各屏 import。</done>
</task>

<task type="auto">
  <name>按问题单处理 P0/P1 功能/导航项并标注 Fix</name>
  <files>.planning/phases/02-core-user-flow-uat/02-01-ISSUES.md, src/screens/HomeScreen.tsx, src/screens/CreateBillScreen.tsx, src/screens/BillDetailScreen.tsx, src/screens/CalendarScreen.tsx, src/navigation/RootNavigator.tsx</files>
  <read_first>
    .planning/phases/02-core-user-flow-uat/02-01-ISSUES.md,
    src/screens/HomeScreen.tsx,
    src/screens/CreateBillScreen.tsx,
    src/screens/BillDetailScreen.tsx,
    src/screens/CalendarScreen.tsx,
    src/navigation/RootNavigator.tsx
  </read_first>
  <action>
    阅读 `02-01-ISSUES.md` 的 `## Walkthrough matrix`：
    - 对每一行满足 **`Severity` 为 `P0` 或 `P1`** 且 **`Area` 为 `Functional` 或 `Navigation`** 的条目：在对应源码文件中实现修复，并在该行的 `Notes` 单元格（或表格下新增 `## Fix log` 小节按 ID 列表）追加文本 **`Fix: &lt;一句说明&gt;`**，说明须含 **至少一个** 修改过的相对路径如 `src/screens/...`。
    - 若不存在任何满足条件的行：在 `02-01-ISSUES.md` 末尾新增小节 `## Plan 02 notes`，其中单独一行必须为 **`No P0/P1 functional issues — UI-only scope.`**（逐字）。
    **禁止**在本任务中修改图表聚合 SQL/聚合函数（仅导航/表单/刷新类修复）。
  </action>
  <verify>grep -q "Fix:" .planning/phases/02-core-user-flow-uat/02-01-ISSUES.md || grep -q "No P0/P1 functional issues — UI-only scope." .planning/phases/02-core-user-flow-uat/02-01-ISSUES.md</verify>
  <acceptance_criteria>
    - `02-01-ISSUES.md` 含子串 `Fix:` **或** 含逐字行 `No P0/P1 functional issues — UI-only scope.`
    - 若含 `Fix:`：至少一处 `Fix:` 同行或相邻行出现子串 `src/screens/` 或 `src/navigation/`
  </acceptance_criteria>
  <done>功能类问题有关闭痕迹。</done>
</task>

<task type="auto">
  <name>主路径屏幕 Clay 收敛（import layout + 卡片圆角边框）</name>
  <files>src/screens/HomeScreen.tsx, src/screens/CreateBillScreen.tsx, src/screens/BillDetailScreen.tsx, src/screens/CalendarScreen.tsx, src/screens/ChartScreen.tsx, src/screens/MineScreen.tsx</files>
  <read_first>
    src/theme/layout.ts,
    src/theme/colors.ts,
    .planning/phases/02-core-user-flow-uat/02-UI-SPEC.md,
    src/screens/HomeScreen.tsx,
    src/screens/CreateBillScreen.tsx,
    src/screens/BillDetailScreen.tsx,
    src/screens/CalendarScreen.tsx,
    src/screens/ChartScreen.tsx,
    src/screens/MineScreen.tsx
  </read_first>
  <action>
    在上述 **6 个** Screen 文件中，每个文件必须：
    1) 增加 `import` 语句，从 `src/theme/layout` 导入 **`radii`** 以及 **`shadows`**。
    2) 至少一处主内容容器（卡片、列表外包 `View` 或等价）的 `style` **数组或对象**中引用 `radii.card` 作为 `borderRadius`，并设置 `backgroundColor: colors.white` **或** 保持白底但增加 `borderWidth: 1` + `borderColor: colors.body` 二者之一组合（与 `02-UI-SPEC` 白卡浮在 canvas 上一致）。
    3) 至少一处 `Pressable`（若无 `Pressable` 则用 `TouchableOpacity` 的 `activeOpacity` 达到 ≤0.96）体现按压反馈。
    **不得**删除既有业务逻辑；**不得**修改 `ChartScreen` 内数据聚合计算（仅样式与分段控件外观）。
  </action>
  <verify>grep -l "theme/layout" src/screens/HomeScreen.tsx src/screens/CreateBillScreen.tsx src/screens/BillDetailScreen.tsx src/screens/CalendarScreen.tsx src/screens/ChartScreen.tsx src/screens/MineScreen.tsx | wc -l | grep -q "[6-9]"</verify>
  <acceptance_criteria>
    - 六个文件每个均包含子串 `theme/layout` 或子串 `from \"../theme/layout\"` 或子串 `from '../theme/layout'` 或子串 `layout` 导入路径指向 `src/theme/layout`（允许别名）
    - 六个文件每个均包含子串 `borderRadius` 与子串 `card`（同一文件内可不同行）
    - `grep -l "Pressable\\|activeOpacity" src/screens/HomeScreen.tsx src/screens/CreateBillScreen.tsx src/screens/BillDetailScreen.tsx src/screens/CalendarScreen.tsx src/screens/ChartScreen.tsx src/screens/MineScreen.tsx | wc -l` 输出 ≥ 4
  </acceptance_criteria>
  <done>主路径视觉与 UI-SPEC 对齐可感知。</done>
</task>

<task type="auto">
  <name>RootNavigator 与 canvas 一致性的轻量对齐</name>
  <files>src/navigation/RootNavigator.tsx</files>
  <read_first>
    src/navigation/RootNavigator.tsx,
    src/theme/colors.ts,
    .planning/phases/02-core-user-flow-uat/02-UI-SPEC.md
  </read_first>
  <action>
    在 `RootNavigator.tsx` 中确保 Tab/Stack 的 `screenOptions` 或 `contentStyle` **至少一处**使用 `colors.canvas` 作为背景（若已满足则仅补充注释 `// Clay canvas` 一行即可，不得删除现有逻辑）。
  </action>
  <verify>grep -q "colors.canvas" src/navigation/RootNavigator.tsx</verify>
  <acceptance_criteria>
    - `src/navigation/RootNavigator.tsx` 包含子串 `colors.canvas`
  </acceptance_criteria>
  <done>导航壳层与画布色一致。</done>
</task>

<task type="auto">
  <name>Typecheck 与 lint</name>
  <files>package.json</files>
  <read_first>
    package.json,
    tsconfig.json
  </read_first>
  <action>
    在仓库根目录执行：`npm run typecheck` 然后 `npm run lint`，均须退出码 0。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/swiftcost-rn/SwiftCostRN && npm run typecheck && npm run lint</verify>
  <acceptance_criteria>
    - 在 `SwiftCostRN` 根目录 `npm run typecheck` 退出码 0
    - 同目录 `npm run lint` 退出码 0
  </acceptance_criteria>
  <done>自动化门槛满足 VALIDATION 策略。</done>
</task>

</tasks>

<verification>
- [ ] `src/theme/layout.ts` 已创建并被 6 屏引用
- [ ] P0/P1 功能项已关闭或显式 defer
- [ ] typecheck + lint 全绿
</verification>

<success_criteria>
- FEAT 主路径可继续进入 02-03 复测；图表聚合逻辑未改
</success_criteria>

<output>
完成后由 execute-phase 生成 `02-02-SUMMARY.md`（若工作流启用）。
</output>
