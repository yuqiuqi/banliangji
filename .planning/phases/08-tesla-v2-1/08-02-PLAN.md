---
phase: 08-tesla-v2-1
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - src/utils/billTimeRange.test.ts
  - src/screens/MineScreen.tsx
  - .planning/phases/08-tesla-v2-1/08-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - V21-02
  - V21-03
  - V21Q-01
must_haves:
  truths:
    - "`08-VERIFICATION.md` 含可 grep 的「同条件同数」验收矩阵，覆盖 Home/Calendar/BillQuery 至少各一条"
    - "`npm run verify` 在 Phase 8 收口红线内全绿"
  artifacts:
    - .planning/phases/08-tesla-v2-1/08-VERIFICATION.md
    - src/utils/billTimeRange.test.ts
  key_links:
    - "V21-03 我的屏关于+数据+设置占位 与 `08-UI-SPEC` 可对照"
---

<objective>
完成 V21-02 的**可追溯性证明**、V21-03 **我的** 扩展、V21Q-01 质量与 **billTimeRange** 单测；落盘 `08-VERIFICATION.md` 并轻量更新 `ROADMAP` / `STATE` 进度（**不**执行 gsd `state planned-phase` 前须先有可运行 verify，本计划最后一步为 verify）。
</objective>

<threat_model>
## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-------------------|
| P8-03 | Information disclosure | Mine 关于文案 | Low | 仅本机+SQLite；不承诺云导出 |
| P8-04 | Information disclosure | 单测日志 | Low | 固定 `new Date(y,m,d)`，无用户目录 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-tesla-v2-1/08-CONTEXT.md
@.planning/phases/08-tesla-v2-1/08-UI-SPEC.md
@src/utils/billTimeRange.ts
@src/db/billRepo.ts
@src/db/billRepo.ts
@src/screens/MineScreen.tsx
@src/screens/CalendarScreen.tsx
@src/budget/monthExpense.test.ts
</context>

<tasks>

<task type="tdd">
  <name>billTimeRange 单测</name>
  <files>src/utils/billTimeRange.test.ts</files>
  <read_first>src/utils/billTimeRange.ts, src/budget/monthExpense.test.ts</read_first>
  <action>
    新建 `src/utils/billTimeRange.test.ts`（`vitest` 与现仓库一致）：
    1) 用固定日 `new Date(2024, 5, 15, 12, 0, 0)` 断言 `getLocalDayStartSec` 对应该日 0:00 本地秒，且 `getLocalDayEndExclusiveSec` 为 **2024-06-16 0:00** 的秒值。
    2) 对 `getRangeFromInclusiveStartEndDay(new Date(2024, 5, 10), new Date(2024, 5, 12))` 断言（**月份 0-based**：5=六月）`endSecExclusive - startSec === 3 * 86400`，且对调开始/结束（如 `new Date(2024,5,12)` 与 `new Date(2024,5,10)` 反序调用）应 **throw**。
  </action>
  <verify>npm run test -- --run src/utils/billTimeRange.test.ts 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - 文件 `src/utils/billTimeRange.test.ts` 存在
    - `npm run test -- --run src/utils/billTimeRange.test.ts` 退出码 0
    - 文件中包含 `getRangeFromInclusiveStartEndDay` 或从 `billTimeRange` 的具名 import
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Mine 屏扩展 V21-03</name>
  <files>src/screens/MineScreen.tsx</files>
  <read_first>src/screens/MineScreen.tsx, .planning/phases/08-tesla-v2-1/08-UI-SPEC.md, src/theme/colors.ts, src/theme/layout.ts</read_first>
  <action>
    扩展 `MineScreen`：
    1) 保留/强化顶栏 `我的` 与 `colors.main` 风格。
    2) 至少 **两个** 独立 `Pressable` 块：**关于**、**数据与存储**；第三块 **设置** 可为占位，Pressable 内说明「后续」或 `尚未开放` 类中文字串。
    3) **数据与存储** 正文须含子串 `SQLite` 与 `本机`（或 `仅本地` 二选一+另一个同义可 grep）。
    4) 不得出现「已支持导出」除非代码路径真实实现（本计划不实现导出，故**不得**写）。
  </action>
  <verify>grep -E '关于|数据|设置|SQLite|本机' src/screens/MineScreen.tsx | head -20</verify>
  <acceptance_criteria>
    - 至少 3 个 `Pressable` 或 2+带 `onPress` 的区块
    - 文件内同时含 `SQLite` 与 `本机` 或 `仅本地`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>08-VERIFICATION 落盘</name>
  <files>.planning/phases/08-tesla-v2-1/08-VERIFICATION.md</files>
  <read_first>.planning/phases/08-tesla-v2-1/08-CONTEXT.md, .planning/ROADMAP.md</read_first>
  <action>
    新建 `08-VERIFICATION.md`：
    1) 标题 `# Phase 8 — Verification / Consistency` 与日期 `**Date:** 2026-04-22`。
    2) 节 `## Consistency matrix`：Markdown 表，**列名固定** 含 `Surface` | `Query condition` | `Expected source` | `Pass/Fail` | `Notes`；至少 3 行，分别写 **Home 月**、**Calendar 单日**、**BillQuery 单日=某日** 在「同一生理日+同一全量流水」下如何核对总额（**不要求** 填真实数字，写方法：如「月内加总 vs 子日」的交叉方式）。
    3) 节 `## Manual UAT`：`npm run verify` 与「设备上 查账 | 记一笔 | 回明细」的简要列表（checkbox markdown）。
  </action>
  <verify>test -f .planning/phases/08-tesla-v2-1/08-VERIFICATION.md && grep -q "## Consistency matrix" .planning/phases/08-tesla-v2-1/08-VERIFICATION.md</verify>
  <acceptance_criteria>
    - 路径存在且含子串 `## Consistency matrix`
    - 表内至少出现子串 `BillQuery` 与 `Calendar` 与 `Home` 或 `明细`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>ROADMAP/STATE/verify 收口 V21Q-01</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>
    1) 更新 `.planning/ROADMAP.md` Phase 8：将原 `_待 plan-phase_` 行改为 `- [ ] 08-01: billTimeRange + BillQuery + 入口` 与 `- [ ] 08-02: 单测+我的+VERIFICATION+ROAD/STATE`；Progress 表 Phase 8 的 **Plans Complete** 写 `2/2` 表示**计划文件**已齐，**实现** 以 `execute` 后勾为准。
    2) 更新 `.planning/STATE.md`：`status` 设为 `ready_to_execute`；正文注明 Phase 8 已有 2 份 `PLAN.md`、待 `/gsd-execute-phase 8`。
    3) 在**本仓库已含计划变更**的提交前运行 `npm run verify`；若本任务仅改 `.planning`+文档且未改 `src/`，应仍通过。
  </action>
  <verify>grep -q "08-01" .planning/ROADMAP.md && grep -q "08-02" .planning/ROADMAP.md && grep -q "Phase 8" .planning/STATE.md && npm run verify</verify>
  <acceptance_criteria>
    - `ROADMAP.md` Phase 8 段落含子串 `08-01` 与 `08-02`
    - `STATE.md` 含 `Phase 8` 或 `v2.1`
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>
