---
phase: 08-tesla-v2-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/BillQueryScreen.tsx
  - src/utils/billTimeRange.ts
autonomous: true
requirements:
  - V21-01
  - V21-02
must_haves:
  truths:
    - "`BillQuery` 在 HomeStack 注册且从 `HomeMain` 顶栏可进入；标题与 `08-UI-SPEC` 一致为「账单」"
    - "单日/区间查询均通过 `queryBillsInRange` 或 `queryBillsForCalendarDay`（后者与前者语义等价）与 `08-CONTEXT` 时间语义一致"
  artifacts:
    - src/utils/billTimeRange.ts
    - src/screens/BillQueryScreen.tsx
  key_links:
    - "HomeStackParamList 含 `BillQuery`"
---

<objective>
实现 V21-01 与 V21-02 的**代码交付**：`billTime` 日界与区间纯函数、`BillQuery` 屏、导航类型与 `RootNavigator` 注册、`HomeMain` 顶栏**查账**入口。不复审文档（`08-VERIFICATION` 在 08-02）。
</objective>

<threat_model>
## Trust boundaries
- 仅本地 SQLite 与本机 UI；不新增网络、不收集 PII 上传。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-------------------|
| P8-01 | Tampering | 本地日期被恶意改？ | N/A 客户端 | 使用设备时区/本地日界与现 `billRepo` 一致；不另信任外部时钟 API |
| P8-02 | Information disclosure | 截屏/录屏 | Low | 不在 UI 打印完整 DB 路径；路径仅在 08-02 我的页控制文案 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-tesla-v2-1/08-CONTEXT.md
@.planning/phases/08-tesla-v2-1/08-UI-SPEC.md
@.planning/phases/08-tesla-v2-1/08-REVIEWS.md
@src/db/billRepo.ts
@src/screens/HomeScreen.tsx
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
</context>

<tasks>

<task type="execute">
  <name>billTimeRange 纯函数</name>
  <files>src/utils/billTimeRange.ts</files>
  <read_first>src/db/billRepo.ts, .planning/phases/08-tesla-v2-1/08-CONTEXT.md</read_first>
  <action>
    新建 `src/utils/billTimeRange.ts`，导出（命名可微调用下划线一致即可，但**必须** export 这些语义）：
    1) 输入 `Date`（仅使用本地年月日）计算 **该自然日 0:00:00** 的 UNIX **秒** `getLocalDayStartSec(d: Date): number`。
    2) 计算 **下一自然日 0:00:00** 的 UNIX 秒 作为 `endSecExclusive`（`getLocalDayEndExclusiveSec` 或等价名）。
    3) 导出 `getRangeFromInclusiveStartEndDay(startInclusive: Date, endInclusive: Date): { startSec: number; endSecExclusive: number }`，满足半开区间 `[startSec, endSecExclusive)`，且当 `endInclusive` 的日历日 &lt; `startInclusive` 时**抛** `Error` 且 message 同时包含子串 `开始` 与 `结束`（或英文 `start` and `end` 二选一应中文优先）。
    4) 文件顶注释 2 行内说明 `billTime` 与 `bill_list` 一致为 **秒**、区间为 **半开**。
  </action>
  <verify>test -f src/utils/billTimeRange.ts && grep -q "endSecExclusive" src/utils/billTimeRange.ts</verify>
  <acceptance_criteria>
    - `src/utils/billTimeRange.ts` 存在
    - `rg "getLocalDayStartSec|getRangeFromInclusiveStartEndDay" src/utils/billTimeRange.ts` 至少 1 行
    - `rg "开始" src/utils/billTimeRange.ts` 在抛错相关逻辑附近存在，或与 Error 同文件内可见
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BillQueryScreen + 导航</name>
  <files>src/screens/BillQueryScreen.tsx, src/navigation/types.ts, src/navigation/RootNavigator.tsx</files>
  <read_first>src/navigation/types.ts, src/navigation/RootNavigator.tsx, src/screens/BillDetailScreen.tsx, src/screens/HomeScreen.tsx, .planning/phases/08-tesla-v2-1/08-UI-SPEC.md</read_first>
  <action>
    1) 在 `HomeStackParamList` 增加 `BillQuery: undefined`（仅该键，类型名一致）。
    2) 在 `RootNavigator` 的 `HomeStackNavigator` 内 `HomeStack.Screen` 注册 `name="BillQuery"`，component=新组件，`options={{ title: "账单" }}`（**逐字**「账单」）。
    3) 新建 `src/screens/BillQueryScreen.tsx`：
    - 顶部 **「单日 / 区间」** 二选一（`Segmented` 可用两 `Pressable`+样式模拟；无需原生 Segmented 库）。
    - **单日** 模式：默认用 **今日** 的 `Date` 作为初始选中；用 `queryBillsForCalendarDay(anchor)` 拉列表（或等价 `queryBillsInRange`）；列表行可 `FlatList`+简单行（金额+时间+`CategoryIcon`），`onPress` → `navigation.navigate("BillDetail", { billId })`。
    - **区间** 模式：两个日期选择（`@react-native-community/datetimepicker` 与现工程一致）。两日期均选定后，用 `getRangeFromInclusiveStartEndDay` + `queryBillsInRange`；**未**选全时展示中文提示，**至少一行** 含子串 `日期` 或 `区间`；若结束&lt;开始则利用抛错在 UI 以 `Text` 显示 error.message。
    - `useBillsRefresh` 与 `useFocusEffect` 在返回后刷新与 `HomeScreen` 同模式（依赖 `generation` 重新 `query`）。
  </action>
  <verify>grep -q 'BillQuery' src/navigation/types.ts && grep -q 'BillQuery' src/navigation/RootNavigator.tsx && test -f src/screens/BillQueryScreen.tsx</verify>
  <acceptance_criteria>
    - `src/navigation/types.ts` 含精确 `BillQuery:`
    - `RootNavigator.tsx` 含 `name="BillQuery"` 与 `BillQueryScreen`
    - `BillQueryScreen.tsx` 含子串 `queryBillsForCalendarDay` 或 `queryBillsInRange`（至少一）
  </acceptance_criteria>
</task>

<task type="execute">
  <name>HomeMain 查账入口</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>src/screens/HomeScreen.tsx, .planning/phases/08-tesla-v2-1/08-UI-SPEC.md</read_first>
  <action>
    在 `useLayoutEffect` 的 `setOptions` 中，将 `headerRight` 从单一 `Pressable` 改为**用 View 容器（`flexDirection: "row"`, `alignItems: "center"`）包一层** 后再放**两个** `Pressable` 子节点：
    1) **左侧** `Pressable`：`accessibilityLabel` **必须**含 `查账` 或 `账单` 之一；`onPress` → `navigation.navigate("BillQuery")`；图标 `MaterialCommunityIcons` `name="filter-variant"`（若不存在则用 `tune` 但须在 PR 说明中改回 filter-variant 或保持 tune 并更新 UI-SPEC 一处）。
    2) **右侧** 保持记一笔，逻辑与现 `CreateBill` 一致。
  </action>
  <verify>grep -q "BillQuery" src/screens/HomeScreen.tsx && grep -q "navigate" src/screens/HomeScreen.tsx && grep -E "查账|账单" src/screens/HomeScreen.tsx</verify>
  <acceptance_criteria>
    - `src/screens/HomeScreen.tsx` 含 `navigate("BillQuery"` 或等价的 `navigate('BillQuery'`
    - 存在 `headerRight` 且其中使用 **View 行内 flexDirection row** 或等价布局字符串 `flexDirection: "row"`
  </acceptance_criteria>
</task>

</tasks>
