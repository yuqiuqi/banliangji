---
phase: 01-data-layer-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-data-layer-baseline/01-01-AUDIT.md
  - src/db/database.ts
  - src/db/billRepo.ts
  - src/types/models.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-03
user_setup: []
must_haves:
  truths:
    - "01-01-AUDIT.md 包含 bill_list 每个列与 Bill/BillRow 字段的对照表（含 nullability 列；历史审计列名可标为 legacy，不作为产品对外叙事）"
    - "01-01-AUDIT.md 写明 queryBillsInRange 使用 billTime >= start 且 billTime < endExclusive（半开区间）"
    - "01-01-AUDIT.md 含 ## amount 存储语义 与 ## 日界与时区 两小节（回应 01-REVIEWS.md / Gemini）"
  artifacts:
    - .planning/phases/01-data-layer-baseline/01-01-AUDIT.md
  key_links:
    - "database.ts 中 CREATE TABLE bill_list 列集合与 AUDIT 表一致（若发生修正则以 DDL 为准）"
---

<objective>
对照本仓库 `database.ts` DDL、`billRepo.ts` 与 `models.ts`，完成可复查的字段与查询语义审查；若发现偏差则在本仓库修正并保证 typecheck/lint 通过。
Purpose: 满足 DATA-01（字段语义一致）与 DATA-03（区间查询能力可解释、可实现）。
Output: `01-01-AUDIT.md`；必要时对 `src/db/` 与 `src/types/models.ts` 的补丁。

**Replan:** 已合并 `.planning/phases/01-data-layer-baseline/01-REVIEWS.md` 建议：列级 **nullability** 对照、`amount` TEXT 存储语义、**本地日历日界**（相对 UTC）对 `queryBillsForMonth` / `queryBillsForCalendarDay` 的一句话结论。
</objective>

<threat_model>
## Trust boundaries
- 数据仅驻留在本机应用沙箱内 SQLite `main.db`；本计划不引入网络同步。

## STRIDE（ASVS L1 视角）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P1-T01 | Tampering | bill_list 行数据 | Low / accepted | 依赖 OS 沙箱；无多租户 |
| P1-T02 | Information disclosure | main.db 文件 | Low | 审查文档不写入密钥；备份语义在 Plan 01-02 中向用户说明 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-layer-baseline/01-CONTEXT.md
@.planning/phases/01-data-layer-baseline/01-PATTERNS.md
@src/db/database.ts
@src/db/billRepo.ts
@src/types/models.ts
</context>

<tasks>

<task type="auto">
  <name>撰写 01-01-AUDIT.md（列级与查询语义）</name>
  <files>.planning/phases/01-data-layer-baseline/01-01-AUDIT.md</files>
  <read_first>
    .planning/phases/01-data-layer-baseline/01-CONTEXT.md,
    src/db/database.ts,
    src/db/billRepo.ts,
    src/types/models.ts
  </read_first>
  <action>
    在 `.planning/phases/01-data-layer-baseline/01-01-AUDIT.md` 新建 Markdown，必须包含：
    1) 表名 `bill_list`、库文件名 `main.db` 与半两记工程约定一致性的单行结论。
    2) 表格列：`sql_column`、`legacy_property`（可选，仅内部审计）、`ts_field`、`nullability`、`notes`。`nullability` 列对每一行写明 SQL NULL 允许性 / TS `| null` 是否一致（可用简短词：如「一致」「RN 更宽」）。行集合至少覆盖：id, type, categoryId, icon, name, remark, amount, createTime, updateTime, billTime（与 `database.ts` 中 DDL 顺序可不同但不得漏列）。
    3) 明确写出：`type` 在 SQL 为 INTEGER，在 RN 侧归一为 `1|2`，`rowToBill` 将非 2 归为 1。
    4) 明确写出：三个时间字段均为 Unix 秒（SQL REAL / RN `number | null` 语义对齐）。
    5) 引用 `billRepo.ts` 中 `queryBillsInRange` 的 SQL 文本或行号，说明谓词为 `billTime &gt;= ? AND billTime &lt; ?`（半开区间）。
    6) 小节 `## Required actions`：若无偏差写单独一行 `None`；若有偏差逐条列出需在任务 2 修改的文件与具体变更要点（例如列类型、NULL 语义、SQL 条件）。
    7) 小节 `## amount 存储语义`：写明 SQL 为 TEXT、RN `Bill.amount` 为 `string | null`；用一句话说明金额解析/运算在业务层（非本小节范围）完成即可。
    8) 小节 `## 日界与时区`：一句结论——`queryBillsForMonth` 与 `queryBillsForCalendarDay` 使用 `new Date(y, m, d)` **本地时区**构造日起点，故 `billTime` 的日/月边界按**设备本地日历**解释（非强制 UTC 日界）；并指向 `billRepo.ts` 中相关行号。
  </action>
  <verify>test -f .planning/phases/01-data-layer-baseline/01-01-AUDIT.md && grep -q "bill_list" .planning/phases/01-data-layer-baseline/01-01-AUDIT.md && grep -q "queryBillsInRange" .planning/phases/01-data-layer-baseline/01-01-AUDIT.md && grep -q "## Required actions" .planning/phases/01-data-layer-baseline/01-01-AUDIT.md && grep -q "nullability" .planning/phases/01-data-layer-baseline/01-01-AUDIT.md && grep -q "## amount 存储语义" .planning/phases/01-data-layer-baseline/01-01-AUDIT.md && grep -q "## 日界与时区" .planning/phases/01-data-layer-baseline/01-01-AUDIT.md</verify>
  <acceptance_criteria>
    - 文件 `.planning/phases/01-data-layer-baseline/01-01-AUDIT.md` 存在且包含字符串 `bill_list`
    - 同文件包含字符串 `queryBillsInRange` 与 `## Required actions`
    - 同文件包含子串 `categoryId` 与子串 `billTime`
    - 同文件包含 `nullability`、`## amount 存储语义`、`## 日界与时区`
  </acceptance_criteria>
  <done>AUDIT 文件满足 acceptance_criteria，且表格覆盖全部 DDL 列。</done>
</task>

<task type="auto">
  <name>按 AUDIT 修正代码并跑 typecheck/lint</name>
  <files>src/db/database.ts, src/db/billRepo.ts, src/types/models.ts</files>
  <read_first>
    .planning/phases/01-data-layer-baseline/01-01-AUDIT.md,
    src/db/database.ts,
    src/db/billRepo.ts,
    src/types/models.ts
  </read_first>
  <action>
    阅读 `01-01-AUDIT.md` 的 `## Required actions`：
    - 若为 `None`：不得为「凑 diff」修改业务代码；仅确认与 AUDIT 结论一致。
    - 若非 `None`：逐条实现所列修正，使 DDL / SQL / TS 类型与 AUDIT 结论一致。
    然后在仓库根执行：`npm run typecheck` 与 `npm run lint`，两者退出码均为 0。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run typecheck && npm run lint</verify>
  <acceptance_criteria>
    - 命令 `npm run typecheck` 在 半两记 根目录退出码 0
    - 命令 `npm run lint` 在 半两记 根目录退出码 0
    - `01-01-AUDIT.md` 中 `## Required actions` 在代码变更后仍为真实状态（若曾有问题，修正后应更新为 `None` 或注明已修复）
  </acceptance_criteria>
  <done>typecheck/lint 全绿；AUDIT 与代码无未关闭矛盾。</done>
</task>

</tasks>

<verification>
- [ ] `01-01-AUDIT.md` 已创建且含列对照与半开区间说明
- [ ] `npm run typecheck` 通过
- [ ] `npm run lint` 通过
</verification>

<success_criteria>
- 审查产物可追溯；DATA-01 / DATA-03 在本计划范围内有明确证据
- 未引入 `any` 或新的 lint 错误
</success_criteria>

<output>
完成后创建 `.planning/phases/01-data-layer-baseline/01-01-SUMMARY.md`
</output>
