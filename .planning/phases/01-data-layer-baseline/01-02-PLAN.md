---
phase: 01-data-layer-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DATA-02
user_setup: []
must_haves:
  truths:
    - "PROJECT.md 含「数据驻留、备份与手工冒烟」小节且声明数据仅存本机、库名为 main.db"
    - "PROJECT.md 含可执行的杀进程再启动冒烟步骤列表（至少 4 步）"
    - "PROJECT.md 含开发者调试定位 main.db 的短说明（Expo SQLite / 应用沙箱，无 PII）"
  artifacts:
    - .planning/PROJECT.md
  key_links:
    - "冒烟步骤与 DATA-02（杀进程后数据仍在）可逐条对应"
---

<objective>
在 `PROJECT.md` 中补充开发者/贡献者可读的**数据驻留位置**、**备份含义**与**手工冒烟**步骤，支撑 `DATA-02` 的人工验证与路线图成功标准第 2、3 条。
Purpose: 让用户与评审理解「数据在本机」；提供可重复的杀进程验证路径。
Output: 更新后的 `.planning/PROJECT.md`（新小节，不删除既有 Core Value / Constraints）。

**Replan:** 已合并 `01-REVIEWS.md`：在同小节内增加**开发者调试**短段落，提示在**应用沙箱**内查找 `main.db`（Expo SQLite），并指向 Xcode / Android Studio 等官方工具，**禁止**写入真实 UDID、用户姓名或可复制家用路径。
</objective>

<threat_model>
## Trust boundaries
- 文档仅描述本地 SQLite 行为；不包含第三方服务端凭证。

## STRIDE
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P1-T03 | Information disclosure | PROJECT.md 读者 | Low | 不写具体设备路径中的用户 PII；仅说明沙箱与 `main.db` 名称 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-layer-baseline/01-CONTEXT.md
@.planning/phases/01-data-layer-baseline/01-01-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>更新 PROJECT.md：数据驻留、备份、冒烟步骤</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    .planning/PROJECT.md,
    .planning/phases/01-data-layer-baseline/01-CONTEXT.md,
    .planning/phases/01-data-layer-baseline/01-01-AUDIT.md
  </read_first>
  <action>
    在 `.planning/PROJECT.md` 的 `## Constraints` 之后（或 `## Context` 之前）插入新二级标题，**标题原文必须为**：`## 数据驻留、备份与手工冒烟`。
    小节正文必须包含以下**可 grep 的短语文本**（字面相等）：
    - `数据仅存本机 SQLite`
    - `库文件名为 main.db`
    - `表名为 bill_list`
    - `无云同步`
    编号列表 **1. 至 4.**（每行以数字、点和空格开头），依次描述：干净安装或已有数据下打开应用；使用「记一笔」或等价入口插入一条可识别金额/备注的测试账单；完全关闭应用进程（非仅退后台）；重新冷启动后仍在明细或同数据源中可见该账单。第 4 步文案中须含子串 `杀进程` 或 `强制关闭` 之一。
    另起一段说明：用户级「备份」当前依赖设备系统备份与应用数据策略；v1 不提供应用内导出（与 PROJECT Out of Scope 一致时可写「应用内导出见 v2」或指向 Out of Scope）。
    在同小节末尾再增加一段，**段落标题**为三级标题：`### 开发者调试（沙箱内查找 main.db）`。正文必须同时包含字面字符串：`Expo SQLite`、`应用沙箱`、`main.db`；并同时提及 `Xcode` 与 `Android Studio`（可各用一句话说明 iOS/Android 侧如何打开应用容器/设备文件视图），**不得**写入真实 UDID、用户姓名、家庭住址或可唯一识别个人的路径。
  </action>
  <verify>grep -n "数据驻留、备份与手工冒烟" .planning/PROJECT.md && grep -n "数据仅存本机 SQLite" .planning/PROJECT.md && grep -n "库文件名为 main.db" .planning/PROJECT.md && grep -n "### 开发者调试（沙箱内查找 main.db）" .planning/PROJECT.md && grep -n "Expo SQLite" .planning/PROJECT.md && grep -n "应用沙箱" .planning/PROJECT.md && grep -n "Xcode" .planning/PROJECT.md && grep -n "Android Studio" .planning/PROJECT.md</verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` 包含二级标题行 `## 数据驻留、备份与手工冒烟`
    - 同文件包含字面字符串 `数据仅存本机 SQLite`、`库文件名为 main.db`、`表名为 bill_list`、`无云同步`
    - 同文件存在以 `1. `、`2. `、`3. `、`4. ` 开头的四行（冒烟步骤）
    - 同文件包含三级标题 `### 开发者调试（沙箱内查找 main.db）` 及字面字符串 `Expo SQLite`、`应用沙箱`
    - 同文件须同时包含子串 `Xcode` 与子串 `Android Studio`（分别覆盖 iOS/Android 调试入口；若合并为一行亦可）
  </acceptance_criteria>
  <done>PROJECT.md 满足 acceptance_criteria 且读起来与 CONTEXT D-01/D-06 无矛盾。</done>
</task>

<task type="auto">
  <name>回归 typecheck 与 lint</name>
  <files>（无代码变更预期）</files>
  <read_first>.planning/PROJECT.md</read_first>
  <action>
    在 SwiftCostRN 仓库根执行：`npm run typecheck` 与 `npm run lint`，退出码均为 0。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/swiftcost-rn/SwiftCostRN && npm run typecheck && npm run lint</verify>
  <acceptance_criteria>
    - `npm run typecheck` 退出码 0
    - `npm run lint` 退出码 0
  </acceptance_criteria>
  <done>静态检查全绿。</done>
</task>

</tasks>

<verification>
- [ ] `PROJECT.md` 新小节可被 grep 检索
- [ ] `npm run typecheck` / `npm run lint` 通过
</verification>

<success_criteria>
- DATA-02 有明确人工验证路径描述
- 文档与 Phase 1 Success Criteria 对齐
</success_criteria>

<output>
完成后创建 `.planning/phases/01-data-layer-baseline/01-02-SUMMARY.md`
</output>
