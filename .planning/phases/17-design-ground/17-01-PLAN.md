---
phase: 17
plan: 17-01
wave: 1
depends_on: []
autonomous: true
requirements:
  - DESIGN-GROUND-01
files_modified:
  - package.json
  - package-lock.json
  - src/theme/motion.ts
  - src/hooks/useReduceMotion.ts
  - src/utils/haptics.ts
  - src/utils/haptics.test.ts
  - src/components/SpringPressable.tsx
  - .planning/phases/17-design-ground/17-VERIFICATION.md
objective: "Wave 1 — 建立 motion/haptics/SpringPressable 基础资产，让 Phase 18 页面改造有成熟 import"
---

# Plan 17-01: Motion / Haptics / SpringPressable 基础资产

## 目的

按 v1.2 §18.2 清单建立 **5 件基础资产**：
1. 重装 `expo-haptics` 依赖
2. 新建 `src/theme/motion.ts`
3. 新建 `src/hooks/useReduceMotion.ts`
4. 新建 `src/utils/haptics.ts` + vitest 测试
5. 新建 `src/components/SpringPressable.tsx`

完成后 Phase 18+ 页面改造有 import。

## must_haves

- `expo-haptics` 安装成功且与 Expo SDK 54 兼容
- `SPRING` / `REDUCE_SPRING` / `FADE_MS` 可从 `src/theme/motion.ts` 导出
- `useReduceMotion()` 可在 RN 组件中调用，响应系统设置变更
- `haptic.select/success/warning/error/light/medium` 全部可用且单测通过
- `SpringPressable` 支持 `PressableProps` 透传 + spring + haptic 可选
- `npm run verify` 绿（tsc + eslint + vitest 全部通过）

---

## Task 17-01-1 — 重装 expo-haptics 依赖

<read_first>
- `package.json` — 确认 Expo / RN 版本
- `.planning/IOS26-DESIGN-GUIDE.md` §13（haptics 场景映射）
</read_first>

<action>
在仓库根执行：

```bash
npx expo install expo-haptics
```

Expo CLI 会：
1. 查询 Expo SDK 54 对应的 `expo-haptics` 版本（预期 `~15.0.8`）
2. 自动写入 `package.json` `dependencies` 节
3. 更新 `package-lock.json`

**严禁** 手改 `package.json` 版本号，交给 Expo CLI 解析。
</action>

<acceptance_criteria>
- `grep -n "\"expo-haptics\"" package.json` 返回非空
- `grep -A 1 "\"expo-haptics\"" package-lock.json | head -3` 显示版本号，形如 `"version": "15.x.x"`
- `npm ls expo-haptics 2>&1 | grep "expo-haptics@"` 显示单一版本（无 UNMET DEPENDENCY 警告）
</acceptance_criteria>

---

## Task 17-01-2 — 新建 `src/theme/motion.ts`

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §11.1 全局弹簧表（含具体数值）
- `src/theme/layout.ts`（参考导出风格）
</read_first>

<action>
创建 `src/theme/motion.ts`，内容：

```ts
/**
 * iOS 26 / Liquid Glass 动效 token — 对齐 `.planning/IOS26-DESIGN-GUIDE.md` §11.1。
 * 禁止在业务组件中使用自定义弹簧参数；一律从本文件导入。
 */

export const SPRING = {
  /** 主弹簧：按钮、分段指示器、列表行 */
  UI: { damping: 15, stiffness: 220, mass: 1 },
  /** 轻盈弹簧：chip、徽章、小标签（更活泼） */
  THUMB: { damping: 13, stiffness: 260, mass: 0.9 },
  /** 厚重弹簧：sheet 底栏、大浮层（更稳） */
  SHEET: { damping: 20, stiffness: 200, mass: 1.1 },
  /** 极快弹簧：手势跟随/复位（接近 interactiveSpring） */
  GESTURE: { damping: 12, stiffness: 300, mass: 1 },
} as const;

/** Reduce Motion 开启时的降级 spring —— 阻尼极大，几乎线性无弹跳 */
export const REDUCE_SPRING = { damping: 50, stiffness: 400, mass: 1 } as const;

/** 辅助 timing 时长（ms），用于 opacity / scrim 等非物理过渡 */
export const FADE_MS = {
  fast: 120,
  normal: 200,
  slow: 350,
} as const;

export type SpringPreset = keyof typeof SPRING;
```
</action>

<acceptance_criteria>
- `test -f src/theme/motion.ts` 返回 0
- `grep -n "export const SPRING" src/theme/motion.ts` 返回非空
- `grep -n "export const REDUCE_SPRING" src/theme/motion.ts` 返回非空
- `grep -n "export const FADE_MS" src/theme/motion.ts` 返回非空
- `grep -n "export type SpringPreset" src/theme/motion.ts` 返回非空
- `npx tsc --noEmit` 通过（类型检查无错误）
</acceptance_criteria>

---

## Task 17-01-3 — 新建 `src/hooks/useReduceMotion.ts`

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §11.9 Reduce Motion 降级（全局）
- `src/hooks/useReduceTransparency.ts` —— 参考风格
</read_first>

<action>
创建 `src/hooks/useReduceMotion.ts`，内容：

```ts
/**
 * 监听系统「减少动画」开关（iOS 设置 → 辅助功能 → 动画）。
 * 对齐 `.planning/IOS26-DESIGN-GUIDE.md` §11.9。
 */
import { useEffect, useState } from "react";
import { AccessibilityInfo } from "react-native";

export function useReduceMotion(): boolean {
  const [reduce, setReduce] = useState(false);

  useEffect(() => {
    let active = true;
    AccessibilityInfo.isReduceMotionEnabled().then((v) => {
      if (active) setReduce(v);
    });
    const sub = AccessibilityInfo.addEventListener(
      "reduceMotionChanged",
      setReduce,
    );
    return () => {
      active = false;
      sub.remove();
    };
  }, []);

  return reduce;
}
```
</action>

<acceptance_criteria>
- `test -f src/hooks/useReduceMotion.ts` 返回 0
- `grep -n "export function useReduceMotion" src/hooks/useReduceMotion.ts` 返回非空
- `grep -n "AccessibilityInfo.addEventListener" src/hooks/useReduceMotion.ts` 返回非空
- `grep -n "reduceMotionChanged" src/hooks/useReduceMotion.ts` 返回非空
- `npx tsc --noEmit` 通过
</acceptance_criteria>

---

## Task 17-01-4 — 新建 `src/utils/haptics.ts`

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §13.2 全仓库场景映射表
- `https://docs.expo.dev/versions/latest/sdk/haptics/`
</read_first>

<action>
创建 `src/utils/haptics.ts`，内容：

```ts
/**
 * 触感反馈 wrapper —— 对齐 `.planning/IOS26-DESIGN-GUIDE.md` §13。
 * 所有调用都 try/catch，系统无触感硬件时静默失败。
 */
import * as Haptics from "expo-haptics";

async function safe<T>(fn: () => Promise<T>): Promise<void> {
  try {
    await fn();
  } catch {
    /* 系统无触觉或用户关闭触觉：静默失败 */
  }
}

export const haptic = {
  /** 选择器 tick、分段切换 */
  select: () => safe(() => Haptics.selectionAsync()),

  /** 成功：记一笔保存、预算设置保存 */
  success: () =>
    safe(() => Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)),

  /** 警告：保留给将来的 blocking 操作（当前本仓库不使用） */
  warning: () =>
    safe(() => Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning)),

  /** 错误：校验失败、删除确认前 */
  error: () =>
    safe(() => Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error)),

  /** 轻触：列表按压、FAB 呼出、Modal 出现 */
  light: () =>
    safe(() => Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)),

  /** 中触：长按进入编辑、FAB 按下 */
  medium: () =>
    safe(() => Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)),
} as const;

export type HapticKind = keyof typeof haptic;
```
</action>

<acceptance_criteria>
- `test -f src/utils/haptics.ts` 返回 0
- `grep -n "export const haptic" src/utils/haptics.ts` 返回非空
- `grep -cE "select|success|warning|error|light|medium" src/utils/haptics.ts` ≥ 6（每个 API 至少 1 处）
- `grep -n "safe" src/utils/haptics.ts` 返回非空（try/catch wrapper）
- `grep -n "export type HapticKind" src/utils/haptics.ts` 返回非空
- `npx tsc --noEmit` 通过
</acceptance_criteria>

---

## Task 17-01-5 — 新建 `src/utils/haptics.test.ts`

<read_first>
- `src/utils/haptics.ts`（上一任务产物）
- `src/budget/monthExpense.test.ts` —— 参考 vitest 风格
</read_first>

<action>
创建 `src/utils/haptics.test.ts`，内容：

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

// 必须在 import haptics 之前 mock
vi.mock("expo-haptics", () => ({
  selectionAsync: vi.fn(() => Promise.resolve()),
  notificationAsync: vi.fn(() => Promise.resolve()),
  impactAsync: vi.fn(() => Promise.resolve()),
  NotificationFeedbackType: { Success: "success", Warning: "warning", Error: "error" },
  ImpactFeedbackStyle: { Light: "light", Medium: "medium", Heavy: "heavy" },
}));

import * as Haptics from "expo-haptics";
import { haptic } from "./haptics";

const selectionAsync = Haptics.selectionAsync as ReturnType<typeof vi.fn>;
const notificationAsync = Haptics.notificationAsync as ReturnType<typeof vi.fn>;
const impactAsync = Haptics.impactAsync as ReturnType<typeof vi.fn>;

describe("haptic wrappers", () => {
  beforeEach(() => {
    selectionAsync.mockClear();
    notificationAsync.mockClear();
    impactAsync.mockClear();
  });

  it("select() calls selectionAsync once", async () => {
    await haptic.select();
    expect(selectionAsync).toHaveBeenCalledTimes(1);
  });

  it("success() calls notificationAsync with Success", async () => {
    await haptic.success();
    expect(notificationAsync).toHaveBeenCalledWith("success");
  });

  it("warning() calls notificationAsync with Warning", async () => {
    await haptic.warning();
    expect(notificationAsync).toHaveBeenCalledWith("warning");
  });

  it("error() calls notificationAsync with Error", async () => {
    await haptic.error();
    expect(notificationAsync).toHaveBeenCalledWith("error");
  });

  it("light() calls impactAsync with Light", async () => {
    await haptic.light();
    expect(impactAsync).toHaveBeenCalledWith("light");
  });

  it("medium() calls impactAsync with Medium", async () => {
    await haptic.medium();
    expect(impactAsync).toHaveBeenCalledWith("medium");
  });

  it("swallows errors silently when haptics unavailable", async () => {
    selectionAsync.mockRejectedValueOnce(new Error("no haptics"));
    await expect(haptic.select()).resolves.toBeUndefined();
  });
});
```
</action>

<acceptance_criteria>
- `test -f src/utils/haptics.test.ts` 返回 0
- `npm test -- src/utils/haptics.test.ts 2>&1 | grep -E "haptic|Tests"` 显示 7 个测试全部通过
- 其他已有测试（`chart` / `budget` / `billTimeRange`）未受影响
</acceptance_criteria>

---

## Task 17-01-6 — 新建 `src/components/SpringPressable.tsx`

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §11.3 按压弹簧 + §13 触感场景映射
- `src/theme/motion.ts`（本 Wave 已产）
- `src/hooks/useReduceMotion.ts`（本 Wave 已产）
- `src/utils/haptics.ts`（本 Wave 已产）
- `src/components/ios/Fab.tsx` —— 参考现有 Pressable 透传风格
</read_first>

<action>
创建 `src/components/SpringPressable.tsx`，内容：

```tsx
/**
 * 对齐 `.planning/IOS26-DESIGN-GUIDE.md` §11.3：
 * - 按下：scale → 0.97 + opacity → 0.92（SPRING.UI 驱动）
 * - 松手：scale → 1 + opacity → 1（弹回）
 * - `onPressIn` 同帧触发 haptic（默认 light；可配 medium / select / 关闭）
 * - `useReduceMotion` = true 时降级：短 timing 替代 spring，触觉保留
 *
 * 仅作为基础 wrapper。Phase 18+ 业务组件引用此处替换裸 Pressable。
 */
import React, { useMemo } from "react";
import { Pressable, type PressableProps, type GestureResponderEvent } from "react-native";
import Animated, {
  useAnimatedStyle,
  useSharedValue,
  withSpring,
  withTiming,
} from "react-native-reanimated";

import { SPRING, REDUCE_SPRING, FADE_MS, type SpringPreset } from "../theme/motion";
import { useReduceMotion } from "../hooks/useReduceMotion";
import { haptic, type HapticKind } from "../utils/haptics";

const AnimatedPressable = Animated.createAnimatedComponent(Pressable);

type HapticTiming = "pressIn" | "press" | false;

export type SpringPressableProps = PressableProps & {
  /** 弹簧预设，默认 UI */
  spring?: SpringPreset;
  /** 触发触觉的时机；false = 不触觉 */
  hapticOn?: HapticTiming;
  /** 触觉类型（默认 light） */
  hapticIntensity?: Extract<HapticKind, "light" | "medium" | "select">;
  /** 按下时目标 scale（默认 0.97） */
  scaleTo?: number;
  /** 按下时目标 opacity（默认 0.92） */
  opacityTo?: number;
};

export const SpringPressable = React.forwardRef<
  React.ComponentRef<typeof Pressable>,
  SpringPressableProps
>(function SpringPressable(
  {
    spring = "UI",
    hapticOn = "pressIn",
    hapticIntensity = "light",
    scaleTo = 0.97,
    opacityTo = 0.92,
    onPressIn,
    onPressOut,
    onPress,
    style,
    children,
    ...rest
  },
  ref,
) {
  const reduce = useReduceMotion();
  const scale = useSharedValue(1);
  const opacity = useSharedValue(1);

  const animatedStyle = useAnimatedStyle(() => ({
    transform: [{ scale: scale.value }],
    opacity: opacity.value,
  }));

  const springConfig = useMemo(
    () => (reduce ? REDUCE_SPRING : SPRING[spring]),
    [reduce, spring],
  );

  const down = () => {
    if (reduce) {
      scale.value = withTiming(scaleTo, { duration: FADE_MS.fast });
      opacity.value = withTiming(opacityTo, { duration: FADE_MS.fast });
      return;
    }
    scale.value = withSpring(scaleTo, springConfig);
    opacity.value = withSpring(opacityTo, springConfig);
  };

  const up = () => {
    if (reduce) {
      scale.value = withTiming(1, { duration: FADE_MS.fast });
      opacity.value = withTiming(1, { duration: FADE_MS.fast });
      return;
    }
    scale.value = withSpring(1, springConfig);
    opacity.value = withSpring(1, springConfig);
  };

  const fire = () => {
    if (!hapticOn) return;
    void haptic[hapticIntensity]();
  };

  const handlePressIn = (e: GestureResponderEvent) => {
    down();
    if (hapticOn === "pressIn") fire();
    onPressIn?.(e);
  };

  const handlePressOut = (e: GestureResponderEvent) => {
    up();
    onPressOut?.(e);
  };

  const handlePress = (e: GestureResponderEvent) => {
    if (hapticOn === "press") fire();
    onPress?.(e);
  };

  return (
    <AnimatedPressable
      ref={ref}
      {...rest}
      onPressIn={handlePressIn}
      onPressOut={handlePressOut}
      onPress={handlePress}
      style={[animatedStyle, style as never]}
    >
      {children as React.ReactNode}
    </AnimatedPressable>
  );
});
```
</action>

<acceptance_criteria>
- `test -f src/components/SpringPressable.tsx` 返回 0
- `grep -n "export const SpringPressable\|export type SpringPressableProps" src/components/SpringPressable.tsx` ≥ 2
- `grep -n "withSpring\|withTiming\|useSharedValue" src/components/SpringPressable.tsx` ≥ 3（Reanimated API 使用）
- `grep -n "haptic\[hapticIntensity\]\|haptic\." src/components/SpringPressable.tsx` ≥ 1（触觉接入）
- `grep -n "useReduceMotion" src/components/SpringPressable.tsx` ≥ 1（reduce motion 降级）
- `npx tsc --noEmit` 通过
- `npx eslint src/components/SpringPressable.tsx` 无 error/warning
</acceptance_criteria>

---

## Task 17-01-7 — `npm run verify` 整体绿 + 产出 `17-VERIFICATION.md`

<read_first>
- 全部上述 6 个任务产物
- `.planning/phases/16-design-guide-v1.2/16-VERIFICATION.md` —— 参考验收格式
</read_first>

<action>
1. 运行 `npm run verify`，期望输出：
   - `tsc --noEmit` → 0 error
   - `eslint . --max-warnings 0` → 0 error
   - `vitest run` → **4 test files**（新增 haptics.test.ts 一档）/ **≥ 16 tests 通过**（原 10 + 新 ≥ 7）

2. 创建 `.planning/phases/17-design-ground/17-VERIFICATION.md`，逐任务勾结构：

```markdown
# Phase 17 Verification — Design Ground

**Date:** 2026-04-24
**Scope:** Wave 1 / Option A — motion/haptics/SpringPressable 5 件基础资产
**Result:** PASS

---

## Task-level 验收

### T1: expo-haptics 重装
- [x] 通过
- 证据：`grep "\"expo-haptics\"" package.json` 返回非空（版本由 `npx expo install` 自动解析）

### T2: `src/theme/motion.ts`
- [x] 通过
- 证据：`SPRING`/`REDUCE_SPRING`/`FADE_MS`/`SpringPreset` 5 导出齐全
- 对齐 v1.2 §11.1

### T3: `src/hooks/useReduceMotion.ts`
- [x] 通过
- 证据：`AccessibilityInfo.isReduceMotionEnabled` + `reduceMotionChanged` listener 双路径
- 对齐 v1.2 §11.9

### T4: `src/utils/haptics.ts`
- [x] 通过
- 证据：6 方法（select/success/warning/error/light/medium）+ safe() try/catch
- 对齐 v1.2 §13

### T5: `src/utils/haptics.test.ts`
- [x] 通过
- 证据：7 tests（含静默失败验证）全部通过

### T6: `src/components/SpringPressable.tsx`
- [x] 通过
- 证据：forwardRef + PressableProps 透传 + spring/haptic/reduce-motion 三路分支

### T7: verify 门禁
- [x] 通过
- 证据：
  - tsc: 0 error
  - eslint: 0 warning/error
  - vitest: 4 test files / ≥ 16 tests passed

---

## v1.2 §18.2 资产清单闭环

| 资产 | v1.2 §18.2 标记 | Phase 17 Wave 1 产出 | 状态 |
|------|-----------------|----------------------|------|
| `src/theme/motion.ts` | 缺失 | ✅ 重建 | Done |
| `src/hooks/useReduceMotion.ts` | 缺失 | ✅ 新建 | Done |
| `src/utils/haptics.ts` | 缺失 | ✅ 新建 + 单测 | Done |
| `src/components/SpringPressable.tsx` | 缺失 | ✅ 新建 | Done |
| `src/components/ios/GlassEffectContainer.tsx` | 缺失 | ❌ 推迟 Phase 18（Option B） | Deferred |
| `src/components/ios/VibrantText.tsx` | 缺失 | ❌ 推迟 | Deferred |
| `src/components/ios/GlassShimmer.tsx` | 缺失 | ❌ 推迟 | Deferred |
| `src/hooks/useTouchRipple.ts` | 缺失 | ❌ 推迟 | Deferred |
| `src/hooks/useGyroHighlight.ts` | 缺失 | ❌ 推迟（RN-T2） | Deferred |
| `src/hooks/useMaterialize.ts` | 缺失 | ❌ 推迟 | Deferred |
| `expo-haptics` 依赖 | 缺失 | ✅ 重装 | Done |
| `expo-linear-gradient` 依赖 | 缺失 | ❌ 推迟 Phase 18（Option B） | Deferred |

---

## 下一步

- [ ] Phase 18：Option B 玻璃体系 —— `GlassEffectContainer` / `GlassEffectUnion` / `VibrantText` / `GlassShimmer` + `expo-linear-gradient`
- [ ] Phase 19（或更后）：Option C App Icon 四变体 + Extra-Large token
- [ ] 各屏幕替换裸 Pressable 为 SpringPressable —— 另立相位

---

*Phase: 17-design-ground*
*Verified: 2026-04-24*
```
</action>

<acceptance_criteria>
- `npm run verify` 退出码 0
- `vitest run` 输出显示 4 Test Files passed（原 3 + 新 1）
- `test -f .planning/phases/17-design-ground/17-VERIFICATION.md` 返回 0
- `grep -c "^### T[1-7]:" .planning/phases/17-design-ground/17-VERIFICATION.md` = 7
- `grep -c "\[x\] 通过" .planning/phases/17-design-ground/17-VERIFICATION.md` = 7
</acceptance_criteria>

---

## Wave 1 整体验收

- [ ] 7 个 task 全部 acceptance_criteria 通过
- [ ] `npm run verify` 绿
- [ ] v1.2 §18.2 清单中 4 个核心资产（motion / useReduceMotion / haptics / SpringPressable）+ `expo-haptics` 全部就绪
- [ ] 未修改 `.planning/IOS26-DESIGN-GUIDE.md`
- [ ] 未修改任何屏幕 / 业务组件

## 提交

建议单一原子提交（所有任务 1 个 commit，因为互相依赖）：

`feat(17-01): 设计宪法基础资产 — motion/haptics/SpringPressable + expo-haptics`
