---
phase: 05-mvp-v2
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/navigation/types.ts
  - src/navigation/RootNavigator.tsx
  - src/screens/BudgetScreen.tsx
  - src/screens/AssetScreen.tsx
autonomous: true
requirements:
  - REF-01
  - REF-02
user_setup: []
must_haves:
  truths:
    - "底部 Tab 顺序为 明细·图表·预算·资产·我的（见 RootNavigator）"
    - "预算屏展示当月已用/ cap、进度条、超支文案与色"
    - "资产屏可列表、新建、编辑余额（手输）"
  artifacts:
    - src/screens/BudgetScreen.tsx
    - src/screens/AssetScreen.tsx
  key_links:
    - "BudgetScreen 使用 sumExpenseForCalendarMonth + queryAllBills 或 queryBillsForMonth"
---

<objective>
将 **预算**、**资产** MVP 接入 **5 Tab 导航**（`05-UI-SPEC.md`），实现 `BudgetScreen` / `AssetScreen`：当月支出进度、cap 编辑、超支提示；资产列表与编辑。**依赖** 05-01 的 repo 与 `monthExpense`。满足 REF-01、REF-02 可验收闭环。
</objective>

<threat_model>
## Trust boundaries
- 仅本地 UI + SQLite；无新第三方 SDK。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P5-T03 | I.D. | 表单输入 | Low | 无敏感字段上云；余额仅为用户自选快照 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mvp-v2/05-CONTEXT.md
@.planning/phases/05-mvp-v2/05-UI-SPEC.md
@src/navigation/RootNavigator.tsx
@src/navigation/types.ts
@src/screens/HomeScreen.tsx
@src/screens/ChartScreen.tsx
@src/db/budgetRepo.ts
@src/db/assetRepo.ts
@src/db/billRepo.ts
@src/budget/monthExpense.ts
@src/theme/colors.ts
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>扩展 Tab 类型与 Navigator</name>
  <files>src/navigation/types.ts, src/navigation/RootNavigator.tsx</files>
  <read_first>
    src/navigation/types.ts,
    src/navigation/RootNavigator.tsx,
    .planning/phases/05-mvp-v2/05-UI-SPEC.md
  </read_first>
  <action>
    1) `RootTabParamList` 增加 `BudgetTab: undefined` 与 `AssetTab: undefined`（命名可一致于文件）。
    2) `Tab.Navigator` 中 **在 `ChartTab` 与 `MineTab` 之间** 插入两个 `Tab.Screen`：`BudgetTab` → `BudgetScreen`，`AssetTab` → `AssetScreen`；`title` 分别为「预算」「资产」；图标使用 MaterialCommunityIcons（如 `chart-pie` 与 `wallet`）。
    3) 导入 `BudgetScreen`、`AssetScreen`（下一步创建的文件；可先写占位再补全，但提交前须可编译）。
    4) 预算/资产 Tab 的 `headerShown`：若需标题栏，设置 `headerShown: true` 且 `headerStyle` 背景色 `colors.main`，与 Chart 行为对齐（若 Chart 无 header，则两新 Tab 可自行 `true` 以展示月份标题）。
  </action>
  <verify>grep -q "BudgetTab" src/navigation/types.ts && grep -q "AssetTab" src/navigation/types.ts && grep -q "BudgetScreen" src/navigation/RootNavigator.tsx</verify>
  <acceptance_criteria>
    - `src/navigation/types.ts` 同时包含 `BudgetTab` 与 `AssetTab`
    - `src/navigation/RootNavigator.tsx` 中 `Tab.Screen` 出现顺序为：HomeTab, ChartTab, BudgetTab, AssetTab, MineTab（通过阅读文件或 grep 五个 name 均存在）
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <done>导航可编译。</done>
</task>

<task type="auto">
  <name>BudgetScreen</name>
  <files>src/screens/BudgetScreen.tsx</files>
  <read_first>
    src/screens/HomeScreen.tsx,
    src/screens/ChartScreen.tsx,
    src/db/budgetRepo.ts,
    src/db/billRepo.ts,
    src/budget/monthExpense.ts,
    src/utils/money.ts,
    src/theme/colors.ts,
    .planning/phases/05-mvp-v2/05-UI-SPEC.md
  </read_first>
  <action>
    实现 `src/screens/BudgetScreen.tsx`：
    1) 状态：当前月 `Date`（默认 `new Date()`）、从 `getBudgetCap(YYYY-MM, -1)` 读取 cap（`month_key` 格式 `YYYY-MM`，与 `monthAnchor.getFullYear()`、`getMonth()+1` 拼接补零）。
    2) 已用：`queryBillsForMonth(monthAnchor)` 得到列表后调用 `sumExpenseForCalendarMonth(monthAnchor, bills)`。
    3) UI：`05-UI-SPEC.md`：展示月份标题、已用/预算文字、`formatAmountDisplay`；`ProgressBar` 可用 `View` 双层宽度比例；cap 为 0 或未设置时显示空态与「设置预算」按钮。
    4) 设置预算：`TextInput`（decimal-pad）+ 保存调用 `upsertBudgetCap(monthKey, -1, formatAmountDisplay(parseAmount(raw)), nowSec)`；保存后刷新。
    5) 超支：`spent > cap` 且 `cap > 0` 时进度条与提示文案使用 `#dc3545` 或等价红色常量；文案含「已超出预算」或 `05-UI-SPEC` 等价句。
    6) `useFocusEffect`（`@react-navigation/native`）在聚焦时重新加载 bills + cap，以便从记一笔返回后更新。
  </action>
  <verify>grep -q "sumExpenseForCalendarMonth" src/screens/BudgetScreen.tsx && grep -q "upsertBudgetCap" src/screens/BudgetScreen.tsx</verify>
  <acceptance_criteria>
    - `src/screens/BudgetScreen.tsx` 存在且包含子串 `sumExpenseForCalendarMonth`
    - 同文件包含子串 `getBudgetCap` 或 `upsertBudgetCap`（至少其一）
    - 同文件包含子串 `useFocusEffect`
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <done>预算屏功能完整。</done>
</task>

<task type="auto">
  <name>AssetScreen</name>
  <files>src/screens/AssetScreen.tsx</files>
  <read_first>
    src/screens/MineScreen.tsx,
    src/db/assetRepo.ts,
    src/utils/money.ts,
    src/theme/colors.ts,
    .planning/phases/05-mvp-v2/05-UI-SPEC.md
  </read_first>
  <action>
    实现 `src/screens/AssetScreen.tsx`：
    1) `FlatList` 展示 `listAssetAccounts()`：每行名称 + `formatAmountDisplay(parseAmount(balance))`；Clay 白卡行样式（圆角 12、边框 `colors.body`）。
    2) 空态：文案 + 「添加账户」。
    3) 添加/编辑：可用 `Modal` 或 `Alert.prompt` 不可用则 Modal；字段 `name`、`balance`（字符串提交前 `formatAmountDisplay(parseAmount(...))` 存库）；保存调用 `insert` 或 `update`；可选滑动删除调用 `deleteAssetAccount`（若实现删除须确认对话框）。
    4) `useFocusEffect` 刷新列表。
  </action>
  <verify>grep -q "listAssetAccounts" src/screens/AssetScreen.tsx</verify>
  <acceptance_criteria>
    - `src/screens/AssetScreen.tsx` 存在且含 `listAssetAccounts`
    - 同文件含 `insertAssetAccount` 或 `updateAssetAccount`
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <done>资产屏可用。</done>
</task>

<task type="auto">
  <name>全量 verify</name>
  <files>—</files>
  <read_first>
    package.json
  </read_first>
  <action>
    在仓库根执行 `npm run verify`，修复所有 lint/type/test 失败直至退出码 0。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/swiftcost-rn/SwiftCostRN && npm run verify</verify>
  <acceptance_criteria>
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>质量门禁绿。</done>
</task>

</tasks>

<verification>
- [ ] 真机或模拟器可走完：设 cap → 记一笔支出 → 预算已用变化（手工）
- [ ] `npm run verify` 绿
</verification>

<success_criteria>
- REF-01 / REF-02 在用户可感知 UI 上可验收；与 `05-CONTEXT` 边界一致。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-mvp-v2/05-02-SUMMARY.md`
</output>
