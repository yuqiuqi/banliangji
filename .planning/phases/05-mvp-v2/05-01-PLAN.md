---
phase: 05-mvp-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/database.ts
  - src/db/budgetRepo.ts
  - src/db/assetRepo.ts
  - src/budget/monthExpense.ts
  - src/budget/monthExpense.test.ts
  - .planning/ADR-2026-04-21-budget-assets-sqlite.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REF-01
  - REF-02
user_setup: []
must_haves:
  truths:
    - "main.db 含 budget_cap 与 asset_account 表且 bill_list DDL 未删减列"
    - "当月支出合计函数与 chartAggregate 一致：仅 type===1，半开月区间"
    - "PROJECT.md 或 ADR 写明新表职责与备份提示"
  artifacts:
    - src/db/budgetRepo.ts
    - src/db/assetRepo.ts
    - src/budget/monthExpense.test.ts
  key_links:
    - "monthExpense 单测含 type 2 被排除的断言"
---

<objective>
为 Phase 5 建立 **SQLite 新表**（预算 cap、资产账户）、**仓库层 CRUD**、与 **`bill_list` 一致的当月支出合计** 纯函数 + **Vitest**；不实现 UI。满足 REF-01/REF-02 的数据基础与 ROADMAP 05-01。
Output: `database.ts` DDL、`budgetRepo.ts`、`assetRepo.ts`、`monthExpense.ts`、测试、ADR 短文档、`PROJECT.md` 一行决策。
</objective>

<threat_model>
## Trust boundaries
- 数据仅存本机 SQLite；本计划不新增网络、定位、通讯录权限。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P5-T01 | Tampering | main.db | Low / accepted | OS 沙箱；无多用户 |
| P5-T02 | I.D. | 本地文件 | Low | 文档提醒备份路径；不记录敏感凭证 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-mvp-v2/05-CONTEXT.md
@.planning/phases/05-mvp-v2/05-UI-SPEC.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@src/db/database.ts
@src/db/billRepo.ts
@src/chart/chartAggregate.ts
@src/chart/chartAggregate.test.ts
@src/utils/money.ts
@src/types/models.ts
</context>

<tasks>

<task type="auto">
  <name>DDL：budget_cap + asset_account</name>
  <files>src/db/database.ts</files>
  <read_first>
    src/db/database.ts,
    .planning/phases/05-mvp-v2/05-CONTEXT.md,
    .planning/phases/05-mvp-v2/05-RESEARCH.md
  </read_first>
  <action>
    在 `getDatabase()` 的 `execSync` 字符串中 **追加**（保留现有 `bill_list` DDL 不变）：
    1) 表 `budget_cap`：列 `id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL`, `month_key TEXT NOT NULL`, `category_id INTEGER NOT NULL DEFAULT -1`（**-1 表示全月总预算**，非 NULL）, `cap_amount TEXT NOT NULL`, `create_time REAL`, `update_time REAL`，以及 `UNIQUE(month_key, category_id)`。
    2) 表 `asset_account`：列 `id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL`, `name TEXT NOT NULL`, `balance TEXT NOT NULL DEFAULT '0'`, `create_time REAL`, `update_time REAL`。
    不得 `DROP` 或 `ALTER` `bill_list`。
  </action>
  <verify>grep -q "budget_cap" src/db/database.ts && grep -q "asset_account" src/db/database.ts && grep -q "bill_list" src/db/database.ts</verify>
  <acceptance_criteria>
    - `src/db/database.ts` 含子串 `CREATE TABLE IF NOT EXISTS budget_cap`
    - 同文件含子串 `CREATE TABLE IF NOT EXISTS asset_account`
    - 同文件仍含子串 `CREATE TABLE IF NOT EXISTS bill_list` 且 bill_list 列集合与修改前一致（id,type,categoryId,icon,name,remark,amount,createTime,updateTime,billTime）
  </acceptance_criteria>
  <done>DDL 追加完成且 bill_list 未破坏。</done>
</task>

<task type="auto">
  <name>monthExpense 纯函数 + 测试</name>
  <files>src/budget/monthExpense.ts, src/budget/monthExpense.test.ts</files>
  <read_first>
    src/chart/chartAggregate.ts,
    src/chart/chartAggregate.test.ts,
    src/types/models.ts,
    src/utils/money.ts,
    src/db/billRepo.ts
  </read_first>
  <action>
    1) 新建 `src/budget/monthExpense.ts`，导出函数 `sumExpenseForCalendarMonth(monthAnchor: Date, bills: Bill[]): number`：
    - 使用与 `queryBillsForMonth` 相同的本地月边界：`start = new Date(y,m,1)`, `endExclusive = new Date(y,m+1,1)`，比较 `bill.billTime`（秒）满足 `>= startSec && < endSecExclusive`。
    - 仅累计 `bill.type === 1` 的行，金额 `parseAmount(bill.amount)`。
    2) 新建 `src/budget/monthExpense.test.ts`：构造 `monthAnchor = new Date(2026, 3, 15)`，账单含 type 1 与 type 2 各一条；断言仅 type 1 计入；再断言与 `chartAggregate` 中对同一 `Bill[]` 的支出过滤逻辑一致（可对比 `filterExpenseBills` 若存在，或直接断言总额数值）。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/swiftcost-rn/SwiftCostRN && npm run test -- --run src/budget/monthExpense.test.ts</verify>
  <acceptance_criteria>
    - 文件 `src/budget/monthExpense.ts` 存在且导出 `sumExpenseForCalendarMonth`
    - 文件 `src/budget/monthExpense.test.ts` 存在且 `npm run test -- --run src/budget/monthExpense.test.ts` 退出码 0
    - `monthExpense.test.ts` 含断言语句涉及 `type` 为 2 的账单不计入（grep 可搜 `2` 与 `type`）
  </acceptance_criteria>
  <done>当月支出合计与支出口径单测绿。</done>
</task>

<task type="auto">
  <name>budgetRepo + assetRepo</name>
  <files>src/db/budgetRepo.ts, src/db/assetRepo.ts</files>
  <read_first>
    src/db/billRepo.ts,
    src/db/database.ts,
    src/types/models.ts
  </read_first>
  <action>
    1) `src/db/budgetRepo.ts`：`BudgetCapRow` 类型（与表列一致）；`upsertBudgetCap(monthKey: string, categoryId: number, capAmount: string, nowSec: number)` 使用 `INSERT ... ON CONFLICT(month_key, category_id) DO UPDATE`（SQLite UPSERT；若驱动写法不同则用「先删后插」或事务内 select+update，但须保证唯一键下仅一行）；`getBudgetCap(monthKey: string, categoryId: number): string | null` 返回 cap_amount 或 null。
    2) `src/db/assetRepo.ts`：`AssetAccountRow`；`listAssetAccounts()`、`insertAssetAccount(name, balance, nowSec)`、`updateAssetAccount(id, name, balance, nowSec)`、`deleteAssetAccount(id)`。
    3) 全用 `getDatabase()` + `runSync`/`getFirstSync`/`getAllSync`；禁止 `any`。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/swiftcost-rn/SwiftCostRN && npm run typecheck</verify>
  <acceptance_criteria>
    - `src/db/budgetRepo.ts` 含字符串 `upsertBudgetCap` 与 `getBudgetCap`
    - `src/db/assetRepo.ts` 含字符串 `listAssetAccounts` 与 `insertAssetAccount`
    - `npm run typecheck` 退出码 0
  </acceptance_criteria>
  <done>两仓库文件通过 typecheck。</done>
</task>

<task type="auto">
  <name>ADR + PROJECT Key Decision</name>
  <files>.planning/ADR-2026-04-21-budget-assets-sqlite.md, .planning/PROJECT.md</files>
  <read_first>
    .planning/PROJECT.md,
    .planning/phases/05-mvp-v2/05-CONTEXT.md
  </read_first>
  <action>
    1) 创建 `.planning/ADR-2026-04-21-budget-assets-sqlite.md`（Markdown）：说明 `budget_cap` / `asset_account` 职责、与 `bill_list` **不共享行**、备份仍为用户设备级备份、`category_id=-1` 表示全月总预算。
    2) 在 `.planning/PROJECT.md` 的 `Key Decisions` 表 **追加一行**：标题如「Phase 5 预算/资产表」；Outcome 指向该 ADR 文件名。
  </action>
  <verify>test -f .planning/ADR-2026-04-21-budget-assets-sqlite.md && grep -q "budget_cap" .planning/ADR-2026-04-21-budget-assets-sqlite.md && grep -q "ADR-2026-04-21-budget-assets-sqlite" .planning/PROJECT.md</verify>
  <acceptance_criteria>
    - `.planning/ADR-2026-04-21-budget-assets-sqlite.md` 存在且含 `budget_cap` 与 `asset_account`
    - `.planning/PROJECT.md` 含子串 `ADR-2026-04-21-budget-assets-sqlite`
  </acceptance_criteria>
  <done>迁移与产品说明可追溯。</done>
</task>

</tasks>

<verification>
- [ ] `npm run test` 通过（含 monthExpense）
- [ ] `npm run lint` 通过
- [ ] `npm run typecheck` 通过
</verification>

<success_criteria>
- REF-01/REF-02 的数据层与文档锚点齐备；执行 05-02 可不再改 DDL 即可完成 UI。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-mvp-v2/05-01-SUMMARY.md`
</output>
