---
phase: 10-persist-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - DATA-02
must_haves:
  truths:
    - "`DATA-02-SMOKE.md` 含明确定义的 **Kill 协议**（排除「仅停止 Metro」）；含 **Result:** `PASS` | `BLOCKED` | `FAIL` 之一"
    - "若 **Result: PASS**：`REQUIREMENTS.md` 中 **DATA-02** 复选框为 `[x]`"
    - "若 **Result: BLOCKED/FAIL**：`DATA-02-SMOKE.md` 含 **阻塞/失败原因** 与 **下一步取证计划**；`STATE.md` 的 Deferred 或正文中可引用该路径"
  artifacts:
    - .planning/phases/10-persist-uat/DATA-02-SMOKE.md
---

<objective>
落实 **DATA-02**：在 iOS（真机或 Simulator 二选一）完成「记一笔 → **系统级杀进程** → 冷启动」可复现记录，并据结果更新需求与状态。吸收 `10-REVIEWS.md`：**Kill 定义**、**WAL/刷盘** 风险说明、可选 **`main.db` 文件级核对**；明确 **`npm run verify` 不证明 DATA-02**。
</objective>

<threat_model>
## Trust boundaries
- 仅本地规划文档与手动冒烟；无新网络接口、无凭据。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P10-01 | Tampering | N/A — 文档与本地 DB 验证，无服务端 |
| P10-02 | Info disclosure | 冒烟文档案不得写入真实隐私；测试数据用可识别假数据 |
</threat_model>

<context>
@.planning/phases/10-persist-uat/10-CONTEXT.md
@.planning/phases/10-persist-uat/10-REVIEWS.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="execute">
  <name>撰写 DATA-02-SMOKE.md（含 Kill 协议与可选 DB 核对）</name>
  <files>.planning/phases/10-persist-uat/DATA-02-SMOKE.md</files>
  <read_first>.planning/phases/10-persist-uat/10-CONTEXT.md,.planning/phases/10-persist-uat/10-REVIEWS.md,.planning/PROJECT.md</read_first>
  <action>
    新建 `DATA-02-SMOKE.md`，**必须**包含以下章节（标题可微调，语义保留）：

    1. **Environment** — 日期、`git rev-parse --short HEAD`（若在仓库内执行）、iOS 版本、Simulator 机型+runtime **或** 真机型号；Expo/React Native 版本可从 `package.json` 摘 `expo`/`react-native`。

    2. **Kill protocol（排除歧义）** — 用编号步骤写清：
       - **禁止**将「停止 Metro / 仅断调试器」算作 DATA-02 的杀进程。
       - **必须**使用 **App Switcher 上划掉应用** 或 **系统设置 → 强制退出**（二选一写清所用方式）；保存账单后 **等待 UI 显示已落库**（回到列表可见该条）再杀进程。
       - 杀进程后 **等待 ≥5s** 再冷启动。

    3. **Test data** — 测试账单识别串（如备注 `DATA02-20260422`）与金额，便于 `grep`。

    4. **Steps** — 从冷启动到记一笔到 Kill 到再启动到 **在明细/列表中检索识别串**。

    5. **Optional: SQLite on disk（推荐）** — 简短说明：若在 Simulator，可用 `xcrun simctl get_app_container booted <bundle-id> data` 定位容器后查找 `main.db`（路径以实际为准）；**可选**用 `sqlite3 … 'SELECT … FROM bill_list WHERE note…'` 核对行存在。**若未执行**，写 `Skipped (reason): …`。

    6. **Result** — 单独一行：`Result: PASS` | `Result: BLOCKED` | `Result: FAIL`，下一行起为证据摘要（截图路径可选）。

    7. **Note on verify** — 固定句：`npm run verify`（typecheck+lint+vitest）**不覆盖**本杀进程场景。

    **不得**留空 `Result:` 行；执行前可先将 `Result:` 设为 `BLOCKED` 并填计划，执行后改为最终状态。
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/10-persist-uat/DATA-02-SMOKE.md`
    - `rg -n "Kill protocol|禁止|App Switcher|Result:" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 至少 3 处匹配
    - `rg "Result: (PASS|BLOCKED|FAIL)" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 恰好 1 行匹配上述三者之一
    - `rg "npm run verify" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 至少 1 匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>按 Result 更新 REQUIREMENTS.md 与 STATE.md</name>
  <files>.planning/REQUIREMENTS.md,.planning/STATE.md</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,.planning/REQUIREMENTS.md,.planning/STATE.md</read_first>
  <action>
    1) 若 `DATA-02-SMOKE.md` 中 `Result: PASS`：将 `.planning/REQUIREMENTS.md` 内 **DATA-02** 行从 `- [ ]` 改为 `- [x]`（仅该条，不改其它 REQ）。

    2) 若 `Result: BLOCKED` 或 `FAIL`：**不要**勾选 DATA-02；在 `STATE.md` 正文 **Deferred Items** 或 **Current Status** 增加一条，指向 `DATA-02-SMOKE.md` 中原因与「下一步取证计划」标题或段落。

    3) 无论何种 Result，在 `REQUIREMENTS.md` 的 **Traceability** 表中可将 Phase 10 / DATA-02 的 Status 从 `Pending` 改为 `In progress` 或 `Done`（与 Result 一致：`PASS`→`Done`，否则 `Blocked`）。
  </action>
  <acceptance_criteria>
    - 若 `rg "Result: PASS" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 成功：则 `rg "^- \\[x\\] \\*\\*DATA-02" .planning/REQUIREMENTS.md` 有 1 匹配
    - 若 `rg "Result: (BLOCKED|FAIL)" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 成功：则 `rg "^- \\[x\\] \\*\\*DATA-02" .planning/REQUIREMENTS.md` **无**匹配
    - `rg "DATA-02-SMOKE|DATA-02" .planning/STATE.md` 至少 1 匹配
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Investigation notes（FAIL 时填根因；否则 N/A）</name>
  <files>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,src/</files>
  <read_first>.planning/phases/10-persist-uat/DATA-02-SMOKE.md,package.json</read_first>
  <action>
    在 `DATA-02-SMOKE.md` 末尾保证存在 `## Investigation notes` 小节：

    - 若 `Result: FAIL`：在本节记录对 `src/` 中 `expo-sqlite` / `openDatabase` / `transaction` / `main.db` 的**只读**结论（是否 await 事务、UI 返回前是否可能未提交）。**不擅自改业务逻辑**；需代码修复时另开任务。

    - 若 `Result: PASS` 或 `Result: BLOCKED`：本节正文仅一行 `N/A — Result was not FAIL.`
  </action>
  <acceptance_criteria>
    - `rg "^## Investigation notes" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 有 1 匹配
    - `rg "N/A — Result was not FAIL|expo-sqlite|openDatabase|transaction" .planning/phases/10-persist-uat/DATA-02-SMOKE.md` 至少 1 匹配
  </acceptance_criteria>
</task>

</tasks>
