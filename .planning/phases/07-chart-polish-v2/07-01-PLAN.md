---
phase: 07-chart-polish-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/layout.ts
  - src/screens/ChartScreen.tsx
autonomous: true
requirements:
  - REF-03
  - CHART-01
  - CHART-02
user_setup: []
must_haves:
  truths:
    - "src/theme/layout.ts 导出 chartFadeMs（数值在 150–250 内）"
    - "src/chart/chartAggregate.ts 无改动"
    - "npm run verify 退出码 0"
  artifacts:
    - src/theme/layout.ts
    - src/screens/ChartScreen.tsx
  key_links:
    - "07-UI-SPEC 柱/条与柱间距表"
---

<objective>
Phase 7 Wave 1：**图表组件结构与主题绑定** — 在 `layout.ts` 增加图表动效**时长令牌**（供 07-02 使用）；在 `ChartScreen` 上优化柱条 **圆角顶、柱宽、柱间距、令牌色** 的 StyleSheet 表现，**不**在本 plan 内接入 `Animated` 运行时动效（留给 07-02）。
</objective>

<threat_model>
## Trust boundaries
- 仅本机 UI 常量和样式；不触及 SQLite、不修改聚合。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P7-T01 | Tampering | 无 | Low / N/A | 无业务数据逻辑变更 |
| P7-T02 | I.D. | 无 | Low | — |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chart-polish-v2/07-CONTEXT.md
@.planning/phases/07-chart-polish-v2/07-UI-SPEC.md
@.planning/phases/07-chart-polish-v2/07-RESEARCH.md
@src/theme/layout.ts
@src/theme/colors.ts
@src/screens/ChartScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>chartFadeMs token in layout.ts</name>
  <files>src/theme/layout.ts</files>
  <read_first>
    src/theme/layout.ts,
    .planning/phases/07-chart-polish-v2/07-UI-SPEC.md
  </read_first>
  <action>
    在 `src/theme/layout.ts` 中于 `pressDurationMs` 附近 **新增并导出** 常量 `chartFadeMs`，类型为 `number`，取值 **200**（满足 UI-SPEC「约 150–250ms」区间的中值）。添加一行简短注释：供图表主图区淡入淡出（Phase 7）。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `src/theme/layout.ts` 含标识符 `chartFadeMs` 且值为 `200`
    - 同文件存在「chart」或「Phase 7」字样的注释与 `chartFadeMs` 同行或相邻 3 行内
    - `npm run verify` 在项目根目录退出码为 0
  </acceptance_criteria>
  <done>chart 动效时长自 `layout` 单源导出。</done>
</task>

<task type="auto">
  <name>Bar row styles — 图表精致化向可读性</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>
    src/screens/ChartScreen.tsx,
    src/theme/colors.ts,
    .planning/phases/07-chart-polish-v2/07-UI-SPEC.md
  </read_first>
  <action>
    在 **不修改** 任何 `chartAggregate` 导入与调用语义的前提下，仅调整 `StyleSheet` 与柱渲染相关常数：
    1) `styles.bar`：`borderRadius` 至少为 **4**（可略增至 **6** 若仍保持竖条顶圆角观感）；`width` 可在 **8–12** 范围内择一固定值以提升细条可读性（与 `07-UI-SPEC`「竖条+圆角顶」一致）。
    2) `styles.barCol`：略增 `marginHorizontal`（例如从 `1` 到 `2` 或 `3`）使柱间留白可感知，且 **全列仍均分** `chartRow` 宽度（保持 `flex: 1` 布局）。
    3) 确认 `barOn` 仍仅使用 `colors.title` 类令牌（已满足则保持）；禁止硬编码新十六进制色。
    4) 可选：在文件顶部自 `../theme/layout` 增加 `import { chartFadeMs }`（仅 import，**不在本 task 使用 Animated**）以便 07-02 直接沿用；若 import 后触发 unused 变量则改为 **07-02 再 import**，本 task 不强制 import。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `ChartScreen.tsx` 中**无**对 `../chart/chartAggregate` 的 diff（可用 grep：`chartPointsWeekOrMonth` 调用行数不变、未改 `aggregateExpenseByCategory` 传参结构）
    - `styles.bar` 含 `borderRadius` 且数值 ≥ 4
    - `styles.barCol` 含 `marginHorizontal` 且 > 0
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>柱条静态视觉与间距提升，主题仍唯 `colors`。</done>
</task>

</tasks>
