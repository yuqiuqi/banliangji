---
phase: 07-chart-polish-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
files_modified:
  - src/screens/ChartScreen.tsx
autonomous: true
requirements:
  - REF-03
  - CHART-01
  - CHART-02
user_setup: []
must_haves:
  truths:
    - "主图区在 granularity 或 period 变化时有 opacity 动效（约 chartFadeMs）"
    - "src/chart/chartAggregate.ts 无改动"
    - "npm run verify 绿"
  artifacts:
    - src/screens/ChartScreen.tsx
  key_links:
    - "07-UI-SPEC 实现对照清单可勾选 2、3、4、6 项"
---

<objective>
Phase 7 Wave 2：**动效、图例/标签、空态与可读性** — 在 `ChartScreen` 为 **主图区（含柱行与轴标签区）** 加 **Animated 淡入淡出**；优化 **柱下 `barLabel` 与分类列表** 的层次与进度条高度；**核对空态** 与 `07-UI-SPEC` 分工（区间内无柱 vs 列表无分类）。
</objective>

<threat_model>
## Trust boundaries
- 仅 UI；聚合与 DB 不变。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P7B-T01 | Tampering | 无 | Low | 无数据逻辑 |
| P7B-T02 | I.D. | 无 | Low | — |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-chart-polish-v2/07-CONTEXT.md
@.planning/phases/07-chart-polish-v2/07-UI-SPEC.md
@.planning/phases/07-chart-polish-v2/07-RESEARCH.md
@src/theme/layout.ts
@src/screens/ChartScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>主图区 Animated fade on period change</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>
    src/screens/ChartScreen.tsx,
    src/theme/layout.ts,
    .planning/phases/07-chart-polish-v2/07-UI-SPEC.md
  </read_first>
  <action>
    1) 自 `react-native` 增加 `Animated`、`Easing` 导入（与现有 `View` 等合并为一 import）。
    2) 自 `../theme/layout` 导入 `chartFadeMs`。
    3) 使用 `useRef(new Animated.Value(1)).current` 作为柱图区 opacity 驱动；用 `useEffect`，依赖项 **`[granularity, safeIndex]`**（或等价的 `periodIndex` + `granularity`），在变化时：先 `Animated.timing` 到 **0**（`duration: chartFadeMs/2` 或合理的一半，`useNativeDriver: true`），`start` 回调**之后**在下一帧再 `timing` 回 **1**（注意：数据已由 React state 更新，动效只包裹 **柱行+柱标签** 容器，不要包裹整个 `ScrollView`）。
    4) 将 `styles.chartRow` 与 `points.map` 及下方 `barLabel` 行包在 **`Animated.View`** 中，或等价的「主图区」子树；`chartEmpty` 文案若在同一卡片内，可与柱区**同步**淡入或保持静态 — 以「周期切换时用户可见柱区重绘感」为优先，与 `07-UI-SPEC` 一致即可。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `ChartScreen.tsx` 含 `Animated` 与 `chartFadeMs`
    - 存在 `useEffect` 且依赖数组包含 `granularity` 与 `safeIndex` 或 `periodIndex`（grep 可验）
    - 存在 `Animated.timing` 与 `useNativeDriver: true`
    - 未改 `../chart/chartAggregate` 任意导入行的语义
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>周期切换时主图区有可见 fade（opacity）。</done>
</task>

<task type="auto">
  <name>分类列表 + 空态 + 小标签层次</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>
    src/screens/ChartScreen.tsx,
    .planning/phases/07-chart-polish-v2/07-UI-SPEC.md
  </read_first>
  <action>
    1) **分类列表（图下区）**：在现有 `catRow` / `catName` / `catAmt` / `progressBg` 上，将 **金额** 字重略加重（如 `fontWeight: '600'`）或使 **类别名** 为 `500` 而金额为 `600`，行距上保持可读（`catRow` 的 `marginBottom` 可 14–18）；`progressBg` 高度在 **6–8** 间取整（若已为 6 可增至 **8** 且同调整 `progressFg`）。
    2) **柱下标签**：`barLabel` 的 `fontSize` 可 **+1** 或色用 `colors.lightTitle` 保持对比；`numberOfLines={1}` 保持。
    3) **空态**：对 `chartEmpty` 与 `emptyWrap` 文案与 `07-UI-SPEC`「区间无支出 / 本区间」两路核对；若需可选图标，仅使用已存在的 **`@expo/vector-icons` / `MaterialCommunityIcons`** 单一小图标，**不** 新增资源包；若与 spec 无冲突可不改字串。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `catAmt` 或 `catName` 至少一项含 `fontWeight` 且为 `'600'` 或 `'500'`（grep `fontWeight`）
    - `progressBg` 的 `height` 在 6–8 间
    - 未引入新 npm 依赖
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>列表层次与空态/小标签与 UI-SPEC 自洽。</done>
</task>

</tasks>
