---
phase: 11-chrome-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/colors.ts
  - src/theme/ThemeContext.tsx
  - src/theme/palette.ts
  - App.tsx
  - src/navigation/RootNavigator.tsx
  - src/screens/HomeScreen.tsx
  - src/screens/CalendarScreen.tsx
  - src/screens/BillQueryScreen.tsx
  - src/screens/MineScreen.tsx
  - src/screens/AssetScreen.tsx
  - src/screens/CreateBillScreen.tsx
  - src/screens/ChartScreen.tsx
  - src/screens/BillDetailScreen.tsx
  - src/screens/BudgetScreen.tsx
  - src/components/BillCalculator.tsx
  - src/components/CategoryIcon.tsx
  - src/components/ios/ListRow.tsx
  - src/components/ios/SegmentedTwo.tsx
  - src/components/ios/GroupedInset.tsx
  - src/components/ios/Fab.tsx
autonomous: true
requirements:
  - THEME-01
must_haves:
  truths:
    - "`useColorScheme()` 驱动亮/暗；无应用内主题开关（per D11-02）"
    - "所有列出的 `src/screens/*.tsx` 与 `src/components/**/*.tsx` 不再从 `../theme/colors` 静态导入 **单一** 浅色常量作为唯一来源；改为 `useAppTheme()` Context 或等价"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/theme/ThemeContext.tsx
    - src/theme/palette.ts
---

<objective>
落实 **D11-01 / D11-02**：系统深色外观下 Tier-1 与全站消费 `colors` 的组件使用 **同一套语义 token**（亮/暗成对），并完成导航与根布局联动。**不**新增应用内主题切换。
</objective>

<threat_model>
## Trust boundaries
- 仅本地 UI；无新网络、无凭据。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P11-01 | Tampering | N/A — 本地表现层 |
| P11-02 | Info disclosure | N/A |
</threat_model>

<context>
@.planning/phases/11-chrome-depth/11-CONTEXT.md
@.planning/phases/11-chrome-depth/11-RESEARCH.md
@.planning/phases/11-chrome-depth/11-PATTERNS.md
@.planning/phases/11-chrome-depth/11-UI-SPEC.md
@UI-SPEC.md
</context>

<tasks>

<task type="execute">
  <name>palette.ts + ThemeContext（亮/暗成对 token）</name>
  <files>src/theme/palette.ts,src/theme/ThemeContext.tsx,src/theme/colors.ts</files>
  <read_first>src/theme/colors.ts,.planning/phases/11-chrome-depth/11-CONTEXT.md,UI-SPEC.md</read_first>
  <action>
    1) 新建 `src/theme/palette.ts`：导出 `lightPalette` — **字段集合与当前 `colors` 导出对象完全一致**（可复制现有常量作为 `lightPalette`）；导出 `darkPalette` — **相同键名**，取值采用 iOS dark grouped 近似（示例：`canvas: "#000000"`, `surface: "#1C1C1E"`, `main: "#2C2C2E"`, `title`/`onMain: "#FFFFFF"`, `lightTitle`/`onMainSecondary: "rgba(235, 235, 245, 0.6)"`, `divider: "rgba(84, 84, 88, 0.65)"`, `tabBarInactive: "#8E8E93"`, `accent`/`tabBarActive: "#0A84FF"`，其余键按对比度合理补齐，**不得**遗漏 `colors.ts` 中已有键）。

    2) 新建 `src/theme/ThemeContext.tsx`：`useColorScheme()` 读取系统外观；导出 `ThemeProvider`、`useAppTheme()`；`useAppTheme()` 返回 `{ colorScheme, colors }`，其中 `colors` 为 `lightPalette` 或 `darkPalette`。

    3) 修改 `src/theme/colors.ts`：保留 **向后兼容** — 仍 `export const colors = lightPalette`（或 `export { lightPalette as colors }`），并 `export { lightPalette, darkPalette } from "./palette"`；若存在 `iosSemantic`，改为 **函数** `getIosSemantic(c: typeof lightPalette)` 或在 Context 内构造等价对象，避免深色屏仍引用浅色 `iosSemantic` 常量。

    **Per D11-01 / D11-02：** 不得添加应用内主题 AsyncStorage 状态。
  </action>
  <acceptance_criteria>
    - `test -f src/theme/palette.ts`
    - `test -f src/theme/ThemeContext.tsx`
    - `rg "useColorScheme" src/theme/ThemeContext.tsx` 至少 1 匹配
    - `rg "darkPalette|lightPalette" src/theme/palette.ts` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>根布局与 Navigation 主题联动</name>
  <files>App.tsx,src/navigation/RootNavigator.tsx</files>
  <read_first>App.tsx,src/navigation/RootNavigator.tsx,src/theme/ThemeContext.tsx</read_first>
  <action>
    1) `App.tsx`：用 `ThemeProvider` 包裹现有 `RootNavigator` 外层容器；最外层 `backgroundColor` 使用 `useAppTheme().colors.canvas`（需将 `App` 拆为子组件或内联 wrapper 使 hook 合法）。`StatusBar`：`style={colorScheme === "dark" ? "light" : "dark"}`。

    2) `RootNavigator.tsx`：`navTheme.colors` 的 `background`、`card`、`border`、`text`、`primary` **绑定**当前 `colors.canvas`、`colors.surface`、`colors.divider`、`colors.title`、`colors.accent`（名称按 React Navigation DefaultTheme 字段对应）。`HomeStackNavigator` 的 `headerStyle.backgroundColor`、`headerTintColor`、`headerTitleStyle.color`、`contentStyle.backgroundColor` 使用当前 palette 的 `main`/`onMain`/`canvas`。

    **Per D11-01：** Tab 的 blur/tint 具体值在 Plan 02 再统一；本任务仅保证 **不透明** 部分与语义色一致。
  </action>
  <acceptance_criteria>
    - `rg "ThemeProvider" App.tsx` 至少 1 匹配
    - `rg "useAppTheme|useTheme" App.tsx` 至少 1 匹配
    - `rg "navTheme|NavigationContainer" src/navigation/RootNavigator.tsx` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>全量迁移静态 colors 导入到 useAppTheme</name>
  <files>src/screens/HomeScreen.tsx,src/screens/CalendarScreen.tsx,src/screens/BillQueryScreen.tsx,src/screens/MineScreen.tsx,src/screens/AssetScreen.tsx,src/screens/CreateBillScreen.tsx,src/screens/ChartScreen.tsx,src/screens/BillDetailScreen.tsx,src/screens/BudgetScreen.tsx,src/components/BillCalculator.tsx,src/components/CategoryIcon.tsx,src/components/ios/ListRow.tsx,src/components/ios/SegmentedTwo.tsx,src/components/ios/GroupedInset.tsx,src/components/ios/Fab.tsx</files>
  <read_first>src/theme/ThemeContext.tsx,src/components/ios/ListRow.tsx,src/screens/HomeScreen.tsx</read_first>
  <action>
    对上述 **每一个** `<files>` 列出文件：删除 `import { colors } from ".../theme/colors"`（或 `iosSemantic` 若仅用于色值）；在函数组件顶层调用 `const { colors } = useAppTheme()`；若文件导出非组件常量需色值，改为从 `lightPalette` 导入仅作默认值或改为函数参数。

    **特殊：** `CategoryIcon.tsx` 若为函数组件，直接使用 hook；否则改为函数组件或接收 `colors` prop（由父传入 `useAppTheme().colors`）。

    完成后全仓库 `rg 'from \"../theme/colors\"|from \"../../theme/colors\"' src` **仅** `colors.ts`、`palette.ts`、`ThemeContext.tsx`、测试文件可保留静态导入；业务组件不应再直接依赖静态浅色唯一真源。
  </action>
  <acceptance_criteria>
    - `npm run verify` 退出码 0
    - `rg "useAppTheme" src/screens` 匹配数 ≥ 8
    - `rg "useAppTheme" src/components` 匹配数 ≥ 4
    - `rg 'from ["\x27]\.\./theme/colors["\x27]|from ["\x27]\.\./\.\./theme/colors["\x27]' src/screens src/components` 无匹配（执行后业务侧不再静态导入 `theme/colors`）
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| GOAL | 系统深色可读 | 01:全任务 |
| REQ | THEME-01 | 01:全任务 |
| CONTEXT | D11-01, D11-02 | 01:全任务 |
| RESEARCH | ThemeContext 模式 | 01:task1–2 |

## PLANNING COMPLETE
