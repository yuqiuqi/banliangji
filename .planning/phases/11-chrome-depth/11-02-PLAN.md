---
phase: 11-chrome-depth
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01-PLAN.md
files_modified:
  - src/components/ios/IOSChromeGlassBackground.tsx
  - src/hooks/useReduceTransparency.ts
  - src/navigation/RootNavigator.tsx
  - src/screens/CreateBillScreen.tsx
autonomous: true
requirements:
  - A11Y-01
  - LG-01
must_haves:
  truths:
    - "单一 hook 或模块订阅 `AccessibilityInfo.isReduceTransparencyEnabled`（per D11-03）"
    - "Tab 与记一笔 Blur 区使用共享分层表面原语（per D11-05），降级时为不透明底"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/components/ios/IOSChromeGlassBackground.tsx
    - src/hooks/useReduceTransparency.ts
---

<objective>
落实 **D11-03～D11-05、D11-07**：集中 **降低透明度** 降级；将 Tab 与记一笔 **Tier-1** `BlurView` 换为 **材质栈**（Blur + tint + 可选 LinearGradient hairline + shadow），Android 维持不透明近似。
</objective>

<threat_model>
## Trust boundaries
- 本地 UI only。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P11-01 | Tampering | N/A |
</threat_model>

<context>
@.planning/phases/11-chrome-depth/11-MATERIAL-MOTION-SPEC.md
@.planning/phases/11-chrome-depth/11-CONTEXT.md
@src/navigation/RootNavigator.tsx
@src/screens/CreateBillScreen.tsx
</context>

<tasks>

<task type="execute">
  <name>useReduceTransparency hook</name>
  <files>src/hooks/useReduceTransparency.ts</files>
  <read_first>package.json,.planning/phases/11-chrome-depth/11-CONTEXT.md</read_first>
  <action>
    新建 `src/hooks/useReduceTransparency.ts`：  
    - 初始值 `AccessibilityInfo.isReduceTransparencyEnabled()`（若 API 可用；否则 `false`）。  
    - `useEffect` 订阅 `AccessibilityInfo.addEventListener("reduceTransparencyChanged", ...)`（API 名以 RN 版本为准；若仅支持 `change` 事件则使用文档对应事件名）。  
    - cleanup 移除监听。  
    - 导出 `useReduceTransparency(): boolean`。

    **Per D11-03：** 全 App 仅此 hook 作为「是否降级毛玻璃」的权威来源之一（与 `useAppTheme` 并列使用）。
  </action>
  <acceptance_criteria>
    - `test -f src/hooks/useReduceTransparency.ts`
    - `rg "AccessibilityInfo|reduceTransparency" src/hooks/useReduceTransparency.ts` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>IOSChromeGlassBackground 原语</name>
  <files>src/components/ios/IOSChromeGlassBackground.tsx,src/components/ios/index.ts</files>
  <read_first>.planning/phases/11-chrome-depth/11-MATERIAL-MOTION-SPEC.md §4.1,src/theme/ThemeContext.tsx</read_first>
  <action>
    新建 `src/components/ios/IOSChromeGlassBackground.tsx`（名称可微调，须在 `index.ts` 导出）：  

    **Props（至少）：** `variant: "tabBar" | "dock"`；`children?: ReactNode`（可选，用于叠字）。  

    **行为：**  
    - 调用 `useAppTheme()` 与 `useReduceTransparency()`。  
    - **iOS + 非降级：** `StyleSheet.absoluteFill` 内 **底层** `BlurView`，`intensity`/`tint`：`colorScheme === "dark" ? "dark" : "light"`（tint 与 Phase 9 一致方向）；之上 **半透明** `View` 使用 `colors.main` 或专用 tint（opacity 约 0.35–0.55 需肉眼校准）；可选顶区 `LinearGradient`（`expo-linear-gradient` 已常用则用之；**若未安装**则用 1px `View` hairline 代替，不新增依赖）模拟 §4.1 hairline；**底部** `tabBar` variant 加 **较强** `shadowOpacity`/`shadowRadius`（Dock 更大）。  
    - **降级或 Android：** **无** Blur；`backgroundColor` 使用 `colors.calculatorDockFallback`（Dock）或 `colors.main` 与 80–95% 不透明度的组合，使 **对比度可接受**；保留顶部分隔 `borderTopColor` 或 hairline。  

    **Per D11-04：** 降级区域必须可见、可交互。  
    **Per D11-07：** Android 走不透明分支。
  </action>
  <acceptance_criteria>
    - `test -f src/components/ios/IOSChromeGlassBackground.tsx`
    - `rg "BlurView|useReduceTransparency|useAppTheme" src/components/ios/IOSChromeGlassBackground.tsx` 至少 3 匹配
    - `rg "IOSChromeGlassBackground" src/components/ios/index.ts` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>接入 TabBar 与 CreateBillScreen</name>
  <files>src/navigation/RootNavigator.tsx,src/screens/CreateBillScreen.tsx</files>
  <read_first>src/navigation/RootNavigator.tsx,src/screens/CreateBillScreen.tsx,src/components/ios/IOSChromeGlassBackground.tsx</read_first>
  <action>
    1) `RootNavigator.tsx`：`tabBarBackground` 由 **内联** `BlurView` 改为 `() => <IOSChromeGlassBackground variant="tabBar" />`（仅 iOS；Android 保持原 `tabBarStyle.backgroundColor` 实色逻辑或同样用组件内 Android 分支）。  

    2) `CreateBillScreen.tsx`：所有 **全屏或大面积** `BlurView`（含 Dock 区）替换为 `<IOSChromeGlassBackground variant="dock">` 包裹原有前景内容，或等价结构，使 **同一降级逻辑** 生效。

    移除不再使用的孤立 `BlurView` import（若文件无其它 Blur）。
  </action>
  <acceptance_criteria>
    - `rg "IOSChromeGlassBackground" src/navigation/RootNavigator.tsx` 至少 1 匹配
    - `rg "IOSChromeGlassBackground|BlurView" src/screens/CreateBillScreen.tsx` 至少 1 匹配（允许仍保留非 Dock 区 Blur 若存在 — 若已全部替换则仅 Glass 亦可）
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| REQ | A11Y-01 | 02:全任务 |
| REQ | LG-01 材质 | 02:task2–3 |
| CONTEXT | D11-03—D11-05,D11-07 | 02:全任务 |
| SPEC | §4.1 材质栈 | 02:task2 |

## PLANNING COMPLETE
