---
phase: 11-chrome-depth
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02-PLAN.md
files_modified:
  - src/navigation/RootNavigator.tsx
  - src/screens/CreateBillScreen.tsx
  - .planning/phases/11-chrome-depth/11-VERIFICATION.md
  - UI-SPEC.md
  - DESIGN.md
autonomous: true
requirements:
  - LG-01
  - THEME-01
  - A11Y-01
must_haves:
  truths:
    - "Tier-1 按压或 Tab 切换使用 Reanimated `withSpring`（per D11-06），非全应用 Pressable 迁移"
    - "`11-VERIFICATION.md` 含可勾选清单；根 `UI-SPEC.md` 与 `DESIGN.md` 增加 dark / 降级 / LG 偏差段落"
    - "`npm run verify` 退出码 0"
  artifacts:
    - .planning/phases/11-chrome-depth/11-VERIFICATION.md
---

<objective>
落实 **D11-06、D11-08、D11-09**：Tier-1 **流体动效**；诚实记录偏差；**VERIFICATION** 与根设计文档闭环。
</objective>

<threat_model>
## Trust boundaries
- 本地文档 + UI；无网络。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P11-01 | Tampering | N/A |
</threat_model>

<context>
@.planning/phases/11-chrome-depth/11-MATERIAL-MOTION-SPEC.md
@.planning/phases/11-chrome-depth/11-VERIFICATION.md
@UI-SPEC.md
@DESIGN.md
@src/navigation/RootNavigator.tsx
</context>

<tasks>

<task type="execute">
  <name>Reanimated spring — Tab 与记一笔 Tier-1</name>
  <files>src/navigation/RootNavigator.tsx,src/screens/CreateBillScreen.tsx</files>
  <read_first>package.json,babel.config.js,src/navigation/RootNavigator.tsx,.planning/phases/11-chrome-depth/11-MATERIAL-MOTION-SPEC.md §4.2</read_first>
  <action>
    1) 确认 `react-native-reanimated` 已在 `package.json`；`babel.config.js` 含 `react-native-reanimated/plugin`（若缺失则 **添加** 并置于 plugins **最后**）。

    2) `IOSChromeTabBarButton`（`RootNavigator.tsx`）：用 Reanimated `Animated.View` 包裹 `PlatformPressable` 子级或使用 `useAnimatedStyle` + `Pressable` 的 `onPressIn`/`onPressOut`：`scale` 在 **0.97–0.99** 间 `withSpring`（damping 12–18，stiffness 180–280 — 具体值可微调）。

    3) `CreateBillScreen.tsx`：对 **记一笔 Dock 主操作容器**（或最明显的主按钮容器）在 `onPressIn`/`onPressOut` 施加 **同类 spring scale**；禁止仅使用 `Animated.timing` 线性缩放作为唯一反馈。

    **Per D11-06：** 不强制修改非 Tier-1 屏的所有按钮。
  </action>
  <acceptance_criteria>
    - `rg "withSpring|useAnimatedStyle" src/navigation/RootNavigator.tsx` 至少 1 匹配
    - `rg "withSpring|useAnimatedStyle" src/screens/CreateBillScreen.tsx` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>11-VERIFICATION + UI-SPEC + DESIGN 同步</name>
  <files>.planning/phases/11-chrome-depth/11-VERIFICATION.md,UI-SPEC.md,DESIGN.md</files>
  <read_first>.planning/phases/11-chrome-depth/11-VERIFICATION.md,.planning/ROADMAP.md Phase 11,.planning/phases/11-chrome-depth/11-CONTEXT.md</read_first>
  <action>
    1) 更新 `.planning/phases/11-chrome-depth/11-VERIFICATION.md`：在 **LG-01** 节补充 **具体** spot-check 步骤（例如：「快速滚动明细 3 秒观察顶栏」「切换 Tab 3 次观察图标/弹簧」）；保留 Accepted deviations 表；**Execution date** 留空或由执行人填写。

    2) `UI-SPEC.md`：新增章节 **「Dark mode（Phase 11）」** 与 **「Reduce transparency（Phase 11）」** 各 3–6 行，指向 `ThemeContext` + `IOSChromeGlassBackground` 行为；**「Liquid Glass 工程近似」** 说明 RN 无系统 Lensing 着色器、Accepted deviation 记在 VERIFICATION。

    3) `DESIGN.md`：在首节或「当前里程碑」附近增加 **同主题** 2–4 句，声明深色/降级/LG 纵深由 Phase 11 交付，**不引入网络**。

    **Per D11-08 / D11-09：** 文档必须提及「非像素级复刻」。
  </action>
  <acceptance_criteria>
    - `rg "Reduce transparency|降低透明度" UI-SPEC.md` 至少 1 匹配
    - `rg "Dark|深色" UI-SPEC.md` 至少 1 匹配
    - `rg "Phase 11|Liquid Glass|非像素" DESIGN.md` 至少 1 匹配
    - `rg "spot-check|滚动|Tab" .planning/phases/11-chrome-depth/11-VERIFICATION.md` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plan:task |
|--------|------|-----------|
| GOAL | LG 动效与文档 | 03:全任务 |
| REQ | LG-01, THEME-01, A11Y-01 文档闭环 | 03:task2 |
| CONTEXT | D11-06,D11-08,D11-09 | 03:全任务 |
| RESEARCH | Reanimated spring | 03:task1 |

## PLANNING COMPLETE
