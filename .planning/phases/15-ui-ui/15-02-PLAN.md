---
phase: 15-ui-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01-PLAN.md"
gap_closure: false
files_modified:
  - src/navigation/types.ts
  - src/screens/HomeScreen.tsx
  - src/screens/ChartScreen.tsx
  - src/screens/BillQueryScreen.tsx
  - src/screens/BudgetScreen.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "首页空列表具备可操作主文案 + **去记一笔**（或等价导航到记一笔）入口，符合 `15-UI-SPEC.md` Copywriting"
    - "图表空态文案引导记账或筛选，避免单独一句「暂无」无动作"
    - "账单查询空态含 **去记一笔** 或导航至记一笔的 `SpringPressable`"
    - "预算屏空态或引导与 `15-UI-SPEC.md` 表格方向一致（可操作提示）"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/screens/HomeScreen.tsx
    - src/screens/ChartScreen.tsx
---

<objective>
Wave 2：**首页 / 图表 / 查账 / 预算** — 强化 **视觉锚点**（统计数字层级）与 **空态文案+主操作**；图表区对比度与空态按 `15-UI-SPEC.md` 调整。**禁止**修改 `motion.ts` 弹簧参数与 `SpringPressable` 缩放逻辑。
</objective>

<threat_model>
## Trust boundaries
- 本地 UI；导航仅至已有路由。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P15-03 | Tampering | N/A |
</threat_model>

<context>
@.planning/phases/15-ui-ui/15-UI-SPEC.md
@src/navigation/types.ts
@src/screens/HomeScreen.tsx
@src/screens/ChartScreen.tsx
</context>

<tasks>

<task type="execute">
  <name>RootTabParamList 支持嵌套 HomeStack</name>
  <files>src/navigation/types.ts</files>
  <read_first>src/navigation/types.ts,src/navigation/RootNavigator.tsx</read_first>
  <action>
    在 `src/navigation/types.ts` 从 `@react-navigation/native` 引入 `NavigatorScreenParams`。  
    将 `RootTabParamList` 的 `HomeTab` 从 `undefined` 改为 `NavigatorScreenParams` 与 `HomeStackParamList` 组合（与 React Navigation 6 嵌套类型文档一致）。  
    保持其他 Tab 键不变。使 `ChartTab` 等屏可调用 `navigation.navigate("HomeTab", { screen: "CreateBill" })` 且 TypeScript 通过。
  </action>
  <acceptance_criteria>
    - `rg "NavigatorScreenParams" src/navigation/types.ts` 有匹配
    - `rg "HomeTab:" src/navigation/types.ts` 行内包含 `HomeStackParamList`
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>HomeScreen 空态 + 统计层次</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>src/screens/HomeScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md,src/theme/typography.ts</read_first>
  <action>
    1. `ListEmptyComponent`：在「本月还没有账单」基础上增加 **`SpringPressable`** 主操作，文案 **`去记一笔`**，`onPress` 调用 `navigation.navigate("CreateBill")`（与 `HomeScreen` 第 143 行附近现有用法一致）。  
    2. 空态副文案可用 `iosType.caption1` + `secondaryLabel` 色系，主按钮文案用 `systemBlue` 取向（`colors.accent`）。  
    3. **可选美化（不改动画）**：`statValue` 使用 `iosType.body` 的 `lineHeight` 并与 `statLabel` 间距为 4 的倍数；避免引入新字号档位（仍用现有 17 或改用 `body` spread）。
  </action>
  <acceptance_criteria>
    - `rg "去记一笔" src/screens/HomeScreen.tsx` 有匹配
    - `rg "ListEmptyComponent" src/screens/HomeScreen.tsx` 有匹配
    - `rg "SpringPressable" src/screens/HomeScreen.tsx` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>ChartScreen 空态与柱色对比</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>src/screens/ChartScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md,src/theme/palette.ts,src/navigation/types.ts</read_first>
  <action>
    1. 在 `ChartScreen` 使用 `useNavigation`，泛型绑定 `RootTabParamList`（或项目内等价的 Tab 导航类型）；调用 **`navigation.navigate("HomeTab", { screen: "CreateBill" })`** 打开记一笔（依赖本 wave 已更新的 `HomeTab` 嵌套参数类型）。  
    2. 将 `chartEmpty` / `empty` 相关视图改为 **引导式**：在现有「暂无」文案下增加 **`去记一笔`** `SpringPressable`，`onPress` 使用上述 `navigate`。  
    3. 柱填充色：确保使用 `colors` 语义（`expense` / `accent` / `chart` 若已存在），**禁止**新增硬编码 `#RRGGBB` 除非来自 `palette` 已有键；若当前有彩虹柱，收敛为 **单色阶或两色（支出/对比）** 并在代码注释引用 `15-UI-SPEC` 语义色条款。  
    4. 不修改图表 Reanimated stagger 数据逻辑（仅色与空态 UI）。
  </action>
  <acceptance_criteria>
    - `rg "去记一笔|记一笔" src/screens/ChartScreen.tsx` 至少 1 匹配
    - `rg "SpringPressable" src/screens/ChartScreen.tsx` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BillQueryScreen 空态 CTA</name>
  <files>src/screens/BillQueryScreen.tsx</files>
  <read_first>src/screens/BillQueryScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    在 `ListEmptyComponent`（或等价空视图）中，保留条件文案逻辑，并增加 **`去记一笔`** `SpringPressable`，`onPress` 导航到记一笔（与项目其他屏相同路由参数）。副文案使用 `iosType.caption1` 或 `footnote` spread（执行前读 `typography.ts` 当前导出字段名）。
  </action>
  <acceptance_criteria>
    - `rg "去记一笔" src/screens/BillQueryScreen.tsx` 有匹配
    - `rg "ListEmptyComponent" src/screens/BillQueryScreen.tsx` 有匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BudgetScreen 空态或引导</name>
  <files>src/screens/BudgetScreen.tsx</files>
  <read_first>src/screens/BudgetScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    若存在「无预算」空视图：文案改为可操作导向（例如「还没有预算」+ **添加预算** 或导航至添加入口），按钮/链接使用 `SpringPressable` + `colors.accent`。若预算始终有默认卡片，则在首屏增加 **简短说明行**（`footnote` + `lightTitle`）不遮挡主进度图形。具体字符串以屏内现有导航方法为准。
  </action>
  <acceptance_criteria>
    - `rg "预算|添加" src/screens/BudgetScreen.tsx` 至少 3 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run verify` 退出码 0
- 手动 spot-check：首页 / 图表 / 查账 空列表均可见主 CTA
</verification>
