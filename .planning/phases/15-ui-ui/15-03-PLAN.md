---
phase: 15-ui-ui
plan: 03
type: execute
wave: 3
depends_on:
  - "15-02-PLAN.md"
gap_closure: false
files_modified:
  - src/screens/AssetScreen.tsx
  - src/screens/MineScreen.tsx
  - src/screens/CreateBillScreen.tsx
  - src/components/BillCalculator.tsx
  - src/screens/CalendarScreen.tsx
  - .planning/phases/15-ui-ui/15-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "资产空态含 **添加资产**（或导航至添加）主操作，文案符合 `15-UI-SPEC.md`"
    - "`BillCalculator` 提交格显示 **保存账单**（原「完成」）；日期选择器工具栏仍可用 **完成**（`15-UI-SPEC` 例外）"
    - "记一笔屏提供 **放弃本次编辑** 头部出口（`headerLeft` 或等价），`onPress` 为 `navigation.goBack()`"
    - "新建 `15-VERIFICATION.md` 含 Before/After spot-check 表与 accent 使用检查项"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/screens/CreateBillScreen.tsx
    - .planning/phases/15-ui-ui/15-VERIFICATION.md
---

<objective>
Wave 3：**资产 / 我的 / 记一笔 / 日历** — 完成 `15-UI-SPEC.md` 剩余 Copywriting 与 focal 提示；补充 **15-VERIFICATION.md** 作为美工验收清单。
</objective>

<threat_model>
## Trust boundaries
- 本地 UI 与文案。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P15-04 | Tampering | N/A |
</threat_model>

<context>
@.planning/phases/15-ui-ui/15-UI-SPEC.md
@src/screens/CreateBillScreen.tsx
@src/screens/AssetScreen.tsx
</context>

<tasks>

<task type="execute">
  <name>AssetScreen 空态与 CTA</name>
  <files>src/screens/AssetScreen.tsx</files>
  <read_first>src/screens/AssetScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    将「暂无账户」空态扩展为：`15-UI-SPEC` 方向 — 说明一句 + **`添加资产`** `SpringPressable`（`onPress` 打开现有「添加账户」导航或 Modal，从文件内已有 handler 复用）。副文案可用 `iosType.caption1`。
  </action>
  <acceptance_criteria>
    - `rg "添加资产" src/screens/AssetScreen.tsx` 有匹配
    - `rg "SpringPressable" src/screens/AssetScreen.tsx` 至少 1 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>MineScreen 层次微调</name>
  <files>src/screens/MineScreen.tsx</files>
  <read_first>src/screens/MineScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md,src/theme/typography.ts</read_first>
  <action>
    按 `15-UI-SPEC`「我的」focal：确保 **标题 / 昵称** 使用 `iosType.largeTitle` 或 `body` + `colors.title`；列表区头使用 `iosType.footnote` + `lightTitle`。仅样式调整，不改业务逻辑。导航栏图标保留并补全 `accessibilityLabel`（若缺失）。
  </action>
  <acceptance_criteria>
    - `rg "iosType\\.(largeTitle|footnote|body)" src/screens/MineScreen.tsx` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>BillCalculator 保存格文案</name>
  <files>src/components/BillCalculator.tsx</files>
  <read_first>src/components/BillCalculator.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    将「完成」键的展示文案改为 **`保存账单`**：`labelFor` 中 `b.kind === "done"` 的返回值，以及 `operateType !== null ? "=" : "完成"` 分支中 **非「=」** 时的字面量（两处需一致为 **保存账单**）。**不要**改日期 picker 的「完成」。
  </action>
  <acceptance_criteria>
    - `rg "保存账单" src/components/BillCalculator.tsx` 至少 2 匹配
    - `rg "kind === \"done\"" src/components/BillCalculator.tsx` 邻近行不出现单独字符串 `完成` 作为提交标签（允许 `=` 分支与其它注释）
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>CreateBillScreen 放弃编辑 header</name>
  <files>src/screens/CreateBillScreen.tsx</files>
  <read_first>src/screens/CreateBillScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md,src/navigation/RootNavigator.tsx</read_first>
  <action>
    使用 `useLayoutEffect` + `navigation.setOptions` 设置 **`headerLeft`**：返回 `SpringPressable` 包裹 `Text`，文案 **`放弃本次编辑`**，`onPress` 调用 `navigation.goBack()`。样式使用 `colors.onMain` 与 `iosType.body`（或 `footnote`）以保持导航栏可读。iOS 若与默认返回冲突，可设 `headerBackVisible: false` 仅在本屏使用自定义左侧（以实际栈行为为准）。**不要**改 `BillCalculator` 内计算逻辑。
  </action>
  <acceptance_criteria>
    - `rg "放弃本次编辑" src/screens/CreateBillScreen.tsx` 有匹配
    - `rg "setOptions|useLayoutEffect" src/screens/CreateBillScreen.tsx` 至少 2 匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>CalendarScreen 空态</name>
  <files>src/screens/CalendarScreen.tsx</files>
  <read_first>src/screens/CalendarScreen.tsx,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    当 `ListEmptyComponent` 或当日无账单时：在「当日无账单」类文案下增加 **`去记一笔`** `SpringPressable`，导航路径与其他屏一致。
  </action>
  <acceptance_criteria>
    - `rg "去记一笔" src/screens/CalendarScreen.tsx` 有匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>15-VERIFICATION 美工验收表</name>
  <files>.planning/phases/15-ui-ui/15-VERIFICATION.md</files>
  <read_first>.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    新建 `.planning/phases/15-ui-ui/15-VERIFICATION.md`，包含：  
    - **Spot-check 表**：Home / Chart / BillQuery / Budget / Asset / Mine / CreateBill / Calendar — 每行：空态文案、主 CTA、accent 是否仅 FAB/主链/分段选中  
    - **Regression**：确认 Phase 14 动效（Spring 缩放、Header collapse）仍正常  
    - **Accepted deviation** 空表  
    引用 `15-UI-SPEC.md` 与根 `UI-SPEC.md`。
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/15-ui-ui/15-VERIFICATION.md`
    - `rg "Spot-check|空态|accent" .planning/phases/15-ui-ui/15-VERIFICATION.md` 至少 3 匹配
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run verify` 退出码 0
- `15-VERIFICATION.md` 存在且可手写勾选
</verification>
