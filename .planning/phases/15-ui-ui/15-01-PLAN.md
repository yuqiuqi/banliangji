---
phase: 15-ui-ui
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: false
files_modified:
  - src/theme/typography.ts
  - src/theme/layout.ts
  - src/components/ios/SegmentedTwo.tsx
  - src/components/ios/GroupedInset.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "`iosType` 各档包含 `15-UI-SPEC.md` 所列 `lineHeight`（nav 22 / body 22 / largeTitle 25 / footnote 18 / caption1 16）"
    - "`SegmentedTwo` 标签字重仅 `400` 或 `600`，字号取自 `iosType`（不再使用 `fontSize: 15` + `fontWeight: 500`）"
    - "`GroupedInset` 仍使用 `radii.card` 与 `shadows.grouped`，未引入根 `UI-SPEC.md` 禁止的粗描边主层次"
    - "未修改 `src/theme/motion.ts` 内 `SPRING` / `FADE_MS` 数值"
    - "`npm run verify` 退出码 0"
  artifacts:
    - src/theme/typography.ts
    - src/components/ios/SegmentedTwo.tsx
---

<objective>
Wave 1：**排版刻度 + 分段控件 + 分组容器** — 落实 `15-UI-SPEC.md` 的 Typography 表（lineHeight）；将 `SegmentedTwo` 收敛到 **两字重**；为垂直节奏在 `layout` 增加 **4 倍数** 导出常量（供后续屏复用）；确认 `GroupedInset` 与契约一致。
</objective>

<threat_model>
## Trust boundaries
- 仅客户端样式；无新网络面。

## STRIDE（ASVS L1）
| threat_id | category | disposition |
|-----------|----------|-------------|
| P15-01 | Tampering | N/A — UI 样式 |
| P15-02 | Information disclosure | N/A |
</threat_model>

<context>
@.planning/phases/15-ui-ui/15-UI-SPEC.md
@UI-SPEC.md
@.planning/phases/14-ios26-motion-polish/14-UI-SPEC.md
</context>

<tasks>

<task type="execute">
  <name>iosType 补 lineHeight</name>
  <files>src/theme/typography.ts</files>
  <read_first>src/theme/typography.ts,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    在 `src/theme/typography.ts` 的 `iosType` 每一档对象上增加 `lineHeight` 字段，数值 **必须** 与 `15-UI-SPEC.md` § Typography 表一致：  
    - `navTitle`: 22  
    - `body`: 22  
    - `largeTitle`: 25  
    - `footnote`: 18  
    - `caption1`: 16  
    保留现有 `fontSize` 与 `fontWeight`，不新增第五档字号。
  </action>
  <acceptance_criteria>
    - `rg "lineHeight: 22" src/theme/typography.ts` 至少 2 处匹配
    - `rg "lineHeight: 25" src/theme/typography.ts` 有匹配
    - `rg "lineHeight: (18|16)" src/theme/typography.ts` 至少 2 处匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>layout 垂直节奏 token</name>
  <files>src/theme/layout.ts</files>
  <read_first>src/theme/layout.ts,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    在 `src/theme/layout.ts` 导出 **命名常量**（值须为 4 的倍数）：  
    - `sectionGapTight = 16`  
    - `sectionGapComfort = 24`  
    注释：`15-UI-SPEC` 垂直节奏；供列表与分组块之间 margin 使用。  
    不删除或重命名现有 `listContentInset`、`radii`、`shadows`。
  </action>
  <acceptance_criteria>
    - `rg "sectionGapTight|sectionGapComfort" src/theme/layout.ts` 至少 2 匹配
    - `rg "sectionGapTight = 16|sectionGapComfort = 24" src/theme/layout.ts` 有匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>SegmentedTwo 字阶收敛</name>
  <files>src/components/ios/SegmentedTwo.tsx</files>
  <read_first>src/components/ios/SegmentedTwo.tsx,src/theme/typography.ts,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    将 `buildSegStyles` 中 `text` / `textOn` 的 `fontSize` 与 `fontWeight` 改为基于 `iosType`：  
    - 使用 `...iosType.footnote` 作为基底（或 `body` 若 footnote 视觉上过轻），**选中态** `fontWeight: "600"`，未选中 `fontWeight: "400"`  
    - **删除** `fontSize: 15` 与 `fontWeight: "500"` 字面量  
    - `textOn` 的 `color` 仍用 `colors.title`；未选中 `colors.lightTitle`  
    不改动 `SPRING.THUMB`、`thumb` 布局逻辑与 `hapticSelect` 调用。
  </action>
  <acceptance_criteria>
    - `rg "fontSize: 15|fontWeight: \"500\"" src/components/ios/SegmentedTwo.tsx` 无匹配
    - `rg "iosType" src/components/ios/SegmentedTwo.tsx` 有匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>GroupedInset 对照验收</name>
  <files>src/components/ios/GroupedInset.tsx</files>
  <read_first>src/components/ios/GroupedInset.tsx,UI-SPEC.md,.planning/phases/15-ui-ui/15-UI-SPEC.md</read_first>
  <action>
    审阅 `GroupedInset`：确认 `marginHorizontal: 16` 等于 `listContentInset`（可改为 `import { listContentInset } from "../../theme/layout"` 并用于 `marginHorizontal`，若当前为魔法数 16）。  
    确认 **无** `borderWidth: 1` + 实色粗外框作为主层次。若已符合，仅做 `listContentInset` 统一替换，不改阴影实现。
  </action>
  <acceptance_criteria>
    - `rg "listContentInset" src/components/ios/GroupedInset.tsx` 有匹配 **或** `rg "marginHorizontal: 16" src/components/ios/GroupedInset.tsx` 有匹配且文件内注释说明对齐 `listContentInset`
    - `rg "borderWidth: 1" src/components/ios/GroupedInset.tsx` 无匹配
    - `npm run verify` 退出码 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run verify` 退出码 0
- `rg "damping:" src/theme/motion.ts` 与 Phase 14 合并前一致（本 wave 不应编辑该文件）
</verification>
