---
phase: 16
plan: 16-01
wave: 1
depends_on: []
autonomous: true
requirements:
  - DESIGN-SPEC-02
files_modified:
  - .planning/IOS26-DESIGN-GUIDE.md
objective: "P0 止血：修正 2 条 High 事实错误 + 解决 3 处规范冲突，防止下游 Phase 17 以错误前提开发"
---

# Plan 16-01: P0 — 事实错误与规范冲突（先止血）

## 目的

对 `IOS26-DESIGN-GUIDE.md` v1.1 执行 **REVIEW §七 P0 清单 5 条** 的修订。这些是**最小可信度基线**：不改则 v1.1 自相矛盾或传播错误事实，Phase 17 编码将建立在错误前提上。

## must_haves（逐条对应 REVIEW）

- v1.2 文档不再把 `glassEffect` / `GlassEffectContainer` / `glassEffectID` 标为「私有 API」（Err-01 闭环）
- v1.2 文档不再把 17pt 叙述为「硬性最小」（Err-03 闭环）
- §1.2 R1 / §2 Layer 2 明确对齐，FAB 归属不再矛盾（Conflict-01 闭环）
- §3.10 与 §21 D2 无规范级冲突（Conflict-02 闭环）
- §1.3 R5 与 §14 无「声音同帧」虚假承诺（Conflict-03 闭环）

---

## Task 16-01-1 — 修正 SwiftUI Glass API 私有/公开表述（Err-01, High）

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md`（**前 20 行**，权威来源段）
- `.planning/IOS26-DESIGN-GUIDE-REVIEW.md` §二 Err-01
- `https://developer.apple.com/documentation/swiftui/view/glasseffect(_:in:)`
- `https://developer.apple.com/documentation/swiftui/glasseffectcontainer`
- `https://developer.apple.com/documentation/swiftui/view/glasseffectid(_:in:)`
</read_first>

<action>
在 `.planning/IOS26-DESIGN-GUIDE.md` 开头「权威来源」段之后的「RN/Expo 边界」行，把：

> **RN/Expo 边界：** SwiftUI 私有 `.glassEffect()` / `GlassEffectContainer` / `glassEffectID` 等 API 无法直接迁移；本文为每条规范提供 **React Native 工程近似** 与 **Accepted Deviation** 判定标准。

改为：

> **RN/Expo 边界：** SwiftUI **公开 API** `.glassEffect(_:in:)` / `GlassEffectContainer` / `glassEffectID(_:in:)` / `glassEffectUnion(id:namespace:)` 均为 iOS 26 SwiftUI 官方文档化接口（见 [Liquid Glass Overview](https://developer.apple.com/documentation/TechnologyOverviews/liquid-glass)），但**无 React Native 原生对应**；本文为每条规范提供 **React Native 工程近似** 与 **Accepted Deviation** 判定标准。 🍎 OFF
</action>

<acceptance_criteria>
- `grep -n "SwiftUI 私有" .planning/IOS26-DESIGN-GUIDE.md` 返回空（无匹配）
- `grep -n "SwiftUI **公开 API**" .planning/IOS26-DESIGN-GUIDE.md` 返回非空
- `grep -n "glassEffectUnion" .planning/IOS26-DESIGN-GUIDE.md` 在权威来源段出现至少 1 次（为 Plan 16-02 新增章节铺路）
</acceptance_criteria>

---

## Task 16-01-2 — 修正 17pt 默认 / 最小混淆（Err-03, High）

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §5.3 排版铁律（定位 `"最小正文 17pt"`）
- `.planning/IOS26-DESIGN-GUIDE-REVIEW.md` §二 Err-03
</read_first>

<action>
在 `.planning/IOS26-DESIGN-GUIDE.md` §5.3「排版铁律」Rule 1，把：

> 1. **最小正文 17pt**（HIG 硬性要求 + 可访问性）—— `body` 以下（13 / 12）仅用于辅助。

改为：

> 1. **推荐正文 17pt**（Apple Typography 默认值，见 [HIG Typography](https://developer.apple.com/design/human-interface-guidelines/typography) 🍎 OFF）；**技术最小 11pt**（系统允许），**本仓库下限 12pt**（仅 `caption1` 使用），13pt 以上（`footnote`）为辅助信息区，17pt（`body` / `navTitle`）为主信息区。
</action>

<acceptance_criteria>
- `grep -n "最小正文 17pt" .planning/IOS26-DESIGN-GUIDE.md` 返回空
- `grep -n "推荐正文 17pt" .planning/IOS26-DESIGN-GUIDE.md` 返回 §5.3 位置
- `grep -n "技术最小 11pt" .planning/IOS26-DESIGN-GUIDE.md` 返回非空
</acceptance_criteria>

---

## Task 16-01-3 — 解决玻璃范围矛盾（Conflict-01）

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §1.3 R1（「玻璃只用于导航层，绝不用于内容」）
- `.planning/IOS26-DESIGN-GUIDE.md` §2 分层模型表格
- `.planning/IOS26-DESIGN-GUIDE.md` §9.5 FAB
- `.planning/IOS26-DESIGN-GUIDE-REVIEW.md` §五 Conflict-01
</read_first>

<action>
在 `.planning/IOS26-DESIGN-GUIDE.md` §1.3「五条潜规则」表格 R1 那行，把：

> | R1 | **玻璃只用于导航层，绝不用于内容** | 列表行本体、卡片体、图表背景若加玻璃，将与控件视觉混战，信息层级崩塌。 |

改为：

> | R1 | **玻璃只用于 Layer 2（导航 / 控件 / 浮层），不用于 Layer 1 内容** 🍎 OFF | 列表行本体、卡片体、图表背景（Layer 1）若加玻璃，将与控件视觉混战、信息层级崩塌；Layer 2 涵盖 Toolbar / TabBar / Sheet / FAB / Popover / 分段控件 —— 见 §2 分层模型表格。 |
</action>

<acceptance_criteria>
- `grep -n "玻璃只用于导航层，绝不用于内容" .planning/IOS26-DESIGN-GUIDE.md` 返回空
- `grep -n "玻璃只用于 Layer 2（导航 / 控件 / 浮层），不用于 Layer 1 内容" .planning/IOS26-DESIGN-GUIDE.md` 返回 §1.3 位置
- §2 分层模型表格中 FAB 仍归类 L2（未被本 task 破坏）
</acceptance_criteria>

---

## Task 16-01-4 — 解决 GlassEffectContainer 必须/建议冲突（Conflict-02）

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §3.10 G1 GlassEffectContainer
- `.planning/IOS26-DESIGN-GUIDE.md` §21 D2 Accepted Deviation 表
- `.planning/IOS26-DESIGN-GUIDE-REVIEW.md` §五 Conflict-02
</read_first>

<action>
在 `.planning/IOS26-DESIGN-GUIDE.md` §3.10 的「应用场景（强制）」小节，把：

> **应用场景（强制）：**
> - TabBar（多 Tab 图标）
> - 工具栏多按钮（如 Chart 周/月/年分段 + 粒度分段并列时）
> - FAB 展开菜单（主 FAB + 子 FAB 同为玻璃态时）
> - Modal 内的分段控件组

改为：

> **应用场景（建议）：**
> - TabBar（多 Tab 图标）
> - 工具栏多按钮（如 Chart 周/月/年分段 + 粒度分段并列时）
> - FAB 展开菜单（主 FAB + 子 FAB 同为玻璃态时）
> - Modal 内的分段控件组
>
> 🍎 OFF（SwiftUI 官方行为） 🛠️ RN-T2（近似，非物理等价）
>
> **降级路径：** 若 `GlassEffectContainer` 模式的工程复杂度（多层嵌套、跨导航路由）不允许，按 **§21 D2 Accepted Deviation** 使用独立 `BlurView`，但须在对应相位 `VERIFICATION.md` 登记偏差原因。
</action>

<acceptance_criteria>
- `grep -n "应用场景（强制）" .planning/IOS26-DESIGN-GUIDE.md` 返回空
- `grep -n "应用场景（建议）" .planning/IOS26-DESIGN-GUIDE.md` 返回 §3.10 位置
- `grep -n "降级路径" .planning/IOS26-DESIGN-GUIDE.md` 在 §3.10 附近出现
- §21 D2 条目未被本 task 触动
</acceptance_criteria>

---

## Task 16-01-5 — 解决 R5 三位一体 vs §14 无声音冲突（Conflict-03）

<read_first>
- `.planning/IOS26-DESIGN-GUIDE.md` §1.3 R5（「视觉 × 触觉 × 声音 同帧触发」）
- `.planning/IOS26-DESIGN-GUIDE.md` §14 声音反馈章节
- `.planning/IOS26-DESIGN-GUIDE.md` §15.1 即时反馈三位一体
- `.planning/IOS26-DESIGN-GUIDE-REVIEW.md` §五 Conflict-03
</read_first>

<action>
在 `.planning/IOS26-DESIGN-GUIDE.md` §1.3 R5 行，把：

> | R5 | **视觉 × 触觉 × 声音 同帧触发** | 任何交互的视觉反馈出现时，触觉必须**同一帧**触发；异步 → 廉价感。 |

改为：

> | R5 | **视觉 × 触觉 同帧触发**（声音交给系统，不自建） 📝 AUTH | 任何交互的视觉反馈出现时，触觉必须**同一帧**触发；异步 > 50ms 即为缺陷。音效遵守 §14 原则 —— 默认不引入自定义声音，由系统触觉附带的微声承担。 |

并同步修订 §15.1「即时反馈三位一体」段落：

> ```
> 用户操作 → 视觉（按压/移动）  ──┐
>           → 触觉（同帧）      ─ 100ms 内全部到位
>           → 声音（可选/系统） ──┘
> ```

改为：

> ```
> 用户操作 → 视觉（按压/移动） ──┐
>           → 触觉（同帧）     ─ 100ms 内双轨到位
> ```
> （声音：默认交给系统触觉附带的微声；自定义音效见 §14 —— 非常规情况不引入）
</action>

<acceptance_criteria>
- `grep -n "视觉 × 触觉 × 声音 同帧触发" .planning/IOS26-DESIGN-GUIDE.md` 返回空
- `grep -n "视觉 × 触觉 同帧触发" .planning/IOS26-DESIGN-GUIDE.md` 返回 §1.3 位置
- `grep -c "声音" .planning/IOS26-DESIGN-GUIDE.md` 在 §15.1 附近出现时带明确「可选」或「系统」修饰
- §14 章节内容未被本 task 修改（只改 R5 + §15.1 对齐）
</acceptance_criteria>

---

## Wave 1 整体验收（must_haves 勾选）

- [ ] 5 个 task 全部 acceptance_criteria 通过
- [ ] `npm run verify` 未受影响（本 Wave 仅改 markdown）—— 若无原依赖问题应保持绿
- [ ] 文档内部自洽性：`grep -n "SwiftUI 私有"` / `grep -n "最小正文 17pt"` / `grep -n "玻璃只用于导航层，绝不用于内容"` / `grep -n "应用场景（强制）"` / `grep -n "视觉 × 触觉 × 声音 同帧触发"` 全部返回空
- [ ] 文档修订记录 §附录 C 由 Wave 3 统一更新（本 Wave 不触碰）

## 提交

原子提交命名：`docs(16-01): P0 止血 — 事实错误与规范冲突 5 条闭环`
