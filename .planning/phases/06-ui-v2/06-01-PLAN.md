---
phase: 06-ui-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/layout.ts
autonomous: true
requirements:
  - REF-04
user_setup: []
must_haves:
  truths:
    - "src/theme/layout.ts 含 pressedOpacity、pressScale、pressDurationMs 与 radii.chip、shadows.raised"
    - "npm run verify 退出码 0"
  artifacts:
    - src/theme/layout.ts
  key_links:
    - "06-UI-SPEC.md Motion & interaction tokens 与代码常量一致"
---

<objective>
Phase 6 Wave 1：**设计令牌扩展** — 在 `layout.ts` 中落地 `06-UI-SPEC.md` 的 motion / chip / raised shadow 常量，供 06-02 逐屏引用。不修改任何 Screen 文件。
</objective>

<threat_model>
## Trust boundaries
- 仅本机主题常量；无网络、无新权限。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P6-T01 | Tampering | 无 | Low / N/A | 无持久化业务数据变更 |
| P6-T02 | I.D. | 无 | Low | — |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-v2/06-CONTEXT.md
@.planning/phases/06-ui-v2/06-UI-SPEC.md
@.planning/phases/06-ui-v2/06-RESEARCH.md
@src/theme/layout.ts
@src/theme/colors.ts
</context>

<tasks>

<task type="auto">
  <name>Motion + shape tokens in layout.ts</name>
  <files>src/theme/layout.ts</files>
  <read_first>
    src/theme/layout.ts,
    .planning/phases/06-ui-v2/06-UI-SPEC.md,
    src/theme/colors.ts
  </read_first>
  <action>
    在 `src/theme/layout.ts` 中 **追加**（保留现有 `radii`、`shadows.card`、`hairlineBorder` 导出不变）：
    1) 导出常量对象或扁平常量：`pressedOpacity = 0.92`、`pressScale = 0.98`、`pressTranslateY = 1`、`pressDurationMs = 100`（数值与 `06-UI-SPEC.md` 表格一致）。
    2) 扩展 `radii`：增加 `chip: 10`（若 `as const` 对象，则合并进现有 `radii`）。
    3) 扩展 `shadows`：增加 `raised`，使用 `Platform.select`，iOS：`shadowColor: "#000"`, `shadowOffset: { width: 0, height: 2 }`, `shadowOpacity: 0.1`, `shadowRadius: 10`；Android：`elevation: 3`。
    所有新常量须从本文件导出，供 Screen  `import { pressedOpacity, pressScale, radii, shadows } from "../theme/layout"`（路径以调用方为准）。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `src/theme/layout.ts` 含标识符 `pressedOpacity` 且值为 `0.92`
    - 同文件含 `pressScale` 且值为 `0.98`
    - 同文件含 `radii` 与 `chip`（grep：`chip`）
    - 同文件含 `raised`（grep：`raised`）
    - 命令 `npm run verify` 在项目根目录退出码为 0
  </acceptance_criteria>
  <done>主题层 motion / chip / raised 已导出且 verify 绿。</done>
</task>

</tasks>
