---
phase: 06-ui-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/screens/HomeScreen.tsx
  - src/screens/CreateBillScreen.tsx
  - src/screens/BillDetailScreen.tsx
  - src/screens/CalendarScreen.tsx
  - src/screens/ChartScreen.tsx
  - src/screens/BudgetScreen.tsx
  - src/screens/AssetScreen.tsx
  - src/screens/MineScreen.tsx
  - src/navigation/RootNavigator.tsx
autonomous: true
requirements:
  - REF-04
user_setup: []
must_haves:
  truths:
    - "关键屏 Pressable 使用 layout.ts 的 pressedOpacity 或 pressScale"
    - "ChartScreen 未修改 chartAggregate 语义"
    - "npm run verify 绿"
  artifacts:
    - src/screens/HomeScreen.tsx
    - src/screens/ChartScreen.tsx
  key_links:
    - "06-UI-SPEC 实现对照清单可在 PR 描述中勾选"
---

<objective>
Phase 6 Wave 2：**逐屏按压与层次** — 按 `06-UI-SPEC.md` 为明细、记一笔、详情、日历、图表**壳**、预算、资产、我的及 Tab 栏提供统一按压反馈与可选略强阴影；**禁止**改动 `chartAggregate.ts` 内聚合逻辑。
</objective>

<threat_model>
## Trust boundaries
- 仅 UI；SQLite 与网络行为不变。

## STRIDE（ASVS L1）
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| P6-T03 | Tampering | main.db | Low | 本计划不修改 `src/db/*` |
| P6-T04 | I.D. | 无 | Low | — |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-v2/06-CONTEXT.md
@.planning/phases/06-ui-v2/06-UI-SPEC.md
@.planning/phases/06-ui-v2/06-PATTERNS.md
@.planning/phases/03-chart-consistency/03-CONTEXT.md
@src/chart/chartAggregate.ts
</context>

<tasks>

<task type="auto">
  <name>HomeScreen — 按压 + 汇总层次</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    src/screens/HomeScreen.tsx,
    src/theme/layout.ts,
    .planning/phases/06-ui-v2/06-UI-SPEC.md
  </read_first>
  <action>
    1) 从 `src/theme/layout.ts` 导入 `pressedOpacity`、`pressScale`（及按需 `shadows`）。
    2) 将 **所有** 交互用 `Pressable` 的 `style` 改为函数形态：`style={({ pressed }) => [baseStyle, pressed && { opacity: pressedOpacity }]` 或等价使用 `pressScale` 的 `transform`（与 `02-UI-SPEC` 一致，二选一或组合，全屏统一一种主策略）。
    3) **汇总条 / 顶区**（含当月收入支出展示容器）：在现有 `shadows.card` 或新增使用 `shadows.raised` 之一，使相对列表 **略强** 层次；具体容器以当前 JSX 中包裹汇总数字的最外层 `View` 为准。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `src/screens/HomeScreen.tsx` 含从 `../theme/layout` 或 `src/theme/layout` 的 `pressedOpacity` 或 `pressScale` 导入
    - 同文件至少 2 处 `Pressable` 使用 `pressed` 参数（grep：`pressed`）
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>明细屏按压与汇总层次已落地。</done>
</task>

<task type="auto">
  <name>CreateBillScreen + BillDetailScreen + CalendarScreen</name>
  <files>src/screens/CreateBillScreen.tsx, src/screens/BillDetailScreen.tsx, src/screens/CalendarScreen.tsx</files>
  <read_first>
    src/screens/CreateBillScreen.tsx,
    src/screens/BillDetailScreen.tsx,
    src/screens/CalendarScreen.tsx,
    src/theme/layout.ts,
    .planning/phases/06-ui-v2/06-UI-SPEC.md
  </read_first>
  <action>
    对上述三文件中 **每一个** `Pressable` 或等价可点主控件，应用与 `HomeScreen` **相同** 的 pressed 策略（`pressedOpacity` 或 `pressScale`）。  
    `CreateBillScreen` 的 **类别网格** 与其它 `Pressable` 均使用函数式 `style` + `pressedOpacity` 或 `pressScale`。  
    不得改变保存/导航的业务逻辑与路由名。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `CreateBillScreen.tsx` 出现 `pressedOpacity` 或 `pressScale` 且从 `layout` 或 `theme/layout` 导入
    - `BillDetailScreen.tsx` 与 `CalendarScreen.tsx` 至少一处含 `pressed` 回调样式
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>记一笔 / 详情 / 日历按压一致。</done>
</task>

<task type="auto">
  <name>ChartScreen — 仅壳层与分段</name>
  <files>src/screens/ChartScreen.tsx</files>
  <read_first>
    src/screens/ChartScreen.tsx,
    src/chart/chartAggregate.ts,
    .planning/phases/06-ui-v2/06-UI-SPEC.md,
    .planning/phases/03-chart-consistency/03-CONTEXT.md
  </read_first>
  <action>
    1) **不得** 修改 `src/chart/chartAggregate.ts` 内任何导出函数的实现与类型。  
    2) 仅调整 **周/月/年分段器**、**外层卡片容器**、**标题区** 的样式与 `Pressable` pressed 态；可应用 `radii.chip` 于分段容器。  
    3) 不得修改用于聚合的 `useMemo` 依赖、`query` 区间计算、或传入 `chartAggregate` 的数据变换。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `src/chart/chartAggregate.ts` 相对 `git diff` 无改动（执行后 `git diff src/chart/chartAggregate.ts` 为空）
    - `ChartScreen.tsx` 含 `pressedOpacity` 或 `pressScale` 或分段相关 `Pressable` 的 `pressed` 处理
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>图表壳层按压完成且数据层未动。</done>
</task>

<task type="auto">
  <name>BudgetScreen + AssetScreen + MineScreen</name>
  <files>src/screens/BudgetScreen.tsx, src/screens/AssetScreen.tsx, src/screens/MineScreen.tsx</files>
  <read_first>
    src/screens/BudgetScreen.tsx,
    src/screens/AssetScreen.tsx,
    src/screens/MineScreen.tsx,
    src/theme/layout.ts,
    .planning/phases/06-ui-v2/06-UI-SPEC.md
  </read_first>
  <action>
    为预算/资产列表行、CTA、可点进度区应用与 Home 一致的 **pressedOpacity 或 pressScale**。  
    `MineScreen` 仅 **令牌对齐**：凡有 `Pressable` 则统一 pressed；**不** 新增大段信息架构或新路由。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `BudgetScreen.tsx` 与 `AssetScreen.tsx` 各至少一处 `layout` 主题 pressed 导入或使用
    - `MineScreen.tsx` 含 `pressedOpacity` 或 `pressScale` 的主题导入且 `Pressable` 使用 `pressed` 样式
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>预算 / 资产 / 我的按压对齐。</done>
</task>

<task type="auto">
  <name>RootNavigator — Tab 按下反馈</name>
  <files>src/navigation/RootNavigator.tsx</files>
  <read_first>
    src/navigation/RootNavigator.tsx,
    src/theme/layout.ts,
    .planning/phases/06-ui-v2/06-RESEARCH.md
  </read_first>
  <action>
    为 `Tab.Navigator` 的 **每个** `Tab.Screen` 增加 `options.tabBarButton`，使用 React Navigation 支持的 props：渲染 `Pressable` 包裹默认 children，`onPress={props.onPress}`、`accessibilityRole="button"`，pressed 时应用 `opacity: pressedOpacity`。  
    若 TypeScript 对 `children` 报错，使用 `@ts-expect-error` **单行** 注释并附理由 `react-navigation tabBarButton children`（**禁止** 文件级 disable）。  
    不得改变 `name`、 `component`、`tabBarIcon` 的 **name** 与路由结构。
  </action>
  <verify>cd /Users/macos/gsd-workspaces/banliangji && npm run verify</verify>
  <acceptance_criteria>
    - `RootNavigator.tsx` 含字符串 `tabBarButton`
    - 同文件含 `Pressable` 与 `pressedOpacity` 或 `pressScale`
    - `npm run verify` 退出码 0
  </acceptance_criteria>
  <done>Tab 可感知按压。</done>
</task>

</tasks>
